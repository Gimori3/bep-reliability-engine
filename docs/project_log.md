# Project log

The dated record of what was built, measured, refuted and closed in this
repository. It was split verbatim from untracked project notes on **2026-08-20**
because those notes had no version history or undo. This tracked log now has
both.

Current architecture and decision records answer *what is true now*; this file
answers *when we learned it, and what we believed before*. The separation keeps
current constraints concise while preserving dated history.

## How to use this file

* **Entries are dated and appended. They are never rewritten.** An entry states
  what was true and believed on its date. When a later result overtakes it, the
  original text stays and a **bracketed, dated marker** is inserted beside the
  affected number, pointing at what replaced it and at the artifact that settles
  it. Deleting the old value would destroy the record of when the project
  believed it; leaving it unmarked is what made a 2026-08-05 thesis check
  re-derive two superseded Stage 6.6 component figures from scratch.
* **This file is not authoritative for any current number.** Where an entry and
  a document of record differ, the document of record governs:
  `docs/architecture.md` (the implementation spec), the accepted ADRs in
  `docs/decisions/`, and the four reports (`phase2_report.md`,
  `phase3_report.md`, `stage6_6_report.md`,
  `production_campaign_2026-07-29.md`) **including their dated addenda, which
  are authoritative where they differ from their own bodies**.
* **Grammar.** This is an undated `docs/<name>.md`, i.e. a document of record
  under `docs/conventions.md` section 9.1, kept current by appending dated
  sections rather than by editing the body. That is the same pattern as
  `phase2_report.md` sections 11 to 14.

Adding an entry: one bullet per closed piece of work, newest last, dated
`YYYY-MM-DD`, naming its driver, its evidence artifact and its gates. The
mechanics are in `docs/conventions.md`.

## Entries

- **Run robustness and monitoring (2026-07-03 health-assessment fixes).** A persisting run writes the raw failure payload to `<output>.raw.h5` *before* M9 fitting (crash recovery; removed on success). The M9 bootstrap skips degenerate replicates instead of raising (counts in `metadata['bootstrap_degenerate_replicates']`). The spec §11 CoV of the P_f estimator is computed per level and recorded (`metadata['mc_convergence']`). The ADR-0006 (amended 2026-07-05) leakage-geometry record runs once per sweep (`metadata['leakage_geometry']`: median leakage lengths, foreland tanh credit, r_e denominator shares, hinterland semi-infinite status + 3·λ_in threshold; descriptive only — the former L/λ_in alarm was withdrawn as a category error: L is the exact linear USACE-L₂ term, never inside a tanh). The r_e ratio form is the exact USACE (2000) Case 7a / TAW (2004) Model 4A solution; Pol thesis Eq. 7.13 is its no-riverside-blanket special case. Hinterland extents resolved conservative (ADR-0006 amendment). The KP62.0 foreland is resolved by ADR-0025: **blanketed baseline adopted**; the open-entry end (x₁ = 0, r_e +37%) is an evidence-disfavored on-demand sensitivity via `config.foreland_treatment: open_entry` (threaded to M8 as the keyword-only `foreland_open`; default `blanketed_tanh` is bit-identical to prior behaviour; never a generated sweep member — drift guard pins it). Fragility fits are anchored to the load excess `h − z_toe` with inverse-variance probit weights (`metadata['fragility_fit']`; `LognormFragility.datum_m`).

- **M7 timestepper acceleration (ADR-0029, implemented 2026-07-07).** The numpy `integrate_progression` loop is restructured (hoisted time-invariant factors, gate-masked `**0.81` power, whole-step skip below `z_toe` under the instantaneous head) and stays **bit-identical** to the straightforward loop over the public M5/M6 kernels — pinned exactly by `tests/test_progression_fastpath.py` (KP58.8 N=1e5 sweep: 46 s → 10 s). An **opt-in Numba backend** (`config.timestepper.progression_backend: numba`, optional `[accel]` extra, `progression_numba.py`) swaps the loop nest (realizations outer via `prange`) for another ~4× (sweep → 2.7 s): equivalence is **< 1e-10, not bit-identity** (platform `pow` ulp; empirically exactly 0 on the dev machine), so the backend is config-owned, stamped in `metadata['progression_backend']`, refused with the aquifer lag, and never touches the frozen scalar `evaluate_realization`. JAX was evaluated and rejected with measurements (5.6× slower than Numba, 5e-7 deviation, needs numpy≥2). Next bottleneck after acceleration: the M9 bootstrap (~19 s Python loop), untouched.

- **Statistical convergence study (ADR-0031, 2026-07-10).** An empirical N-ladder (R=50 replicates, `N` from 10³–3·10⁵) on both reachable sections (KP58.8, KP60.0) confirms **N=1e5 sufficiency**: the empirical LHS CoV of transient P̂_f falls as 1/√N and meets the spec §11 <5% target down to per-level transient P_f ≈ 5e-3 (degrading below ~1e-3, the ADR-0024 raw-tail-CI regime by design). It also **reproduces the fm5 tail-variance refutation on two independent sections**: the LHS-vs-crude-MC variance-reduction ratio decays from ≈1.4–1.5 in the bulk to ≈1.0 (parity) in the deep transient tail, while the static branch (no C_e) keeps its LHS advantage throughout — isolating the C_e×k_aq interaction (fm7) as the cause. Reusable code: `convergence.py` (stats only, physics injected) + `scripts/convergence_study.py` (driver, supports `--config`/`--levels` for further sections); companion note `adr0031-convergence-study.md`.

- **Aquifer-response diagnostic executed — instantaneous M4 retained (ADR-0032, 2026-07-11).** The spec §11 lag gate was **pre-registered** (S_s ∈ [1e-5, 1e-4] m⁻¹ with the upper bound as driver; threshold Π\* = 0.10 against the rising-limb time T_rise) and then executed unchanged: Π ≈ 0.010–0.012 at the τ_aq-bounding governing pair KP58.8/KP60.0 (~10× margin; even the p99-τ × flashiest-limb stress case clears; **S_s did not bind**, so no uncertain input was added). **Both those figures are Π_central over the ensemble-median T_rise = 18 h** (2026-07-31 labelling fix): the same τ_aq (680 s / 765 s) over the run's own canonical-event T_rise = 23 h gives the stamped `pi_central` 0.00821–0.00924, and the per-run `margin_vs_threshold` is the stricter Π\*/Π_**corner90** = 3.83–19.54. No number changed; the denominator and the numerator are now stated wherever either margin appears (reconciled in the companion note's "Two margins" section). **D5's pre-registered bounding pair is confirmed at all eight strata**: τ_aq central 765 (KP60.0) and 680 s (KP58.8) are the two largest against 350 (KP57.4) and 150 s (KP62.0), so the pair also carries the smallest margins — argued from the priors before any τ_aq existed. The measured loading is **not flashy** (median T_rise 18 h, plateau 9 h — the spec's old "~1.5 h plateau" characterization is retired), so the 3600 s native cadence resolves the peak (~9 samples). `aquifer_lag_active: false` is now *evidenced*, not inherited. Wiring: `hydraulics.aquifer_response_diagnostic` + `AQUIFER_RESPONSE_*` constants, `hydrographs.flood_timescales`, stamped per run as `metadata['aquifer_response']`. Scope amendment: the Π screen detects elastic leaky-confined response only — valid where the aquifer is channel-connected and saturated at base flow (confirmed for all four production sections); outside that regime the instantaneous form over-translates the gate head (conservative post-ADR-0027/0028).

- **Japanese case-validation campaign closed (2026-07-11).** Three literature cases validate the engine against observed piping/no-piping outcomes and calibrated FEMs: Gounokawa (gate ~2.3× conservative; single-k schematization decisive), Yabe (the transient race condition passes its first field test: correct 3-site ranking, timeline bracketed), Shikaga Case A (M5 exonerated with FEM heads; the M4 instantaneous-translation factor 1.13–2.7× keyed on channel connectedness, not elastic storage — not a Tokachi production blocker). Cross-case threads: no discrete pipe 3/3, C_e on the fast side, static comparator conservative 4/4. Notes `docs/validation/{gounokawa-shimohara,yabe,shikaga}-case.md`, synthesis `_thesis_validation_japan.tex`, drivers `scripts/validate_*.py`. Registered follow-up: a **KP58.8 r_e-halved QA member** in the next re-sweep (`scripts/run_sweep.py` header; ADR-0032 scope amendment) converting the shoulder bound into a measured ΔP_f.

- **Per-branch fragility deliverable (ADR-0024, implemented 2026-07-06).** Fits are Optional (`P_f_*_fit: LognormFragility | None` — a branch with <2 interior points yields None, M9 never aborts a completed sweep). Every result carries always-on Clopper–Pearson 95% CIs on the raw points (`FragilityResult.binomial_ci`, additive field per the ADR-0017 precedent, persisted under `/binomial_ci/*`; legacy files recompute on load). `metadata['fragility_deliverable']` flags per branch: `form` (`fitted_lognormal` where the data-driven criterion max raw P_f ≥ 0.5 brackets the transition and a fit exists, else `raw_tail_binomial`), `transition_bracketed`, `max_p_f_raw`, `fit_role` (`deliverable`/`extrapolative_only`/`unavailable`). Raw-tail-with-CIs is the **intended primary transient presentation** where the transition is unreachable (per-level probability ratios against the static curve), not a fallback. KP62.0's grid carries the static-bracketing extension (0.5 m steps 51.0–56.5 m MSL, N_h=38; hypothetical fit-stabilizers above crest and max attainable stage — never plot as attainable).

- **Variance-based GSA (ADR-0033, Stage 6.5, 2026-07-12).** Sobol' S_i/ST_i of the 8D production input space (7 θ + stochastic L) for four QoIs (transient/static failure indicators, l_e/L, Z_static) at four conditioning levels on the governing pair KP58.8+KP60.0. New code: `sensitivity.py` (scrambled-Sobol' radial design, Saltelli-2010 S_i + Jansen ST_i, replicate-t + row-bootstrap CIs, `GsaInputSpace` generator→physical map bit-identical to M2 — physics-free, `convergence.py` pattern), `gsa_qoi.py` (M8-mirroring continuous-output batch adapter, failure flags bit-identical to `evaluate_batch`, drift-guarded), `scripts/gsa_study.py` (driver + figures), machinery validated on Ishigami/g-function/threshold-indicator/correlated-copula benchmarks (`tests/test_sensitivity.py`). Correlated inputs: production prior is independent (ADR-0012), so plain Sobol' is exact; a Nataf ρ=0.6 bounding companion runs via the Rosenblatt/generator route (Mara–Tarantola full/independent indices). Headline: the stochastic seepage length L is the top total-effect input for every QoI; C_e×k_aq carries the fm7 interaction gap. Records: `docs/decisions/adr0033-gsa-study*.json`, note `adr0033-gsa-study.md`, report `_thesis_gsa.tex`.

- **Phase 2 built (2026-07-12): `bayesian_reliability_updating` package (ADR-0034/0035/0036).** The complete Bayesian reliability updating framework exists as a second top-level package: Accept-Reject filtering of the Phase 1 prior against the observed 2016 typhoon survival (Schweckendiek 2014 direct updating; thesis methodology). Key facts: all evaluation goes through M8 (`evaluate_batch_diagnostics`, the ADR-0034 additive batch twin — `evaluate_batch` now delegates to it); the replay reconstructs the run from the metadata snapshot (config hash-checked, theta regenerated bit-for-bit, stochastic L regenerated via the public `run.seepage_length_samples_for_config`); replay Δt = the run's own 225 s grid (ADR-0036 supersedes ADR-0022 decision 2's 1800 s); h_2016 per section = observed Obihiro stage → inverse gauge rating → section rating (verbatim M3) → peak anchored to the surveyed right-bank flood trace (ADR-0035; `data/processed/2016_event/` extracts committed, raw 188 MB drop gitignored; September stage sheet of the source workbook is corrupt — window closure below-toe proven at all 4 sections). Posterior fragility = masked retained matrices (default) with an exact re-evaluation verification mode (`run.conditioning_hydrographs_for_config`); sequential events compose as masks over original rows. One command: `python -m bayesian_reliability_updating results/*_historical_*.h5 --verify`. Self-test (N=4000, KP58.8+KP60.0): transient rejection 5.2%/3.3%, static 58%/74%, **marginal transient rejection 0.0 (transient failure set nested in static under the real 2016 loading)**, posterior C_e mean −4%, k_aq −3 to −4%, induced Spearman(k_aq,C_e) ≈ −0.05; peak-based (WBI+) reading would over-reject 3.2 to 4.0× — the full-transient replay is load-bearing. Report: `docs/phase2_report.md` (incl. the 2011-event flag); package README has usage. **Event set CLOSED at 2016 (ADR-0044, 2026-07-18):** the 2011/2006 drops arrived with EMPTY stage dirs; the 2011 H23.9 trace survey (committed extract `data/processed/2011_event/flood_trace_2011.csv`) shows 2011 peaks sub-toe at KP57.4/62.0 and +0.73/+1.40 m at KP58.8/60.0; the sustained-peak bound (64 d hold at the trace peak, production N, `scripts/assess_2011_2006_closure.py` → `docs/decisions/adr0044-event-closure-bound.json`) rejects ZERO rows at 7/8 strata (incl. KP58.8) and bounds the 2011 marginal beyond 2016 at 0.316% (KP60.0 matrix only); 2006 has nothing constructible. Thesis methodology 2011 paragraph rewritten accordingly; report §12.

- **Production campaign closed (2026-07-13; `docs/close_out_2026-07-12.md`).** All **8 production sweeps** exist (4 matrix + 4 bulk, N=1e5, 225 s); the registered **KP58.8 r_e-halved QA member is measured** (`scripts/qa_re_halved_member.py`, drift-guarded bit-identical baseline; max |ΔP_f| = 0.181 at 41.25 m MSL, shoulder-concentrated, zero new failures — standard r_e confirmed conservative). The **Phase 2 production posterior update ran at full N across all 8 strata with exact masked-vs-reevaluation verification**: transient rejection (matrix) KP57.4 0.07% / KP58.8 5.67% / KP60.0 3.36% / KP62.0 0.00%; bulk ≤ 0.02%; **marginal transient rejection exactly 0 at production N in every stratum** (nesting confirmed); C_e/k_aq means −4% at the informative sections; WBI+ peak-shortcut over-rejection 2.75–3.9× [**Superseded 2026-08-20: this factor is canonical-event-conditional. The 2026-08-10 canonical-shape study measures it at 1.448 (KP58.8) / 1.568 (KP60.0) under the approved alternate member `HPB_m067_1978`, so the quotable figure is a measured two-event bracket **1.45 to 3.90** with the conditioning event named in the same sentence. Artifact: `docs/decisions/canonical-shape-sensitivity.json` -> `peak_shortcut`.**]. Caveat carried explicitly: the engine evaluates the *unremediated* foundation at the drained sections (`remediation_state` is a label, not physics), so the informative updates land at KP58.8/60.0 while KP62.0/57.4 are near-vacuous — the inverse of the thesis's tiering narrative. Report addendum: `docs/phase2_report.md` §11.

- **Length effect wired (ADR-0037, 2026-07-13).** λ_ac = 250 m primary (Kanning 2012 Table 4-7/§4.7.3 blanket-thickness 200–300 m midpoint; Schweckendiek 2014 Table 7.1 adopts 200 m; both PDFs local), conservative bracket 100/40 m ⇒ n_eff = max(1, 200/λ_ac) = 1/2/5. New `config.length_effect` (Optional; generated configs carry it `enabled: false`; **None is dropped from `to_metadata()` so pre-ADR-0037 config hashes are preserved — load-bearing for the Phase 2 replay's hash check**); when enabled, run.py records segment curves in `metadata['length_effect']` only — persisted cross-section curves never change. Post-hoc tables: `scripts/segment_fragility.py` → `results/segment_fragility_adr0037.json`. Finding: at the primary value the 200 m segment correction is the identity (n_eff = 1) — state as a finding, not an omission.

- **Worst-case timestep stress verification (ADR-0039, 2026-07-13).** The literal spec §11 single-realization test is executed (`scripts/timestep_convergence_stress.py`, ~4 min, deterministic): flashiest real HPB member (HPB_m049_2001 — 25% of amplitude in one native step; the production canonical shape ranks #2281/3000) × p99 k_aq / p99 C_e / p01 D_bl (+ p01-L variant) at KP58.8 and KP57.4, ladder 3600→14.0625 s, 0.05 m refined threshold grid, all through the frozen `evaluate_realization`. Native 3600 s re-refuted at row level (breach threshold 0.80 m of stage too low at both sections — stalled ~3 m pipes stepped over the H_eq barrier to full breach; 12 trans-not-static levels, 0 from 900 s; converged thresholds match z_toe + H_c + 0.3·D_bl within one 0.05 m step). The **failure indicator is Δt-stationary at 225 s** (threshold fixed from 450 s, zero stall/breach flips down to 14 s) — production Δt unchanged, no re-sweep; the **literal ≤1% l_e criterion needs Δt ≤ 112.5 s** at sub-breach staircase levels (KP57.4: 16.8% l_e residual at 225 s) — a rider binding l_e-*magnitude* consumers (trajectory diagnostics, future l_e-based QoIs), not flags. Evidence `docs/decisions/adr0039-timestep-stress.json`, figure `docs/figures/adr0039-timestep-stress.png`.

- **Stage 6.6 static–transient gap decomposition executed (ADR-0040/0041, 2026-07-17).** New `gap_decomposition.py` + `scripts/stage6_6_gap_decomposition.py` + `tests/test_gap_decomposition.py`: ten comparators on ONE shared sample (θ + stochastic L) at KP62.0/KP57.4, N=1e5 — statics C0 (≡ production)/C0b/C1 (crack-reduced)/C2, the **exact analytic sustained-peak limit** C3a/C3b (failure ⇔ heave gate ∧ H_erosion > H_c,trans; ODE-verified to zero disagreements at 64 d holds, zero Euler jumps), transients C4a–C4d (ADR-0017 α hook × the new ADR-0041 opt-in `equilibrium_end_factor` override on M7/M8-batch — default None ≡ 0.9 bit-identical, numba-refused, not a config field). Two telescoping ladders (engine −1/3 endpoint = the production gap; physics −1/2 endpoint), paired-bootstrap component CIs, static-pair Shapley. Gates: **C0/C4b bit-identical to the persisted production sweeps** (38+23 common levels; hash gate relaxed for the physics-inert post-sweep ADR-0037 `length_effect` YAML block); **all Euler-flip counts exactly 0 at N = 1e5**. Headlines (`docs/stage6_6_report.md`, `results/stage6_6/`): conventional-practice bias ~21× (KP62.0) / ≥32× (KP57.4) per event at HWL [**both superseded — ADR-0040 hwl-bias-resolution: 26.9 [21.6, 35.3] resolved at N=1e6 and B ≥ 148**]; temporal-dominated at the shoulder (58–**81**% of the production gap; pure temporal C3/C4 ≈ 2–8), head-convention-dominated (= 0.3·D_bl exactly) in the design-level tail (**75**–97%); initiation gate immaterial to the production gap (binds only at KP57.4 under α=−1/2); **ADR-0009 H_eq-conservatism closed at the indicator level: +1–39% of transient P_f over the levels the report tabulates** [**Normalisation stated 2026-08-20: that range is against the transient probability *as computed* (comparator C4b; measured 0.95 to 38.71%). Against the end-factor-1.0 baseline C4c the same rows read **1.0 to 63.2%** (0.96 to 63.16). Always name the denominator. Artifact: `results/stage6_6/stage6_6_kp*_analysis.json` -> `components.auxiliary.heq_conservatism_engine`; `docs/stage6_6_report.md` §10.1.**] (the ≈1.95× rate factor compresses); dimensional component −0.5 under matrix d70 but **sign-flips to +0.61 under bulk at the KP62.0 grid top, +0.37 at KP57.4's attainable max** [**Caveat added 2026-08-20 from `stage6_6_report.md` §10.2: the two entries are *not* comparable as stated. KP62.0's +0.6133 sits at 56.5 m MSL, inside the ADR-0024 hypothetical extension and six metres above that section's maximum attainable stage of 50.5 m; within the attainable range the bulk dimensional component is resolved but tiny, +0.0069 at 50.50 m. KP57.4's +0.3738 is at its maximum attainable stage. The bulk companion also runs at N = 10,000, not the 100,000 of the matrix ladder.**] (the F_s scale group crosses unity); Shapley interaction up to +0.14 (KP57.4) — quoted component magnitudes are order-conditional. **Component figures corrected 2026-08-06** from the live post-ADR-0047 analysis files (`stage6_6_report.md` §10): the previously indexed 85%, +10–25% and +0.40 were all pre-adoption KP62.0 values, and report §4.2 and §4.4 carried no supersession marker until then. The head-datum gate vs Pol SIE 2024 Eqs. (6)/(8) re-verified against the paper verbatim: PASSED (crack term subtracted once, load side, H_erosion and H_eq share the h_e = z_toe datum).

- **Phase 3 skeleton built (ADR-0038, 2026-07-13): `system_integration` package** (third top-level package, BEP-physics-free, consumes persisted artifacts + verbatim M3): `segments.py` (200 m registry from the committed rating grid, `exact` BEP-source policy default; Uemura section-table seam), `bep_input.py` (posterior-default BEP curves; ADR-0024 evaluation policy — fits where deliverable, probit-interpolated raw points otherwise, never extrapolated above the grid), `surface_curves.py` (**the Uemura curve arrival contract**: long CSV `river,bank,kp,mechanism,scenario,stage_m_msl,p_f` in T.P. m MSL, validating loader + synthetic stub; the CLI refuses stubs without `--allow-stub`), `hazard.py` (empirical annual-max stage-frequency through verbatim M3, per-event RQ4 characteristics, CSV cache), `composition.py` (series system + dominance shares), `annualize.py` (annual P_f + stratified RQ4 metrics). One command: `python -m system_integration`. Tests: `tests/test_system_integration.py`.

- **Phase 3 FULL RQ3+RQ4 campaign executed (ADR-0042/0043, 2026-07-17/18; branch `feature/phase3-uemura-integration`; `docs/phase3_report.md`).** The 2026-07-17 Uemura WP2 drop (`data/digitized/uemura_fragility_curves/`, gitignored) censused GO: it carries his models + exact inputs, no curve tables. **ADR-0042**: surface curves by faithful re-execution of his overflow (Dean/2024-paper) and scour (USACE/`ErosionModel_231019.py`) models in the quarantined `system_integration/uemura_models.py` (equivalence to his own code test-pinned), conditioned on the production canonical shape (`HPB_m064_1987`) per node with common random numbers (curves exactly monotone), N_MC=1e4; his `df_river.csv` rating coefficients are **identical** to the committed M3 rating files (datum already T.P. m MSL); committed contract CSVs under `data/processed/uemura_surface_curves/` (primary [corrected USACE scour] + sine-30h + as-received-script-k companions). **Two flagged findings**: his scour k unit conversion is ≈105.6× the dimensionally correct value (a dimensionally-indefensible slip whose converted value is moreover dead code in his own MC loop) — **amended 2026-07-21 (ADR-0042 dec. 9): the primary now uses the dimensionally-correct USACE conversion `0.3048/47.8803`, under which fluvial scour is negligible at every one of the 114 nodes (0/1e4); the as-received script factor is the labeled `scour_script_k` companion; resolved on dimensional grounds (Dean 2010/USACE basis), no Uemura confirmation pursued** — and the USACE f_c log-law diverges at 1.6 mm depth (regularized by a tested 0.05 m onset floor — the only behavioural deviation). **ADR-0043**: the 9-section table (Tokachi 1–5 = KP62.4/61.4/59.6/58.0/56.4, Satsunai 1–4 = KP7.0/6.4/5.2/4.2) reconstructed from his own SECTIONS.shp, 66/114 segments = his own notebook count; within-section rule = Eq. 14 max **conditional on discharge** (`composition.max_within_section_rated` — a naive absolute-stage max mixed local datums and overstated KP56.4 ~200×); `load_section_table(allow_gaps=True)`; `hazard.load_reach_hazard` streams each workbook once (114 nodes × 2 scenarios in ~4 min, cached under `results/system_integration/hazard_cache/`); `build_registry` now filters to the 0.2 km grid (rating files carry off-grid gauge nodes). **RQ3** (post the 2026-07-22 D7 rating-error correction below; **the KP62.0 figures were superseded 2026-07-29 by the ADR-0047 L adoption — `docs/phase3_report.md` §11 and the ADR-0047 bullet below govern**): BEP dominates all 4 quantified sections historically (**81–100%** of summed annual contributions; KP62.0 **0.812**, superseding 0.637) and under +4K leads at 3 of 4 (91–100%) with **KP62.0 level at 0.500/0.500 — overflow no longer leads anywhere** (the superseded reading was "overflow leads KP62.0 only under +4K, 66%"; its "clamped lower bound" qualifier is **withdrawn**, not merely superseded — `bep_clamped_above_grid` is False in all 20 KP62.0 rows both before and after the adoption, and the flag fires only on 16 KP57.4/58.8 **bulk** rows, `phase3_report.md` §11.3); **fluvial scour is zero everywhere under the corrected USACE conversion** (the as-received `scour_script_k` companion would add ≤8% at the BEP sections and dominate ~70 surface-only segments historically — the reach-wide *surface* dominance, not the BEP comparison, is conversion-conditional); the basin's worst segment in both climates is BEP-driven KP58.8, and its section (Tokachi 4 = KP58.0) governs the basin. [**Conditionality added 2026-08-20: these dominance shares are correct as the production deliverable at the adopted prior means, but the mechanism *ordering* is not invariant. The aquifer-conductivity bracket contests it at 3 of 4 sections historically and 4 of 4 under +4K under matrix d70, and no cell is robust under both d70 readings; the canonical-event bracket additionally moves KP62.0 +4K from 0.500 to 0.380, where overflow leads. Never quote a dominance share without its bracket. Governing record: `docs/phase3_report.md` §12.**] **RQ4**: median annual system P_f over 114 segments 0 → 3.7e-4 historical → +4K (>½ the segments carry no historical failure — the corrected rating error leaves historical peaks rarely reaching the crest; mean 1.0e-4 → 1.9e-3 = ~18×); segments >1e-3/yr 2 → 45; BEP-section system ratios **5.5–12.7×** (KP57.4 12.7, KP58.8 5.5, KP60.0 7.9, **KP62.0 12.7 post-ADR-0047** — the 19.5 quoted here before, and with it the 5.24e-4 historical annual system P_f at KP62.0, are superseded by `phase3_report.md` §11.2: 5.24e-4 → **1.01e-3** historical, ratio 19.5 → **12.7**); λ_ac=40 bracket ×1.6–3.4; bulk d70 cuts BEP-driven numbers ~5–15× (more where the historical number drops to the floor); posterior−prior −12% at KP58.8; risk concentrates ~100–400× in >24 h-above-toe years whose frequency +4K roughly triples (the duration channel carries the climate signal). **Validation**: event-based re-execution over the full ensembles at the 9 section nodes — overflow agrees with the curves within tens of %; **fluvial scour is zero both curve- and event-based under the corrected conversion**; event-based +4K overflow now within ~20% of WP2 Table 4 overtopping at KP56.4/58.0 (he computed those with these same workbook rating-error values); their erosion-dominance headline does NOT reproduce (the more starkly now — corrected scour is zero). Campaign outputs `results/system_integration/phase3/` (gitignored, regenerable: `scripts/phase3_campaign.py` ~10 s cached), figures `docs/figures/phase3_*.png`, thesis fragment `_thesis_system_integration.tex`. Blockers D1/D2 CLOSED; **D7 CLOSED 2026-07-22** (`Uncertainty_HQrelation.xlsx` arrived — gitignored, machine-local under `data/raw/`; it is the direct implementation of paper Eqs. (9)/(10), and both gauges' rating error is now measured: Tokachi←Obihiro N(−0.160, 0.294) m, Satsunai←Nantai N(−0.051, 0.283) m — the interim 0.6/0.38 traced to his toy demo notebook `frajilty curve ver2.ipynb`, NOT Eq. 10; sign pinned three ways so `Ave` is used with native sign; ADR-0042 dec. 6 amended in place; the correction **flipped KP62.0 to BEP-dominant historically**, collapsed the reach-wide historical overflow (95→31 dominant segments), and tightened +4K overflow vs his WP2 Table 4 — RQ3/RQ4 above and report §5–§9 are post-correction; `scripts/adapt_uemura_inputs.py` `WL_ERR_BY_RIVER`); residual D8 (WP2 hydrograph provenance).

- **Phase 3 hazard-coverage diagnostics (2026-07-18; HKV-audit item 2).** `annualize()` records per curve (each mechanism + `__system__`) the fraction of ensemble peaks above/below the stage grid plus the clamp end-values (`AnnualizedResult.coverage`, additive field), and **warns** when peaks exceed a grid top with P_f < 0.99 (`lower_bound_clamp` — the annualized number is a genuine lower bound; **the "KP62.0 BEP situation" attribution once given here is withdrawn**: the flag is False in every KP62.0 campaign row and fires only on 16 KP57.4/58.8 *bulk* rows, `phase3_report.md` §11.3) or fall below a grid bottom with P_f > 0.01 (`below_grid_unresolved`). Mirrors HKV's four truncation guards; numbers untouched; CLI payload carries `annualized.coverage`, campaign rows carry `system_lower_bound_clamp`/`system_frac_peaks_above_grid` on future regenerations.

- **z_toe epistemic datum sensitivity (ADR-0046, 2026-07-18; HKV-audit item 3).** The ADR-0021 surveyed toe ±0.3 m is a **systematic per-section epistemic scenario, never a stochastic column** (HKV's iid `h_exit` treatment examined and rejected). Baseline z_toe stays surveyed-deterministic everywhere. Phase 2 gained the replay-only scenario knob `z_toe_delta_m` (`load_phase1_run` / `Phase2Settings` / CLI `--ztoe-delta`; default 0.0 bit-identical; outputs name-suffixed `_ztoe_plus0.30m` + stamped `metadata['phase2']['z_toe_scenario']` so scenarios never masquerade as baseline; posterior-fit datum deliberately stays the baseline toe there). The fully consistent scenario = Phase 2 on the shifted Phase 1 companions (their configs carry the datum). Companion: `scripts/ztoe_sensitivity_study.py` → `docs/decisions/adr0046-ztoe-companion.json` + report §13 (results under gitignored `results/sensitivity/adr0046_ztoe/`).

- **Opt-in Sellmeijer model factor m_p (ADR-0045, 2026-07-18; HKV-audit item 1).** m_p ~ Lognormal(mean 1.0, CoV 0.12) (Pol SIE 2024 Table 2 / HKV WBI practice) is available via the optional `config.sellmeijer_model_factor` block — **default OFF, bit-identical baseline, None dropped from `to_metadata()` so pre-ADR-0045 config hashes are preserved** (the ADR-0037 pattern; Phase 2 hash gate keeps passing). Not an 8th θ column: a third independent 1-D LHS draw (`sampling.sample_model_factor`, own SeedSequence salt) that never shifts the θ or L draws. When enabled, M8 multiplies the **single-source H_c in BOTH its uses** (static comparator AND transient H_eq anchor — one model-form belief per realization; one-branch-only application is rejected, ADR-0045 §2); regenerable via `run.model_factor_samples_for_config`, threaded through the Phase 2 replay exactly like L. Companion deliverable (`scripts/mp_model_factor_companion.py` → `docs/decisions/adr0045-mp-companion.json`, companion results under gitignored `results/sensitivity/adr0045_mp/`): static shoulder ×2.2–2.4 (deep tail up to ×5–6), transient shoulder ×1.5–2.5, ±2% above the transition, at both informative matrix sections. ADR-0008 consequence 3 amended (m_u stays uncarried); ADR-0026/spec §7 wording reconciled — the "~12% Sellmeijer model factor" the C_e justification invokes now names where the factor actually lives.

- **Independent borehole cross-validation + prior-mean epistemic scenarios (ADR-0048, 2026-07-28).** Six Kunijiban (PWRI) borehole logs from **two campaigns independent of OYO 1999** (2013/14 十勝川上流地質調査業務, 大地コンサルタント; 2005/06 堤防点検業務, 北開水工コンサルタント) were transcribed into gitignored `data/raw/borehole_and_soil_survey/` (`TRANSCRIPTION_layers.csv`, `TRANSCRIPTION_tests.csv`, `SOURCE_METADATA.md`); **both id blocks were enumerated in full — there is NO unretrieved Kunijiban borehole in the KP 53.8–66.2 study reach**, and the `室内試験` column is blank on all six, so the lab d70 data provenance §3.3 wants is in the parent reports, not the database. **Positions are load-bearing and author-confirmed:** H25TK-B4 (KP58.8) and B5 (KP61.0) are **floodplain, ~300 m riverward** (so their 2.80 m silt / 0 m cover are *foreland* readings — they do NOT bear on `D_bl` or `z_toe`; an earlier reading of B4's collar as a z_toe check is **retracted**), KP1.8R-1 crest, KP1.8R-2 **landside toe**, KP2.1L-2 riverside. **Corroborations (nothing changed):** D_aq at KP58.8 measured 7.80 m vs tabulated 8 (−2.5%, the only independent D_aq check); aquifer base = **Nagareyama Fm. tuffaceous sandstone** at 21.05/28.25/30.60 m T.P. rising +3.13/+1.07 m/km (M4's impermeable base is now a mapped unit); `relative_density_insitu: 0.725` corroborated by SPT-derived **D_r ≈ 78%** (Meyerhof, 12 aquifer blows, 3 refusals censored ⇒ lower bound) and shown mildly conservative; three phreatic readings agree to 0.02 m (31.71–31.73 m T.P.). **ADR-0048 (the one decision):** new optional `Config.prior_mean_scenario` (`PriorMeanScenario`: enabled/label/factors) applies **multiplicative factors to named θ prior means only** — default None, `enabled=False` bit-identical, **None dropped from `to_metadata()`** so pre-ADR-0048 hashes and the Phase 2 replay gate survive; `generate_configs.py` deliberately does **not** emit the block (avoids the ADR-0037 cross-generation hash trap). `Config.effective_marginal_specs()` is the single sampled-marginals source, called by both `run._sample_prior` and `replay.py` so a scenario run replays as itself. Stamped `metadata['prior_mean_scenario']` (label + factors + baseline/effective means); companion outputs name-segregated under `results/sensitivity/adr0048_prior_means/`. Tests `tests/test_prior_mean_scenario.py` (12). **Motivating finding:** the field-permeability population is now **6 members / 2 contractors / 2 decades** (4 OYO 1999 recorded-but-set-aside in provenance §3.6, + landside-toe 5.15e-4 and riverside 8.61e-5 m/s; units cm/s, author-confirmed), geometric mean 5.94e-5 m/s = **17–51× below** the Form-5 analysis constants the priors are anchored to, and **5.0–7.3σ below the prior median** under CoV 0.50 — an epistemic *mean* offset the CoV cannot carry. Neither population is truth (single-borehole JGS-1314 tests are biased low for bulk horizontal k; Form-5 constants are deliberately high), so they **bracket** it: CSV unchanged, bracket carried as the opt-in scenario. **Measured (companion `scripts/prior_mean_scenario_companion.py` → `docs/decisions/adr0048-prior-mean-companion.json`, KP58.8+KP60.0 matrix, N=1e5): k_aq is the LARGEST single epistemic knob quantified in the project**, by a wide margin — bigger than m_p (ADR-0045) or z_toe (ADR-0046). Monotone one-directional mechanism: higher k_aq ⇒ longer Mazure leakage length ⇒ higher r_e ⇒ easier gate, *and* lower H_c. A **fourth** scenario `k_aq_regional_upper` (1.0e-2 m/s, the Chiyoda regional band's upper end) was added because thesis Ch7 had already recommended exactly that bounding run as follow-on work — **it is now executed and that recommendation is closed.** Transient ratios (lowest reachable / shoulder / grid top): KP58.8 field-geomean 0 / 0 / ×0.024, field-toe 0 / ×0.088 / ×0.743, **regional-upper ×198 / ×1.99 / ×1.01**; KP60.0 field-geomean 0 / ×1.9e-4 / ×0.082, field-toe 0 / ×0.395 / ×0.924, **regional-upper ×2428 / ×1.89 / ×1.016**. **Production sits INSIDE the bracket (~mid-range on a log scale), NOT at its conservative end** — reading only the field-test scenarios would license that comfortable but wrong conclusion. Three carried properties: the spread is **strongly stage-dependent** (orders of magnitude at low stage, →1 in the design tail as P_f saturates), it **dwarfs** the §11 MC CoV and ADR-0024 CP bands (so absolute P_f must never be quoted without it), and — **consequence 3 SUPERSEDED 2026-07-30 (ADR-0048 Amendment)** — it was *claimed* to largely cancel in the Stage 6.6 static-vs-transient ratio, licensing "comparative claims robust, absolute not"; that was **REFUTED by measurement at all four matrix sections** (`epistemic-bracket-synthesis.md` §4(c)): k_aq does not cancel, it **amplifies** the ratio (max resolved departure ×82/×66/×163/×46; 1.1–1.8 decades of ρ per decade of k_aq). Surviving rule: **a bracket cancels only if it is pure common-mode** — `m_p` alone qualifies, by ADR-0045 §2 construction. **`gamma_bl_sub` lower bound (6.0 vs 6.90) is nearly inert**: static **exactly** invariant (ratio 1.000 everywhere — an independent confirmation of the ADR-0028 static/gate separation), transient +29% (KP58.8) / +50% (KP60.0) only at the lowest reachable level, decaying to 1.000 by the shoulder; and the three in-situ densities (5.98/6.28/8.44 kN/m³) actually **bracket** the 6.90 prior rather than sitting below it. **ADR-0025 amended:** B5's zero-cover foreland at KP61.0 shows the foreland blanket is **discontinuous** in this reach, downgrading "evidence-disfavored" to "not established at KP 62.0" — but the open-entry sensitivity is already executed at max |ΔP_f| = 2.3e-4, so the baseline stands; and `D_fore`/`k_fore` (landside proxy, ADR-0005) is shown **conservative at KP58.8** (2.80 m measured vs 0.85 m proxy). No production sweep re-run; no persisted result altered.

- **Seepage length L model adopted UNCHANGED (seepage-length L study, 2026-07-19; `docs/decisions/seepage-length-L-study.md`, Accepted).** The ADR-0033 GSA put L on top, so it was scrutinised on three axes and kept as-is (no default touched, no new binding ADR, no new config field — the CoV band is already reachable via `seepage_length_cov`; the study driver `scripts/seepage_length_study.py` is validated bit-identical to the persisted sweeps, max |ΔP_f| = 0.0). **(1) Marginal:** per-section Lognormal(mean = geometry.L, CoV 0.20; 0.15 at KP60.0) traces to thesis `tab:seepage_length_prior` + the L-determination memo (engineering judgement, not a fit; base-width scatter alone → CoV ≈ 0.08–0.16, so 0.20 is a padded, mildly-conservative lumped allowance; no external CoV(L) exists — Dutch practice takes L deterministic). The **transient shoulder P_f is ~3–4× sensitive to CoV(L) over 0.10–0.40** (design level robust ≤ 1.4×; deterministic-L under-predicts the shoulder 3–5×); shape is second-order (~1.1–1.3×); the physically-indicated **one-sided upside cuts P_f ×0.33–0.57**, so the symmetric prior is conservative — the thesis must report the shoulder CoV(L) band as the dominant epistemic knob there, with the memo's one-sided-upward case as its complement. **(2) Spatial correlation** = the length effect at system scale: inter-segment independence over-counts by λ_ac/spacing (= 1.25 at ADR-0037's 250 m), but the production `exact` deliverable (four OYO sections 1.2–2.0 km apart ≫ λ_ac) is insensitive (reach-union independent/comonotone bounds within 1.4–1.7×) and internally consistent (Uemura Eq. 14 = full-dependence max within a section). The tension is latent; a densely-populated reach (`nearest` policy / future 土層縦断図) must be composed through the reach-scale length effect — new pure, opt-in, **default-independent** helpers `system_integration.composition.length_effect_effective_count` / `reach_union` (unwired, no Config field, hash gate untouched; `tests/test_system_integration.py`). **(3) Phase 2 ceiling:** the 2016 survival barely moves L (production N=1e5 posteriors: mean +0.5–1.4%, CoV −1.7–3.6%) while k_aq/C_e shift ≈ −4%, so the **ST_L ≈ 0.49–0.78 total-effect share is irreducible by the θ-only Accept–Reject filter** — the Bayesian claim is scoped to tightening θ, not the fragility-dominant geometric L. Locked into `docs/architecture.md` §7/§12/§13. Figures `docs/figures/seepage_length_*.png`; records under gitignored `results/sensitivity/seepage_length/`.

- **Foreshore width B_f verified, defined, and measured inert (ADR-0025 amendment, 2026-07-28; companion `docs/decisions/adr0025-foreshore-width-and-sensitivity.md`, evidence `adr0025-foreshore-sensitivity.json`, driver `scripts/foreshore_width_study.py`).** Triggered by a satellite-imagery check that appeared to contradict the CSV at KP62.0 (imagery ~200–300 m vs recorded 44 m). **No input value changed.** (1) **Definition:** the column is the OYO 様式-3 `高水敷幅` = **high-water-bed width** (the terrace one step above the 低水路, dry at normal flow), *not* the levee-to-waterline distance — on this braided セグメント-1 reach the 低水路 is hundreds of metres of dry gravel bar, so imagery over-reads 5–10×. At KP62.0: 高水敷高 45.00 m vs base flow 41.60 m vs mean bed 38.4 m (様式-5/-3). It is also the *correct* quantity: USACE (2000) App. B ¶h defines L₁ to the effective seepage entry (an open face fully penetrating the aquifer, blanket between it and the levee), and the channel bed cuts ~6 m below the A_c base, so the 低水路 bank IS the entry face; braid-plain gravel adds zero entry resistance. B_f is stage-independent (at flood the head sits *on top of* the blanket over its full width — that is the theory's own configuration); `B_f = 0` means *no blanket*, not *water at the levee*. (2) **Verified:** all four 様式-3 annotations re-read verbatim (200/325/600/44) = CSV exactly; independently corroborated by the MLIT **2008** 堤防現況縦断図 row 4)② `高水敷幅 Bfp(m)` (digitized ±5 m, clips at ~150 m): KP57.4/58.8/60.0 all "≥148", KP62.0 ≈34, reach KP60.3–63.9 17–35 m — so **no widening 1998→2008** (the channel-migration/樹林化 hypothesis is refuted). (3) **ADR-0025 open-entry sensitivity EXECUTED** (it had never been run): baseline arms asserted bit-identical to the persisted production sweeps, then B_f → 0 gives max |ΔP_f,trans| = 0.00111 (KP57.4) / 0.00170 (KP58.8) / 0.00440 (KP60.0) / **0.00023 (KP62.0)**, with max |ΔP_f,static| **exactly 0** everywhere (asserted, the ADR-0028 consequence). The tanh is saturated (credits 0.835 / 0.969–1.000): at KP62.0 any B_f ≳ 100 m is numerically identical, so 44 vs 250 m is worth 5e-5 and the adopted narrow value is the *conservative* end. r_e is inert at KP62.0 because the heave threshold D_bl·γ'_bl/γ_w ≈ 0.32 m is already exceeded by r_e·(h−z_toe) ≈ 0.45 m at the lowest failing level — **section-specific**, cf. the KP58.8 r_e-halved QA member at ΔP_f = 0.181. (4) **Source decision: OYO 1998 RETAINED over MLIT 2008** — MLIT clips and so cannot supply 3 of 4 values, is a ±5 m raster digitization vs stated numerals, breaks vintage consistency with every other column (and with the ADR-0005 `D_fore`/`k_fore` proxies), and "post-remediation" does not apply (remediation is landside, extending `L`, not 高水敷幅); above all `geometry.foreshore_width` is inside `config_hash()`, so editing the CSV would invalidate all 8 Phase 1 sweeps + the Phase 2 posterior + Phase 3 via the replay hash gate, to chase ≤0.0044. (5) **Narrative correction (the real finding):** the Study Area "foreshore-width control on risk" argument does **not** survive — KP62.0 has the **lowest** r_e of the four (0.330 vs 0.417–0.438), i.e. transmits the *least* head, not "nearly the full river head"; the measured B_f effect is *largest* at KP60.0 (600 m) and *smallest* at KP62.0; KP57.4's 1998 gradient pass is **not** secured by its 200 m foreshore (deleting it moves P_f by 1e-3); and the 1998 i_v = 0.97 vs 0.04 contrast is an OYO **fully-blanketed** FE output (ADR-0025 Evidence 4), not evidence of foreshore control. What governs KP62.0 is the **0.45 m blanket**, which cuts both ways (minimises uplift resistance *and* shortens λ_in). Corrected in both `_thesis_studyarea.tex` and msc-thesis Ch. 3 (8 passages each, incl. the stale "exit elevation = foreshore crest" sentence superseded by ADR-0021, and the subsection retitled "…Section-Scale Controls on Risk" with its `\label` deliberately unchanged). Provenance §3.9 records the definition, verification, and source decision so the question cannot recur.

- **R10 foreshore-exhaustion screening indicator built — Tier 1 only (2026-07-28; companion `docs/decisions/r10-foreshore-exhaustion-screening.md`, evidence `…-screening.json`, figure `docs/figures/r10_foreshore_exhaustion.png`, indicator `system_integration/foreshore_exhaustion.py`, driver `scripts/foreshore_exhaustion_study.py`, tests `tests/test_foreshore_exhaustion.py`).** Closes the R10 scoping note (`docs/scoping_bank_retreat_mechanism.md`) at **Tier 1; Tiers 2 and 3 declined by the author** (Tier 2 would need ADR-0047 first). **No ADR consumed, no `Config` field, no production default touched, nothing persisted to `results/` as a deliverable, and no mechanism added to `system_integration.composition`** — the module is unwired by construction, pinned structurally by `test_screening_module_is_not_wired_into_the_phase_3_composition`, and the full Phase 3 campaign re-ran **byte-identical** in 11/12 artifacts (the 12th, `campaign_summary.json`, differs only in its `generated`/`runtime_s` stamps; substantive content identical). **Why:** under ADR-0042 dec. 9 the fluvial-scour branch is exactly 0 at all 114 segments while the documented failure record here is channel migration consuming the 高水敷 (2011 Otofuke KP18.2; 2016 Otofuke KP21.2 + Satsunai KP40.5) — the indicator quantifies *which segments are exposed*, converting a Discussion admission into a measured statement. **Method:** pure, physics-free, forcing injected (`convergence.py`/`sensitivity.py` pattern): `T_mob` = hours the stage stands above the high-water-bed surface (`floodplain_m_msl` of the committed `data/processed/uemura_segments/segment_inputs.csv` = Uemura df_river `FloodplaneHeight`), `exposure_ratio = v_lat·T_mob / B_f`, headline reported as the **critical rate `v* = B_f / T_mob`**. Forcing = the ADR-0035 observed 2016 record, the ADR-0020/0030 conditioning records, and the d4PDF ensemble via Phase 3 `hazard.load_reach_hazard` (study-local cache, never the production one). **Coverage is 4/114** — only the confined OYO sections have a measured `B_f`; **no width interpolated** for the other 110. **Results:** `v*` at design HWL = 0.72 (KP62.0) / 3.00 (KP57.4) / 3.85 (KP58.8) / 9.77 (KP60.0) m/h; under 2016 = 2.59 / 4.35 / 4.51 / 27.3. KP62.0-vs-KP60.0 separation **10.5× (2016), 13.6× (design)** against a 13.6× width contrast (driver asserts a 5× floor, and asserts the four `foreshore_width_m` still read 200/325/600/44). KP62.0 is the **only** section flagged at the central 1 m/h rate in either climate (ensemble 1.2%→3.6% hist→+4K); bed-mobilising-year frequency roughly doubles under +4K at 3/4 sections — the climate signal enters through **duration**, matching the Phase 3 finding, since ADR-0023 makes the stage→ratio map climate-invariant. **The honest headline:** every `v*` lies inside the assumed 0.1–10 m/h bracket, so **the verdict flips within the bracket** — this establishes an exposure *ordering* and scale, not a screening decision. The 2011 ~5 m/h datum is **one narrative observation and is longitudinal (levee length), not lateral**; it enters as the labelled `narrative_2011` member **unconverted**. Threshold ±1 m (the measured KP62.0 OYO-45.00 vs MLIT-43.82 spread) moves `v*` by 2–3×, co-equal with the rate uncertainty. Constant-rate is the **bounding** law (measured mean/peak excess depth 0.27–0.46 ⇒ a depth-linear law gives 2–4× smaller ratios). **Cannot be validated at Tier 1**: both 2016 bank-erosion failures lie outside the modelled reaches. Thesis: msc-thesis Ch. 7 §"The Surface-Mechanism Model Set as a Conditioning Assumption" (+2 paragraphs) and Ch. 8 §"Recommendations for Future Research" (new subsection: Tier 1 exists, Tier 2 is the remaining step with the falsification test as its point, Tier 3 declined).

- **Seepage length L re-measured from 2025 lidar; ADOPTED at KP62.0 only, held elsewhere (ADR-0047, measured 2026-07-28, adoption executed 2026-07-29; companion `docs/decisions/adr0047-dem-seepage-length.md` + `.json`, driver `scripts/dem_cross_section_study.py`, tests `tests/test_dem_cross_section.py`, figure `docs/figures/adr0047_dem_seepage_length.png`, profiles under gitignored `data/raw/geometry/dem_cross_sections/`).** The ADR-0025 close-out handed off "B_f is inert, **L** is the input worth measuring"; ADR-0033 ranks L top total-effect (ST_L 0.49–0.78) and the seepage-length L study proved the 2016 survival cannot tighten it — so only better data can. **No input value changed** (`geometry.L` is inside `config_hash()`; the CSV and configs are read-only, pinned by `test_the_study_never_writes_the_committed_inputs_csv_or_configs`). **Method:** whole path in committed code, no QGIS (which cannot open JPGIS(GML) natively) — 100 GSI **DEM5A** tiles (mesh 644331, `devDate` **2025-06-20**) → mosaic; levee alignment chained part-aware from `SECTIONS.shp` with EPSG:2455 inverse TM implemented in-repo (round-trips < 1 mm, **no geospatial dependency added**); profiles perpendicular to the local tangent, 1 m posts; toes by a stated slope-break rule (crest band 0.5 m; outward slope ≥ −0.10 sustained 8 m, ≥ 1.5 m below crest — **evaluated in the direction of travel on both sides**, an earlier one-sided array pushed the riverside toe 3–4 m out and moved every L by ≈4 m); **outer** landside toe (walks past a berm, capped 40 m beyond) primary, embankment-only reported beside it. Headline is the **median of 31 stations over ±300 m** (the KP anchor is only good to ≈±150 m — the design-crest ramp is too linear to cross-correlate better), under two uniform screens against independent committed data: landside-structure and raised-crest-vs-2019-design. **Datum gate PASSED** on 3 series × 551 stations: crest vs 2019 `DesignBankHeight_R` **+0.30 ± 0.55 m**, landside ground vs Uemura `ground_m_msl` −0.65 ± 0.68, riverside terrace vs `floodplain_m_msl` −0.24 ± 0.73. **Extraction validated before its L was believed:** ADR-0021 `z_toe` residuals −0.15 / −0.38 / +0.36 (KP58.8/60.0/62.0) and −0.85 at the contaminated KP57.4; obliquity 1/cos θ = **1.001–1.006** (perpendicularity real, not assumed). **Results (clean-station median vs CSV 1998):** KP58.8 **42** vs 35 (+7, 31/31 clean), KP60.0 **43** vs 34.8 (+8, 31/31), KP62.0 **40** vs 47 (**−7**, 28/31); **KP57.4 yields NO adoptable number** (6/31 clean, along-levee CoV 0.60, apparent +3.5 m smaller than the rule's own ≈−2 m bias → **no resolvable change**). **The differences track `remediation_state`**: the two `drained` sections (post-1998 berm + toe drain) lengthen, the `unreinforced` one does not — it measures *shorter* — reproducing provenance §3.1's own prediction from an independent surface. **KP57.4's contribution is a negative result:** its nominal station sits on a road interchange embankment (crest +1.6 m over design across ~200 m; a second embankment 40–100 m landward), the all-station median 67 m is **road fill**, and so §3.1's "+10 to +30 m" berm-only prediction does *not* hold for the levee proper. **Measured ΔP_f (N=1e5, matrix, every baseline arm asserted bit-identical to its persisted sweep):** max |ΔP_f,trans| = 0.132 (KP57.4, →36.5 m) / 0.749 (KP57.4, →67 m) / 0.232 (KP58.8) / 0.279 (KP60.0) / **0.201 (KP62.0, →40 m, P_f *raised*, up to 15× in the tail)**, static within 0.03 of transient throughout — i.e. **L outweighs B_f by 119×/137×/64×/874× per section** (ADR-0025's *bounding* B_f→0 gave 0.00111/0.00170/0.00440/0.00023, static exactly 0), and unlike B_f it moves **both** branches, so adoption is a re-run not an amendment. All maxima sit above design HWL; KP62.0's at 50.00 m MSL is above crest inside the ADR-0024 hypothetical extension and **must never be plotted as attainable**. **CoV(L) confirmed, not narrowed:** measured along-levee spread 0.073 (KP58.8) / 0.184 (KP60.0) / 0.102 (KP62.0) brackets the seepage-length-L-study §1.2 base-width figure 0.08–0.16 — but the padding to 0.20/0.15 covers the *unverified landside blanket boundary and exit position*, which a bare-earth surface cannot see, so the prior stands. Known rule bias ≈ **−2 m** (finite slope window on 1:3 faces; conservative, reported not corrected). By-product 高水敷幅 DEM 102/288/546/**236** m vs ADR-0025's verified 200/325/600/**44** — KP62.0's 2025 terrace is 5.4× wider, but **ADR-0025 is not reopened**: that tanh is measured saturated (B_f ≳ 100 m numerically identical; full `B_f → 0` worth 0.00023 there). **THE STATIC-vs-TRANSIENT RATIO DOES *NOT* SURVIVE THE L CHANGE** (§4.5, evidence `adr0047-dem-seepage-length-ratio.json`, driver stage `... ratio`): ratio-of-ratios ρ = (P_s/P_t)_DEM ÷ (P_s/P_t)_CSV by **paired bootstrap** over the 16 joint pattern counts (2000 reps; null pinned at ρ=1.0 exactly; baselines gated on the WHOLE failure matrices) gives at design HWL **×2.250 [1.471,4.465] (KP57.4) / ×1.642 (KP58.8) / ×2.226 (KP60.0) / ×0.475 [0.263,0.724] (KP62.0)** — **all 87 evaluated levels resolved at 95%**. **The L bracket does NOT cancel**: both branches share `H_c` but L additionally enters the transient side via `Z = L − l_e` and the rate denominator, so it is not a common-mode shift. **Stage 6.6's bias headlines (~21× KP62.0, ≥32× KP57.4 at HWL) are L-conditional and must be quoted as such whether or not the values are adopted** (and, per the 2026-07-30 synthesis, **k_aq-conditional too, more strongly** — the "unlike k_aq, which cancels" contrast once drawn here is withdrawn) — under DEM-L the KP62.0 bias roughly halves, KP57.4's roughly doubles. ADR-0024 form: all four matrix transients are ALREADY `fitted_lognormal` (KP62.0 included; no flip — it stays fitted, max raw P_f 0.9698→0.9901); the only form change is the contaminated KP57.4 road arm *losing* its bracket. **KP62.0 IS A CORRECTION, NOT A BRACKET ENDPOINT** (§4.1b): §3.1's 47 m is "toe-to-toe *incl. landside berm*" but §3.2 calls the section `unreinforced` — **all three §3.2 confirmation lines bear on BERM presence** (the toe *drain* is §3.2's stated residual), and the 2025 surface shows **no berm** (outer toe == embankment toe at all 28 clean stations; crest → 1:3 face → toe → level ground, no bench; median 40/40/41 m as the outer-toe cap is raised 40→60→80→120 m, so the 40 m is not a cap artefact) ⇒ the L memo credited a berm that its own 様式-5 source did not model, and **the production model is under-conservative at the governing section** (transient P_f ×8.7 at HWL, ×3.2 at design crest). **Adoption deferred to the author**; ADR-0047 Consequences tabulates the re-run cost (CSV + `generate_configs.py` + `tests/test_configs.py` + 8 Phase 1 sweeps + Phase 2 posterior + Phase 3 campaign + ten bit-identity-asserting companion studies + the documents of record). Free cross-check recorded: the ADR-0048 `config.py`/`run.py`/`replay.py` changes were already in the tree when these arms ran, so every baseline reproducing its persisted sweep bit-for-bit **proves ADR-0048 is baseline-neutral**. **ADOPTION EXECUTED 2026-07-29 (author decision), KP62.0 ALONE — principle: adopt where the 1998 value is WRONG, hold where it is merely OLD.** KP62.0's 47 m credited a berm that never existed (a defect, under-conservative at the governing section) ⇒ CSV `L_m` 47.0→**40.0**, one cell; KP57.4 (nothing resolvable) and KP58.8/60.0 (real +7/+8 m from the 1999–2003 works — merely old, and adopting the longer path while the engine models **no** toe drain would import only the anti-conservative half) are **HELD**, their DEM values carried as the unadopted bracket, `seepage_length_cov` unchanged everywhere, 40 m adopted **uncorrected** so it is the conservative end of the measurement. **This CONFIRMS provenance §3.2's `unreinforced` label as a fourth independent line** (all three original lines bear on *berm*; the *drain* is §3.2's residual). **Executed end-to-end with gates:** config diff **exactly 2 files / 1 field**; both KP62.0 Phase 1 sweeps re-run (and they reproduce the §4.3 in-memory arm to <1e-12 — independent validation of the whole measurement chain); **Phase 2 rejection stays 0.00% at both strata with exact masked-vs-reeval verification, so the "marginal transient rejection = 0 in every stratum" claim survives an ×8.7 fragility rise**; Stage 6.6 drift guard **bit-identical at 38 levels**, all Euler flips 0 (at N = 1e5); Phase 3 containment **20 of 2280 RQ4 rows, all KP62.0**; consumers enumerated **programmatically** (7 found, all re-run or re-pinned — incl. `foreshore_width_study` [held sections reproduce exactly; KP62.0 0.00023→0.00024, still inert], `assess_2011_2006_closure` [**all 8 strata reproduce exactly**], `segment_fragility`, `seepage_length_study`, `foreshore_exhaustion_study` [**byte-identical**], and this study itself re-pinned to drive the **withdrawn 47 m** arm); **ADR-0045/0046/0048 companions checked and NOT re-run — KP58.8+KP60.0 only**. **Moved:** transient P_f at HWL 1.5e-4→**1.3e-3**; Stage 6.6 bias nominal 21.0→44.7 at HWL **but neither is resolved (1 and 4 failing rows)** — where counts are adequate the bias **FALLS ~⅓** (15.0→10.5 at 47.0 m MSL), matching ρ; RQ3 BEP share hist 0.637→**0.812**, +4K 0.344→**0.500** (**overflow no longer leads at +4K**); RQ4 annual system P_f 5.24e-4→**1.01e-3**, climate ratio **19.5→12.7**. **Two pre-existing doc errors found and corrected:** `phase3_report.md` §5's claim that KP62.0's BEP is flagged `bep_clamped_above_grid` "in every output row" is **false in all 20 rows before and after** (the flag fires on 16 KP57.4/58.8 **bulk** rows) — withdrawn in §11.3; and stage6_6's "~21×" was never resolved. Reports gained dated addenda (`phase2_report.md` §14, `phase3_report.md` §11, `stage6_6_report.md` §8); msc-thesis **Ch. 3** (3 passages incl. the L prior table row 47→40, μ_ln 3.831→3.669) and **Ch. 7** (new Discussion subsection "Not Every Epistemic Knob Cancels in a Ratio") updated, no raw CJK. Superseded artifacts under gitignored `results/superseded_adr0047_L47/`. **556 green.**

- **Definitive production campaign executed end to end (2026-07-29; driver `scripts/production_campaign.py`, manifest `results/production_campaign_manifest.json`, document of record `docs/production_campaign_2026-07-29.md`).** One idempotent, resumable, physics-free driver now sequences the whole campaign (configs -> 8 Phase 1 sweeps -> 8 Phase 2 posteriors + both documented variants -> Stage 6.6 -> Phase 3 + event validation -> 9 bit-identity companions -> diagnostics), preserving what it supersedes to `results/superseded_<ts>/` and recording per stage the command, runtimes, output SHA-256s and every gate. **Why:** 6/8 persisted sweeps carried hashes not matching `configs/*.yaml`; verified key-by-key that the diff is EXACTLY the three physics-inert ADR-0037 `length_effect` keys (KP62.0 already matched, re-run at the ADR-0047 adoption). **G1 PASSED: all 8 strata reproduce both failure matrices, theta, the grid and both raw P_f vectors bit-for-bit, with `metadata_value_changes` and `metadata_regressions` EMPTY everywhere** - which re-confirms **ADR-0048 baseline-neutrality** under a fresh full re-execution. **The one allowlist deviation, resolved by a rule not an exception:** `aquifer_response` was ABSENT->PRESENT at the three matrix strata generated 2026-07-10 (pre-ADR-0032), so the metadata check is now **asymmetric** - an additive key is recorded and passes, a *changed value* or a *regression* still fails. The same rule then covered two more cases unaided (R10 `d4pdf_ensemble` null->populated; ADR-0044 and ADR-0033 GSA volatile-only rewrites of tracked records, auto-restored from git so documents of record do not churn on timing noise; all 6373 GSA Sobol' index lines byte-identical). **G2 PASSED** (`--verify` exact everywhere; **marginal transient rejection exactly 0 in all eight strata** at production N); **G3 PASSED** (drift guard bit-identical at 38 + 23 levels, every Euler flip 0 at N = 1e5; side effect: hashes now match directly so ADR-0040's `length_effect` relaxation is no longer exercised); **G4 PASSED** (`rq4_annual.csv` **0 changed rows**); **G5** (ADR-0032 `instantaneous` 8/8, margins 3.8-19.5x = Pi\*/Pi_**corner90** at each run's own canonical-event T_rise, NOT the published ~10x which is Pi\*/Pi_**central** at the ensemble-median T_rise; bootstrap degenerate 0/0 everywhere; all four matrix strata `fitted_lognormal` on both branches; Phase 3 coverage capture 8 files, **0 flagged curves**; `bep_clamped_above_grid` 16 rows, all KP57.4/58.8 **bulk**); **G6** all nine companions pass. **CHANGES exactly two published numbers**, both stale on the withdrawn L=47 m Phase 1 (`e9b8760b`): `phase2_report.md` sec. 11.3 KP62.0 anchor **0.01% -> 0.05%** and strict posterior **30.46% -> 39.55%**. **FOUND ALREADY STALE (the ADR-0047 adoption fixed headlines, not derived tables; all KP62.0-only):** `phase3_report.md` sec. 6.1 "**2** to 45 of 114" is **3 to 45** (KP62.0 crossed 1e-3 when its historical annual rose 5.24e-4 -> 1.006e-3); sec. 6.2's four KP62.0 brackets (lambda40 x3.1/1.6 -> **x3.3/1.9**; bulk x2.6/1.2 -> **x5.0/1.5**; `scour_script_k` +45% -> **+22%**; `overflow_sine30h` -19..27% -> **-12/-13%**); `stage6_6_report.md` sec. 1 ratio "15 at 47.0 to 1.9 at 50.5" -> **10.5 to 1.4**, its sec. 4.1 KP62.0 rows, and C3b/C4b "7.9 -> 1.9" -> **6.0 -> 1.4**; and `docs/decisions/adr0047-dem-seepage-length.json` itself, whose KP62.0 block still names 47.0 m as baseline (remedy `python scripts/dem_cross_section_study.py all --overwrite`, deferred to the figure pass). **Every KP57.4/58.8/60.0 number in all three reports reproduces exactly** - the adoption's containment claim was sound; the residue is purely documentary. [**Conditionality added 2026-08-20: the RQ3 shares and RQ4 ratios below are the production deliverable at the adopted prior means; the mechanism ordering they express is contested by the aquifer-conductivity bracket (3 of 4 sections historically, 4 of 4 at +4K) and by the canonical-event bracket (KP62.0 +4K 0.500 -> 0.380). See `docs/phase3_report.md` §12.**] **RQ3/RQ4 scoped per the author's decision 5:** the answer is the four geotechnically characterised sections (RQ4 climate ratios **12.7 / 5.5 / 7.9 / 12.7**; BEP dominant 81-100% historically, leading 3 of 4 under +4K with KP62.0 level at 0.500/0.500), and the 114-segment distribution is labelled **reach context** because **110 of 114 carry `bep_source=None`** under the production `exact` policy and are surface-only lower bounds. The programmatic consumer enumeration surfaced **three consumers the brief's list omitted** (`mp_model_factor_companion`, `ztoe_sensitivity_study`, `prior_mean_scenario_companion`) - **not broken** by the hash change, since each rebuilds its `Config` from the sidecar's own `config` block; they stay OFF per decision 3. **Figures deliberately NOT regenerated** (separate pass). **Decision 6 arrived as an unfilled placeholder (`<your D-6 answer>`), so no design-HWL resolution method was applied or invented**; everything a higher-N or importance-sampled Stage 6.6 re-run needs is preserved, and sec. 6.1 quantifies the problem: at design HWL the transient comparator rests on **4 failing rows at KP62.0 and 0 at KP57.4**. **559 green**, ruff/black clean; no config default, physics or ADR touched (execution, not decision).

- **Epistemic brackets extended to all four matrix sections; ADR-0048 property (c) REFUTED (2026-07-30; note `docs/decisions/epistemic-bracket-synthesis.md`, evidence `…-synthesis.json`, driver `scripts/epistemic_bracket_synthesis.py`, tests `tests/test_epistemic_bracket_synthesis.py`).** ADR-0047's log left **KP62.0, the governing section, with NO measurement of m_p, z_toe or the k_aq bracket**; all three companions re-run at **all four** matrix sections (not just the two new ones - the KP58.8/60.0 records predated the campaign, so the set now carries current hashes; 28 companion sweeps). Confirmed **not assumed** that all four sidecars round-trip their `config_hash` and match `configs/*.yaml`, so the drivers do not refuse; each of the four baselines re-run and asserted **bit-identical on the WHOLE failure matrices**. **The one ranking table the thesis needs now exists** (7 brackets x 5 anchors x 4 sections, as a comparable multiplicative `span`). **`k_aq` is the largest knob at every section and every anchor** (x6.65e3 at the KP62.0 midpoint vs a Clopper-Pearson span of 1.01) - ADR-0048's headline survives extension. **NEW: at KP62.0's design HWL `z_toe` is the SECOND-largest knob (x184), ahead of L (x15)** - ADR-0046 had never measured KP62.0; the anchor sits 0.11 m above HWL on 15 failing rows, so +/-0.3 m of datum moves the section across its own threshold. **ADR-0048's three carried properties: (a) stage-dependence CONFIRMED** (qualified - the collapse toward 1 needs the *arm* to saturate, which KP57.4's grid top does not allow, span 757 there); **(b) "dwarfs the statistical uncertainty" CONFIRMED FOR `k_aq` ONLY and must not be generalised** - at KP62.0 design HWL the entire m_p bracket (2.80) is NARROWER than the CP band (2.95) and the MC CoV band (3.05); **(c) "largely cancels in the static-vs-transient ratio" REFUTED at ALL FOUR sections, including the two where it was asserted** (ADR-0047 §4.5 method, 2000 reps, null pinned at rho=1.0, baselines gated on whole matrices): max resolved departure **x82/x66/x163/x46** for `field_geomean`, resolved at essentially every level - **larger than the L bracket's own non-cancellation**. **Mechanism read from code, not fitted:** `k_aq` has ONE common-mode channel (`H_c` via `_factor_Fs`) and **TWO transient-only** ones (`r_e`->uplift/heave gate under ADR-0028; the ODE rate `dl/dt = 89*C_e*(k_aq*...)^0.81`) where L had one - it could never have cancelled; **`m_p` (x1.07-1.22) is the ONLY knob that cancels, because ADR-0045 §2 applies it to the single-source `H_c` in BOTH uses by construction**. Surviving rule: **a bracket cancels only if it is pure common-mode; measure it per knob, never assume it from "shared sample, fixed parameter"** - so the Stage 6.6 bias headlines are `k_aq`-conditional as well as L-conditional, and the larger of the two conditionalities is `k_aq`. **A pre-registered prediction reported as NOT confirmed:** the raw departure ordering (x2.2 KP62.0 < x7.0 KP58.8 < x9.3 KP57.4) looked like ADR-0025 gate saturation but is **an artifact of unequal shift size** (a scenario is an absolute *target* mean, so `k_aq` moves x0.17 at KP57.4 but x0.515 at KP62.0); normalised, non-cancellation is section-independent at **1.1-1.8 decades of rho per decade of `k_aq`** - >1 everywhere, i.e. the ratio *amplifies*. **Production's position:** mid-range in *input* space (55.1% of the log-`k_aq` range at KP62.0, confirming ADR-0048) but in *P_f* space at KP62.0 design HWL only **26.5%**, i.e. **upside x1800 if the regional-upper band is right, x0.067 if the field population is** - KP62.0-specific (KP58.8 gives 72.6%, upside x3.49). Method reproduces ADR-0047 §4.5 three ways (KP60.0 rho 2.226 = published; KP62.0 max departure 2.106 = 1/0.475; KP62.0 L rise at 46.75 m x8.67 = published x8.7). Free by-product: **ADR-0028 static/gate separation reconfirmed at 98 levels** (`gamma_bl_sub` static ratio exactly 1.000, 4/4 sections). **Two shoulders, not one:** ADR-0045 quotes m_p at the rising limb (P_f ~ 2e-3), ADR-0048 quotes `k_aq` at the transition midpoint (P_f ~ 0.5, traced to its own KP58.8 x0.088 at 41.50 m) - the table names both and a test forbids the ambiguous word. **CARRIED FORWARD, deliberately not fixed:** the three ADR-0047 records are a **matched pre-adoption set** (evidence JSON, figure, AND the ratio JSON - the last **not previously flagged**), all from `e1e7789`; the campaign's copy is **not** a drop-in replacement (verified: **no `datum_check` block**), and fixing the JSON alone would desynchronise it from the figure it depicts, so both `dem_cross_section_study.py all --overwrite` and the `ratio` re-run belong to the figure pass as one atomic action. Knobs stay **OFF** per campaign decision 3; no `Config` default, physics, config, CSV or persisted production sweep touched. **576 green** (559 + 17), ruff/black clean.

- **Design-HWL static-vs-transient bias RESOLVED at KP 62.0; KP 57.4 bounded; the tilted estimator REFUTED for this estimand (2026-07-30; pre-registered note `docs/decisions/adr0040-hwl-bias-resolution.md`, evidence `…-resolution.json`, driver `scripts/hwl_bias_resolution.py`, tests `tests/test_hwl_bias_resolution.py`; closes the campaign's open decision 6).** Method chosen by the author (brute force at N=1e6 → validate tilted IS against it → apply at KP 57.4), executed under a **Part 1 pre-registration written before any number** (criteria R1 k≥30 ∧ R2 CI width ≤2.0×; falsifiers F1/F2/F3; estimator criteria V1–V4; scope rule). **KP 62.0 design HWL (46.39 m): 44.7 on 4 rows → 26.9, 95% CI [21.6, 35.3], on 63 rows — RESOLVED.** The old 44.7 was **counting noise that OVERSTATED the bias 1.66×** (statistically consistent; G-A3 passed 59/59). Nearest grid level 46.50 m: 26.2 → **21.6 [18.8, 25.2]** on 176 rows. **Both anchors resolved; A1 and A2 are different levels and must never again be quoted as one another** (test-pinned). **F2 did not fire** (intervals overlap) but the *paired* test — declared as an addition, since a sharper test can only make a falsifier easier to fire — gives **ρ=1.249 [1.039,1.556] resolved: the bias is 25% larger 11 cm lower**, so the number is never quotable without its stage. **KP 57.4 still UNRESOLVED at N=1e6 (2 failing rows; R1 needs ~N=1.5e7)**: report the Clopper–Pearson bound **B ≥ 148** (supersedes *"at least 32"*, which rested on ZERO rows) and lead with the resolved anchor **42.7 [39.4, 46.6] at 39.50 m**. **THE ADR-0029 TILTED SAMPLER FAILED ITS PRE-REGISTERED VALIDATION for this estimand** — V1✓ V3✓ but **V2✗** (46.75 m disagrees resolvably, 25% low) and **V4✗** (Kish n_eff 86.9 < 200), CE shift ν={k_aq 3.17, C_e 0.90} (the design-HWL failure region is reached through extreme k_aq, barely through C_e). Its transient-side gain is real (**4.66×** CoV, at the favourable end of ADR-0029's 3.2–4.1×) but a transient-optimised tilt **INFLATES THE STATIC BRANCH 1.50× at the anchor rising to 940× at saturation** — structurally the wrong instrument for a *ratio between* branches, not a tuning failure. **ADR-0029 is NOT contradicted**: what fails is a new application to a different estimand. Brute force used throughout; **no weighted number in any headline**. **NEW NUMERICAL FINDING: gate G-A2 FIRED at KP 57.4 at N=1e6 — 4 `c4b_not_c3b` Euler barrier-jump rows in 1e6** at 39.50/40.25/40.75 m, **none at either anchor**; expected count at production N=1e5 is 0.4, which is why every prior run saw zero. **No production result is affected** (all at N=1e5, gate passes, drift guard bit-identical); KP 62.0 is clean (0 flips/39 levels). Indicator-level counterpart of ADR-0039's KP 57.4 Δt rider. **Stage D epistemic band on the RATIO at the anchor, unweighted at N=1e6, two bit-identity gates:** m_p negative control **passes at all 3 anchors with ≥63 rows (1.010/0.925/0.901) and fails at the 2 with ≤10 rows (1.550/1.707) — monotone in row count, so counting noise, not machinery; those arms discarded**. KP 62.0 HWL: k_aq dominates (`regional_upper` ρ=0.096 resolved, B 26.9→**2.59**), z_toe second (ρ=0.515), **m_p cancels**, `gamma_bl_sub` **exactly 1.000**; `field_geomean` is **indeterminate, NOT unbounded** (BOTH branches zero — checked, nearly mis-stated). **F3 is section-specific: does NOT fire at KP 62.0 (9.0 vs threshold 10) but FIRES at KP 57.4 (unbounded — field-k_aq gives static failures with zero transient failures)**. Either way the epistemic band is **6–9× the statistical interval**, so absolute bias figures must carry it. `gamma_bl_sub` is inert at KP 62.0 but moves KP 57.4's 39.50 m by a resolved 19% — as ADR-0048 predicts near the bottom of a reachable range. **Two driver defects found and fixed:** a gate that raised *before* persisting (destroyed 2.5 h of evidence it was raised about — now persist-then-gate, with offending levels recorded), and a per-section writer that overwrote the other section (now merges). **602 green** (577 pre-existing + 25 new; the prior note's "576 = 559 + 17" undercounted its own file, which collects 18), ruff/black clean. No `Config` default, physics, config, CSV or persisted production sweep touched; the one shipped-module change is an additive default-`None` `run_comparator_ladder(theta_override=…)` seam, bit-identical by test.

- **Figure pass + number-consistency audit executed (2026-07-30; `docs/number_audit_2026-07-30.md`, `docs/thesis_number_inventory_2026-07-30.md`, new `scripts/_figstyle.py`, tests `tests/test_figure_pass.py`).** **THE COPY PROBLEM IS FIXED STRUCTURALLY, BOTH WAYS:** (1) every driver now writes its publication copy **directly** to tracked `docs/figures/` (`stage6_6_gap_decomposition._write_figure`, `plot_fragility_curves.save_both`) so no human copies anything, and (2) a new **`figures` stage in `scripts/production_campaign.py` (gate G7)** re-renders every figure with a cheap redraw path and then **fails if any figure under `docs/figures/` is older than the artifact it depicts** - it caught 9 genuinely stale GSA figures on its first run, including one 17 days old. Covers **44 of 52** tracked figures; the 8 unmapped (ce_prior x3, adr0012, adr0029, adr0032, adr0039, r10) need a full study re-run and are **listed, not silently ignored** [**superseded 2026-07-31: now 52 of 52 declared, 51 staleness-gated — see the next bullet**]. Two new **no-drift redraw seams**: `stage6_6 --figures-only` (loads persisted ladder + analysis, writes no evidence file, AST-pinned) and Phase 2 `--figures-only` (`Phase2Settings.figures_only`, default False bit-identical; refreshed the KP 62.0 diagnostics with the persisted posterior **md5-verified untouched**, which matters because the campaign manifest records its SHA-256). **26 figures regenerated** from current artifacts. **FOUR NEW FIGURES the last two steps earned:** `adr0040_hwl_bias_resolved.png` (both N drawn together, so 44.7-on-4-rows -> 26.9-on-63 is visible not asserted; A1 46.39 and A2 46.50 separated in a zoom panel), `adr0040_tilted_is_validation.png` (**the structural point**: transient gain 4.66x against static inflation 1.50x -> 940x, so a transient-optimised tilt is the wrong instrument for a *ratio between branches*, not a tuning miss), `epistemic_vs_statistical.png` (epistemic band **6 to 9x** the statistical interval; m_p the visible control at unity), `phase3_rq4_four_sections.png` (**the RQ4 answer** per author decision 5; `phase3_climate_shift.png` demoted and re-captioned **reach context** because 110 of 114 segments are surface-only lower bounds). **AUDIT: 86 verified rows (78 claim rows + 8 figure-compliance), 39 current / 8 superseded-and-labelled / **26 superseded-and-UNLABELLED** / **4 untraceable** - all 30 of the last two FIXED; counts are `python`-parsed from the audit file itself so its own arithmetic is checkable.** Sweep findings: **(a)** `26.9 [21.6, 35.3]` on 63 rows at 46.39 m is the resolved value; **no A1/A2 conflation found anywhere** (the two anchors are 0.11 m apart and resolvably different at rho = 1.249); **(b)** the six unqualified "every Euler-flip count exactly 0" claims now all carry **at N = 1e5**, and the KP 57.4 anchor's own **1-in-521 flip contamination** (biasing 42.7 down ~0.2%, conservative) is propagated wherever the anchor is; **(c)** ADR-0029's *"must* come from the tilted estimator" softened to *may* plus a dated scope amendment, and `architecture.md` failure mode 5 gained the ratio exclusion; **(d)** the cancellation withdrawal **verified independently** - present in all 10 locations, and `architecture.md` sec.7 + the decisions table additionally strengthened because they asserted L-conditionality as if it were the *whole* conditionality (k_aq is larger). Also corrected: `phase3_report.md` sec.6 (2->**3** of 114 above 1e-3/yr; KP 62.0 ratio 19.5->**12.7**; four KP 62.0 brackets), `phase2_report.md` sec.11.3 (KP 62.0 anchor 0.01->**0.05%**, strict 30.46->**39.55%**), `stage6_6_report.md` sec.1/4.1 (15->**10.5** and 1.9->**1.4**; C3b/C4b 7.9->**6.0**), `architecture.md` revision note (0001-0046 -> **0001-0048**). **ADR-0047's matched pre-adoption set regenerated ATOMICALLY** (`dem_cross_section_study.py all --overwrite` + `ratio --overwrite`): `datum_check` **present**, KP 62.0 now `baseline_L_m` 40.0 with the `withdrawn_1998` arm, and every published number reproduces (0.132/0.749/0.232/0.279/**0.201**; rho 2.250/1.823/3.219/**2.106 = 1/0.475**). **HARDENING SWEEP: the same two Step-4 anti-patterns were STILL LIVE in `cmd_verify`, the sibling of the function Step 4 fixed** - a gate raising before `result.save()` (discarding a 15 min ladder per section, and on a two-section run the *other* section's verified record too) and an overwriting per-section writer; both fixed. `stage6_6`'s summary was rebuilt from scratch, so `--sections kp62_0` deleted KP 57.4 and would have failed the campaign's own G3 gate; fixed by merging. `timestep_convergence_stress.py` has the same collision shape but is **listed not fixed** (merging a `--quick` smoke payload into the production record would be worse). `run_sweep`/`phase3_campaign`/`gsa_study`/`convergence_study`/`production_campaign` are clean on both axes. **ADR-0024 checked on every regenerated figure: no violation** (the extension is 51.0-56.5 m, `attainable_max_m` = 50.5). msc-thesis: one `untraceable` fix (`tab:kaq_scenarios` column head "Shoulder" -> **"Transition midpoint"**, since ADR-0045 and ADR-0048 mean stages two orders of magnitude apart in P_f); **10 typeset em dashes across 4 chapters FLAGGED not fixed** (house rule, prose not in scope). Ch. 6 and Ch. 7 remain 84- and 73-word skeletons: **47 numbers await a home, 29 with a figure and 18 without** - that manifest is the input to the chapter-drafting step. **625 green** (602 + 23), ruff/black clean; no `Config` default, physics, config, CSV or persisted production sweep touched.

- **Figure provenance at full coverage; the ADR-0032 margin disambiguated (2026-07-31; `scripts/production_campaign.py` `FIGURE_DRIVERS` + `stage_figures`, `docs/number_audit_2026-07-30.md` §M + addendum, `docs/decisions/adr0032-aquifer-response-diagnostic.md`, tests `tests/test_figure_pass.py`).** **G7 COVERS 52 OF 52** (was 44): the eight figures whose studies have no plot-only path now carry **declaration-only** entries — `command: None`, real `requires`/`produces`/`sources`, plus a `redraw` field recording what re-running would cost — so they are staleness-gated **without being re-run**, and the coverage note became a **hard gate** (`every tracked publication figure is declared`). All 52 verified content-current first; **0 figures changed content**, all 11 campaign stages still `passed`. **Three traps the declarations hit, each fixed rather than papered over:** (a) a shared `results/sensitivity/ce_prior/*.json` glob would have measured all three C_e figures against the newest of the three and failed the 2026-07-19 pair against the 2026-07-29 one — **one entry per figure**; (b) ADR-0039's figure and evidence are **both tracked and were added by one commit** (780eb0d) that left the JSON with a 2026-07-17 mtime and the figure with 2026-07-13, so on mtime it looks 4 days stale while the JSON's own `generated: 2026-07-13T07:53:42` **precedes** it by 3.6 min — new opt-in `source_epoch: "json_generated"` gates it on the content date, test-pinned; (c) **ADR-0032 is the one genuine "nothing to bind to"** (the study writes only the figure; its inputs are the two configs, which this campaign's own `configs` stage rewrites unconditionally, and the immutable gitignored d4PDF workbooks) — declared `sources: []` with the reason recorded in a new `staleness` field and surfaced in the manifest, **explicitly out of scope, never silent**; currency pinned instead by identity of `tau_aq_central_s` (680/765 s) between note and every run stamp. So: **51 staleness-gated + 1 recorded waiver = 52 declared, 0 unmapped.** **THE ADR-0032 MARGIN IS TWO DIFFERENT QUANTITIES, both correct, verified from the manifest before anything was edited:** `margin_vs_threshold` (3.83/4.31/8.37/19.54) is Π\*/Π_**corner90** — the pre-registered D3 p90 τ_aq corner — over each run's **canonical-event** T_rise (23 h); the published **~10×** (9.53/8.47) is Π\*/Π_**central** over the **ensemble-median** T_rise (18 h). **The published "Π ≈ 0.010–0.012" DOES match the measured 0.00821–0.00924**: same τ_aq (680.0/765.0 s exactly), different denominator — 680/64800 = 0.0105 vs 680/82800 = 0.00821. **No number changed**; every occurrence in `architecture.md` (§M4, §11, §13, pseudocode), the ADR, the companion note, the campaign doc and here now names its Π and its T_rise. **One real defect found:** the campaign doc called the margins "at the conservative `S_s` corner" — **wrong corner** (S_s is at its upper bound in *both* Π columns), corrected in place. **POSITIVE RESULT: ADR-0032 D5's pre-registered τ_aq-bounding pair is confirmed at all eight strata** — τ_aq central 765 (KP60.0) and 680 s (KP58.8) are the two largest against 350 (KP57.4) and 150 s (KP62.0), so the pair carries the smallest margins and bounds in the direction claimed; argued from the priors before any τ_aq existed, and now dated into ADR-0032 D5. Verdict `instantaneous` **8/8 at both Π definitions**. **Audit gap closed:** the 2026-07-30 pass contained **no ADR-0032 row at all** (grep "aquifer" → zero hits); new **section M** adds 9 rows (6 current / 3 untraceable-as-written, all fixed), counts re-parsed mechanically: **87 claim rows, 95 verified rows, 33 needed fixing** (was 78/86/30). **628 green** (625 + 3), ruff/black clean; no `Config` default, physics, config, CSV, persisted sweep or figure *content* touched.

- **Structural repository audit executed; the silent-skip guard class closed (2026-07-31; audit + execution record `docs/repo_audit_2026-07-31.md`, conventions §9/§10, commits `3608e04`..`b0eca87`).** A full tracked-tree audit (371 files, 15.9 MB) proposed **zero moves and zero renames** and the author approved that as the correct outcome: every structural candidate was measured against the path coupling installed by the figure pass and none cleared the bar. **The measurement is the deliverable** - a `docs/decisions/` restructure is **293 reference lines across 84 files** (66 tracked + 18 in the *gitignored* untracked supporting files), plus **12 paths whose SHA-256 sits in `results/production_campaign_manifest.json`** (8 configs + 4 evidence JSONs), 16 G7 `FIGURE_DRIVERS` literals, 6 hard-coded `test_figure_pass.py` paths and the thesis Appendix C register, against an **already gap-free 0001-0048 grammar**; a `docs/audits/` subdirectory was declined for a sharper reason (below). Recommendations against the restructure, the audits subdirectory and the `head-datum-re-convention-CLOSED.md` rename all stand as reasoned declines, recorded with their costs. **THE REAL FINDING WAS NOT STRUCTURAL: 13 guards across 3 test files gated on a *tracked* path via `pytest.skip`/`skipif`, so a move, rename or deletion disabled them while the suite stayed GREEN** - and the worst skipped when *the claim was absent from the document*, so **deleting the Euler-flip claim made its own guard pass**. All 13 now assert (`_require`/`_require_tracked`); the 11 genuinely-optional gitignored-data-drop marks keep `skipif` and already say "untracked"; the claim set names its exemptions explicitly (`EULER_CLAIM_EXEMPT`, empty by design); `test_no_guard_in_this_file_skips_on_a_tracked_path` keeps it out, **AST-parsed not grepped** (a string check matched its own explanation twice). **Listed not fixed, exactly one:** `test_phase2_events.py::requires_processed` gates on the **tracked** `data/processed/2016_event/stage_hourly_Tokachi_201608.csv` across ~~12 decorator sites, 6 unpaired~~ **9 sites, 4 unpaired** (count corrected by AST parse; **CLOSED 2026-07-31, see the next bullet**) - a collection-structure change, not a line. **DELETED with recovery `3608e04`:** root `figures/` (11 PNGs, **SHA-256 identical 11/11** to their `docs/figures/` twins, re-verified immediately before removal; nothing wrote or read it - the thesis includes its *own* copies - and it sat **outside** G7, which gates `docs/figures/` only), the two never-used `configs/{base,experiments}/.gitkeep` stubs, and the empty untracked `notebooks/notebooks/`. **6 documents MARKED SUPERSEDED** (dated headers, zero body prose rewritten, nothing moved); **2 the audit listed were already compliant** and deliberately untouched. **Two authorised guardrail exceptions:** ADR-0027's two pointers to `OPEN-head-datum-re-convention.md` were dangling since the `08267ee` rename (**pointer strings + one dated line only**; no Status, decision or consequence touched - this discharges that commit's unfinished reference update), and a new `data/processed/2006_event/README.md` (its two sibling event dirs each had one; **`.gitignore:252` had to move to the 2011 `*/` pattern or the README would have been invisible** - verified scoped, `rain.xlsx` still ignored). **`README.md` was 4 lines** describing only the install and now describes the as-built three-package repo. **Retention policy written into tracked `docs/conventions.md` §10** (untracked notes are excluded from version control, so this policy must live here): `results/` is **2.12 GB / 723 files**; **`results/hwl_bias_resolution/` (839 MB) is EVIDENCE not a cache** - a 170 min re-run is not the artifact the manifest hashed; **11 of 15 `superseded_*` dirs were empty and were removed**, each asserted empty in the same operation, resolving the audit's open UNCERTAIN item (the driver creates the preservation dir before knowing it has anything to preserve); the 4 non-empty (476.6 MB, incl. `superseded_adr0047_L47`) stay to the defence. §9 records the two `docs/` document kinds, the three `docs/decisions/` grammars, the figure dual-write, and the skip-vs-assert rule. **Free by-product:** a stray real run of `assess_2011_2006_closure.py` rewrote its tracked evidence JSON - diffed key-by-key, **`runtime_seconds` ONLY (8 leaves), every substantive number identical**, so the ADR-0044 bound reproduces exactly; restored from git. **629 green** (628 + 1 new structural guard), ruff/black clean, **G7 resolves 52/52 with 0 stale**, `--dry-run` manifest path completes, **43/43 scripts compile**; no `Config` default, physics, config, CSV, persisted sweep or figure content touched.

- **Four audit leftovers closed; the "runs when probed" script class eliminated (2026-07-31; execution record `docs/repo_audit_2026-07-31.md` §12).** Four documented, verified defects the structural audit put out of scope. **(1) `scripts/assess_2011_2006_closure.py` had NO argparse**, so `--help` executed the whole 8-stratum study and silently rewrote its **tracked** evidence JSON `docs/decisions/adr0044-event-closure-bound.json` — that is precisely how the audit's own `--help` sweep triggered the stray run it recorded as a by-product. It now carries the convention surface (`--strata` choices per `foreshore_width_study.py`, `--out` defaulting to the tracked record so the campaign's no-argument call is byte-unchanged), and a `--strata` subset **merges into** the existing record rather than truncating it — the `prior_mean_scenario_companion.py` precedent, chosen because writing it any other way re-introduces the overwriting-per-section writer the 2026-07-30 sweep had to fix twice (verified: one-stratum re-run preserved all 8 entries in `STRATA` order, zero substantive diffs). **(2) Sweep of all 43 scripts.** Shape (a) *no argparse ⇒ any argument runs the tool*: **22 drivers, ALL fixed** with the same three lines at the top of `main()`; `seepage_length_figures.py` had no `main()` at all and gained one; the 43rd file `_figstyle.py` is a style **module** with no entry point and is correctly left alone. Shape (b) *unconditional tracked-`docs/` write with no `--overwrite`*: three classes, **only the first a defect** — the one driver that wrote before it could be asked what it does (fixed); the **`docs/figures/` publication writes, deliberately NOT guarded** (in-place overwrite is the dual-write contract the figure pass installed, and **G7 re-renders unconditionally then gates on staleness — an `--overwrite` refusal would break G7 and reopen the copy problem**); and four evidence JSONs on hard-coded tracked paths (`mp_model_factor_companion`, `prior_mean_scenario_companion`, `ztoe_sensitivity_study`, `tail_variance_study`) **listed not fixed** — writing that record is the driver's purpose and **none is invoked by `production_campaign.py`** (ADR-0045/0046/0048 knobs stay OFF per campaign decision 3), so none can churn a tracked file during a campaign; cheap future improvement is an `--out` default apiece. **One pre-existing non-defect:** `dem_cross_section_study.py` / `foreshore_exhaustion_study.py` raise `UnicodeEncodeError` on `--help` into a **cp1252 pipe** because `description=__doc__` carries the deliberate Japanese terms conventions §8 keeps here; both exit 0 under UTF-8 stdout, nothing is written either way, and both candidate fixes are worse than the symptom. **(3) `tests/test_repo_hygiene.py` docstring claimed `docs/references/` was excluded from the tree scan; `skip_dirs` never excluded it — THE CODE WAS RIGHT**: `skip_dirs` holds only directories that are *not ours*, `docs/references/` is curated, the stated reason (gitignored PDFs) buys nothing since a `.pdf` cannot match `THESIS_SUFFIXES`, and gitignored-ness exempts nothing else in the same scan (`results/`, `data/`). Docstring corrected with the withdrawn claim named so it cannot return as a "fix". **(4) `requires_processed` — last member of the silent-skip class — converted** to `pytest.mark.usefixtures("tracked_2016_extracts")`, whose fixture asserts both committed extracts and names the regeneration command. **Final split (AST-parsed): 9 `requires_processed` sites tracked ⇒ now assert, same 9 sites; 5 `requires_rating` sites untracked `data/raw/` ⇒ `skipif` retained** and commented with why. **No decorator site moved — only the outcome flipped from skip to fail, so this was a one-mark change, not the collection-structure change the audit feared.** New structural guard `test_no_existence_skip_in_this_file_gates_on_a_tracked_path` (`ast`-parsed, not grepped, for the same self-matching reason as its `test_figure_pass.py` sibling). **(5) `docs/architecture.md` §9's two stale example paths** fixed (`configs/tokachi_kp58.yaml` → `configs/kp58_8_historical_matrix.yaml`; `results/tokachi_kp58_historical.h5` → `results/tokachi_kp58.8_historical_matrix.h5`); prose only. **630 green** (629 + 1 = the new structural guard alone; the `requires_processed` conversion changed no count), ruff/black clean, **G7 still 52/52 declared with 0 stale** (51 gated + 1 ADR-0032 waiver), `--dry-run` completes with all 11 stages resumed, **43/43 compile and 42/42 drivers answer `--help` exit 0 with `git status` clean of artifacts afterwards** (which also proves no script does I/O at import), 6 touched figure drivers re-run with **0 figure bytes changed**; no `Config` default, physics, config, CSV, persisted sweep, figure content or ADR touched.

- **Five thesis-inventory figure gaps closed; the two gitignored-only sources given committed slices (2026-07-31; driver `scripts/thesis_figure_gaps.py`, tests `tests/test_thesis_figure_gaps.py`).** `docs/thesis_number_inventory_2026-07-30.md` named five numbers with high thesis value and no figure anywhere; all five are built, each with a committed **CSV table source** beside it so a chapter typesets from the same evidence: **(1)** `phase2_survival_update.png` -- the central Bayesian claim (4.1/4.2/5.2), drawn so the **nesting is visible rather than asserted** (the transient rejection bar sits *inside* the static one; marginal transient rejection is a column of exact zeros in all **16** runs, and the masked-vs-re-evaluation verification is annotated, not implied); a second panel gives the honest width of the claim under all three documented readings (0 to 5.7 % deliverable against 39.6 to 99.6 % strict no-initiation). **(2)** `epistemic_bracket_ranking.png` -- 6 epistemic brackets + the 2 statistical yardsticks x 5 anchors x 4 sections as log spans; **`unbounded` is drawn as an arrow, never clipped to a finite number that would read as a measurement**, and is kept distinct from **`not defined`** (KP 57.4's design-level anchor has ZERO transient failures in 1e5, so no multiplier exists there -- a fact about the section, not a gap). Panel C carries the cancellation claim. **(3)** `adr0040_kp57_4_bound.png` -- B against stage with both anchors marked **unresolved**, the Clopper-Pearson bound **B >= 148** drawn as a bound, and the quotable **42.7 [39.4, 46.6] at 39.50 m on 521 rows**; the anchor's own **1-in-521 Euler barrier-jump contamination is drawn on the row-count strip**, not left in the note. **(4)** `rq4_sensitivity_brackets.png` -- lambda_ac, bulk d70 and prior-vs-posterior as factors on the production deliverable, **both climates on one shared log axis** so a bracket is comparable between them. **(5)** `epistemic_knobs_mp_ztoe.png` -- m_p (ADR-0045) and z_toe (ADR-0046) at all four sections, **on one shared y scale** so that m_p barely leaves the unit line while the datum bracket swings four decades is visible rather than stated. **THE SOURCING RULE IS THE STRUCTURAL PART:** figures 1 and 4 were sourced ONLY from gitignored `results/production_campaign_manifest.json` and `results/system_integration/phase3/rq4_annual.csv`, so they would not have regenerated on a fresh clone; a separate `extract` stage now cuts the slice each needs into committed `docs/decisions/{phase2-survival-update-per-stratum,phase3-sensitivity-brackets}.json` and `figures` reads **committed evidence only** (AST-pinned). **The provenance gate is a CONTENT comparison, not a file digest** -- re-extracting from the live artifact must reproduce the committed block; the manifest carries per-stage timestamps, so a SHA-256 gate would fire after every campaign run without a number moving (the `runtime_seconds`-only churn the audit already hit), and the recorded digest is kept only to identify which artifact was cut. **G7 stays at 100 %: 57 of 57 declared, 0 stale** (56 gated + 1 ADR-0032 waiver), and the forced figures stage reports **`figures_changed: []`** -- every pre-existing figure re-rendered byte-identical. `hwl_bias_resolution`'s `adr0040_*.png` glob was **narrowed to its two exact filenames**, because it would otherwise have claimed the new `adr0040_kp57_4_bound.png`, bound it to the wrong driver's sources and never re-drawn it; a test now forbids any figure being claimed twice. **Two corrections the work forced:** inventory **5.4's *"< 2 % elsewhere"* is WRONG** -- KP 60.0 historical drops **11.0 %** against KP 58.8's 12.4 % (both are the informative sections; only KP 57.4 and KP 62.0 are < 0.3 %); and the inventory header's **"47 / 29 / 18" does not reconcile with a row-by-row parse** (74 numbered rows: 45 with a figure / 24 without before this pass, **55 / 14 after**), so **14 figure-less entries remain, not 8** -- both recorded as a dated addendum rather than by rewriting what the 2026-07-30 pass said. **Also carried into the figures, deliberately:** the synthesis record's `design_hwl` anchor is the nearest **grid level** to a section's HWL, not the HWL (KP 62.0: 46.50 vs 46.39 m), so every panel says "design-level anchor" and states the HWL beside it -- ADR-0040 section 2.5 resolved those apart at rho = 1.249 and a test forbids the conflation; and the m_p annotations quote values **measured from these curves at named anchors** (static x3.0 to 6.0 deepest / x1.3 to 1.7 at the rising limb; transient maxima x1.6 to 2.8) rather than reproducing ADR-0045's two-section headline at its own stage, which is the 7.14 "two shoulders" trap. **NOT DONE, reported not silently dropped:** inventory 4.3/4.4/5.1 were **not** promoted from gitignored `results/phase2/figures/` -- `bayesian_reliability_updating.pipeline._figures` writes into a single `out_dir / "figures"` with **no dual-write seam**, so promoting the two named kinds (16 of the 44 files) means editing a shipped Phase 2 module whose posteriors are SHA-256-recorded in the campaign manifest, not copying a file; a manual copy is exactly what conventions section 9.3 forbids. **666 green** (630 + 36), ruff/black clean; no `Config` default, physics module, config, CSV, persisted sweep, ADR or existing figure content touched, and no `.tex`/`.bib` anywhere.

- **The four Phase 2 figure gaps the approved thesis plan named are closed; the Phase 2 package gained its own dual-write seam (2026-08-02; `bayesian_reliability_updating/{pipeline,plots}.py`, `scripts/thesis_figure_gaps.py`, tests `tests/test_thesis_figure_gaps.py`).** `msc-thesis/scratch/THESIS_PLAN_2026-08-01.md` section 3.4 carried inventory rows **4.3, 4.4, 4.7 and 5.1** forward as a dependency this repository had to resolve; all four now have a tracked publication figure. **(A) The seam, not a copy.** Rows 4.3/4.4/5.1 were marked *yes* in the inventory but their figures existed only under **gitignored** `results/phase2/figures/` -- not tracked, not G7-gated, and absent from a fresh clone. `plots._save` gained an optional `publication_path` and `pipeline._figures` passes one, so **both copies come from one figure object in one call** (the `stage6_6._write_figure` / `plot_fragility_curves.save_both` pattern); `publication_path=None` is the default everywhere else and is bit-identical to the pre-seam behaviour, AST-pinned to **exactly the two promoted plot calls** so the seam can neither be dropped nor widened by accident. **(B) Four of 44 promoted, deliberately:** only the `marginals` and `fragility_update` kinds (the other four kinds stay run-local diagnostics), and only at **KP 58.8 + KP 60.0 matrix** -- the two informative strata (transient rejection 5.67 % / 3.36 % against <= 0.07 % everywhere else). Every number rows 4.3/4.4/5.1 quote is measured at exactly those two, and row 5.1's *concentration* half is already carried across all eight strata by `phase2_survival_update.png`, so a near-null pair at KP 57.4 or KP 62.0 would add a figure without adding a fact. **Keying the registry on the run stem is load-bearing:** ADR-0046 suffixes a z_toe scenario's stem (`_ztoe_plus0.30m`), so a scenario finds no entry and writes nothing to `docs/figures/` -- the guarantee that a scenario can never masquerade as the baseline now extends to the publication directory. **THE PERSISTED POSTERIORS WERE NOT TOUCHED:** the redraw runs through the existing `--figures-only` path (recomputes in memory from the Phase 1 parent, writes no artifact), and all **16** `results/phase2/*_posterior.{h5,json}` were asserted **byte-identical by SHA-256** across every run in this pass -- which matters because the campaign manifest records those digests. **(C) Row 4.7 was BUILT, not declined:** `phase2_peak_shortcut.png` + `phase2-peak-shortcut.csv` from a third committed slice `docs/decisions/phase2-peak-shortcut.json` (cut by `thesis_figure_gaps.py extract` from the 8 sweeps + 8 posteriors, recording all **16** source paths and SHA-256s, with a content-not-digest reproduction gate). `phase2_report.md` section 11.1 states the comparison in prose only, so the slice recomputes it -- the peak-only reading is the Phase 1 **prior transient** curve interpolated **linearly on its raw MC points** at the observed 2016 peak, which reproduces the published percentages to three decimals and the factors exactly: KP 58.8 15.596 % vs 5.673 % (**x2.75**), KP 60.0 13.114 % vs 3.363 % (**x3.90**). **Both readings are transient**, so this is method against method on one sample, never limit state against limit state (a peak-only *static* number would have read as a far larger effect: 57.6 % at KP 58.8). Three things the figure refuses to blur: four strata reject nothing under **either** reading so their factor is **not defined** -- not 1.0, not unbounded, and they keep their rows and say so; KP 57.4 matrix (x7.46, **65** rejected rows) and KP 60.0 bulk (x6.12, **23**) are hatched as the section-11.1 **small-number regime** and **excluded from the headline band**, since admitting them would widen the published "2.75 to 3.9x" to "2.75 to 7.5x" on the strength of 88 rows. [**Superseded 2026-08-20: the headline band itself is canonical-event-conditional and is now the two-event bracket 1.45 to 3.90; see the 2026-07-13 entry's marker and `canonical-shape-sensitivity.json`. The small-number exclusion reasoning below is unaffected.**] **(D) A pre-existing legibility defect fixed because promotion exposed it:** the marginals panels used matplotlib's default log locator, which labels decade *minors* -- illegible collisions on the sub-decade `gamma'_bl` panel (5.5 to 9.5) and row-title overlap with no `tight_layout`. Percentile-anchored ticks now give exactly three in-range labels whatever the span. **Chrome only; no plotted value depends on it**, and it was invisible while these figures lived only under gitignored `results/`. **G7 stays at 100 %: 57 of 57 -> 62 of 62 declared, 0 stale** (61 staleness-gated + the one recorded ADR-0032 waiver), forced figures stage `figures_changed: []`. The Phase 2 entry is the **first whose command is `-m <package>` rather than a `scripts/` path**, so the driver-shape guard in `test_figure_pass.py` was **widened to this repo's three packages rather than deleted**; at ~6.2 min for two strata it is the slowest redraw path there, and **declaring it declaration-only would have been cheaper and wrong** -- a real plot-only path exists, so the four figures are kept unconditionally fresh rather than merely watched. **Inventory recount, mechanically parsed:** 74 numbered rows, **55 with a figure / 14 without -> 56 / 13**; only 4.7 moves the count, the other three were already counted. **13 figure-less entries remain**: 1.3, 2.6, 4.6, 4.8, 4.9, 4.10, 5.3, 6.12, 7.6, 7.8, 7.12, 7.13, 8.3. The 2026-07-31 "Not done, and why" paragraph is left standing and answered by a new dated section rather than rewritten. **685 green** (666 + 19), ruff/black clean, `--dry-run` completes with all 11 stages resumed and `--help` inert with `git status` unchanged; no `Config` default, physics module, config, CSV, persisted production sweep or ADR touched, and no `.tex`/`.bib` anywhere.

- **Every main-body thesis figure re-rendered clean of engine-internal identifiers and em dashes (2026-08-04; rule in tracked `docs/conventions.md` section 9.3.1, handoff record in `msc-thesis/scratch/EXECUTION_LOG.md`).** The thesis campaign's 2026-08-04 integrity checkpoint found that **binding rules 2 and 3 and the em-dash rule are violated in exactly one place no `.tex` scan can reach: the rendered content of the engine figures placed in the main body**. It inspected the 13 already placed and reported 10 dirty. This pass covered **all 30** main-body figures of plan section 3.2, including the **17 that were not yet placed and had never been inspected by anyone**, and found **22 dirty against 8 clean** -- so **11 of the 12 newly found came from the unplaced Chapter 6/7/8 set**, which is the half the checkpoint could not see. **One contradiction of the checkpoint, confirmed by eye:** `validation_yabe_timeline.png` was listed clean but prints "engine breach" in a panel title. **The worst offenders were all in Chapter 6, not Chapter 5:** the three `fragility_*.png` share a stamped footer carrying **five ADR numbers and three em dashes**, and its first line was **clipping off the right edge of the canvas** in all three (the function's own docstring claims it is "wrapped manually so nothing clips"); dropping the ADR pointers and rewrapping fixed both. **`adr0047_dem_seepage_length.png` also rendered a claim this repository's own record contradicts** -- "measurement only, no input value changed", true of the ADR-0047 measurement study but overtaken when it was **adopted at KP 62.0** and `L_m` went 47.0 to 40.0 -- closing the open item `conventions.md` section 9.3.1 had carried since 2026-08-02. **No filename changed**, deliberately: a filename never typesets, "ADR" appears zero times in the typeset main body, and keeping the names means **not one `.tex` file had to be edited** and step 8 inherits a working document. **No plotted value changed**: 54 hunks across 14 drivers, every one read, and every one a string literal, a display map, a docstring, an import, or the new additive redraw branch. **A record field name is never renamed to satisfy this rule** -- the substitution happens at render time through per-driver display maps (`ARM_DISPLAY_NAMES`, `LADDER_DISPLAY_NAMES`, `CASE_DISPLAY_NAMES`, `RUN_DISPLAY_NAMES`) plus one shared `scripts/_figstyle.py::section_label` (`tokachi_kp58.8` to `KP 58.8`, unrecognised input returned unchanged). **That distinction was not theoretical: the first attempt edited `PHASE2_RUNS`'s `no_breach` description, which is carried verbatim into the committed slice `docs/decisions/phase2-survival-update-per-stratum.json`, and `test_thesis_figure_gaps.py` caught it.** **`scripts/timestep_convergence_stress.py` gained the `--figures-only` path** the 2026-07-31 audit had said it lacked (the `stage6_6_gap_decomposition.py` precedent): it rebuilds only the showcase trajectories from the persisted ladder -- the evidence already carries both member ids, so no ladder and no 3000-member flashiness scan re-runs -- and **writes no evidence file**, which retires the overwriting-per-section collision the audit recorded and left alone; `--figures-only` and `--quick` are mutually exclusive so a smoke payload can never reach the production record through the figure path. Its `FIGURE_DRIVERS` entry is **promoted from declaration-only to a real redraw**. The R10 figure was regenerated with its evidence **diverted to a scratch path via the existing `--out` flag**, so the tracked JSON was never written at all; the scratch record reproduces it exactly apart from `elapsed_s` and the known additive `d4pdf_ensemble` population. **Gates: G-A** an AST sweep of every rendered-text call in all 14 main-body drivers returns **0 hits**, with the 7 surviving hits proved by enclosing-function analysis to belong to appendix figures only (`adr0031-tail-lhs-vs-crude*`, `epistemic_knobs_mp_ztoe`, `stage6_6_heq_*`, `adr0040_tilted_is_validation`); **G-B** all 30 opened and read after regeneration, which is the real gate because a PNG holds no extractable text and strings are assembled at runtime -- it caught the `gsa_levels` suptitle **overrunning the canvas** on the first attempt, fixed by shortening; **G-C** the diff above; **G-D** 678 fast tests green, ruff/black clean, **no tracked evidence JSON or CSV moved at all**. **Appendix figures are deliberately out of scope and still carry ADR numbers, `fm5` and em dashes**; the exemption is now written down so it is a decision rather than an oversight. No `Config` default, physics module, config, CSV, persisted sweep or ADR touched, and no `.tex`/`.bib` anywhere.

- **Canonical-shape invariance established from source; defence-brief item A1 answered in substantial part at zero runtime (2026-08-09; companion note `docs/decisions/canonical-shape-invariance.md`).** Nothing was run: no sweep, no study driver, no campaign, no figure regeneration, no `results/` artifact, no code. **The pinned member `HPB_m064_1987` reaches the results through exactly ONE channel — it supplies `h(t)` to the M7 timestepper. It does NOT supply the peak.** `conditioning_record_for_level` sets `peak = level_m` **verbatim** (`hydrographs.py:731`; `resample_record` carries it through at `:804`) and M8's static branch reads `float(hydrograph.peak)` and nothing else off the record (`evaluator.py:874-877` batch, `542-545` scalar; `record.h` is not touched until `:881`). So everything that consumes only `peak` is **exactly** invariant, not approximately. **C1 CONFIRMED** (whole static branch: `P_f_static_raw`, its fit, its CP band, `Z_static`, plus `H_c`/`l_c`/`lambda_in`/`r_e`/`leakage_geometry`). **C3 CONFIRMED** (Phase 2 rejection 5.673 %/3.363 % and the marginal-transient-rejection = 0 nesting at all 8 strata: the record is built from the observed Obihiro series + trace anchor, `events.py:442-450, 475-484`, and `replay.py` imports neither `load_canonical_shape` nor `conditioning_record_for_level`, `:42-51`; the **posterior curves** are still conditional, since they mask the retained Phase 1 transient matrix). **C4 CONFIRMED** (numerator-only exposure: `thesis_figure_gaps.py:499` vs `:500`). **C2 CONFIRMED FOR THE COMPONENTS, REFUTED AS STATED FOR THE PERCENTAGES** — `C0/C0b/C1/C2` and the analytic `C3a/C3b` are all invariant, so exactly one telescoping step (`temporal_net`) is shape-exposed in each ladder, plus `heq_conservatism_*`, `dimensional_at_transient` and the two ladder totals; **the whole static Shapley lattice is invariant** (`gap_decomposition.py:871-879`); but `component_table` computes `fraction = delta / total` with `total = C0 - C4*` (`:796, :811`), so **the head-convention SHARE (0.75 to 0.97 at design level) is shape-conditional even though the component in probability units is exactly invariant** (1.35e-3 at KP 62.0 HWL, 1.16e-3 at KP 57.4 — unmovable). `C3` is shape-free *by design*, a by-product of ADR-0040 Decision 2's closed form. **NEW CONSTRUCTION RESULT the brief did not ask for: the design-HWL bias (invariant numerator / conditional denominator) and the peak-shortcut factor (conditional numerator / invariant denominator) have OPPOSITE one-sidedness, so under one shape swap they move in opposite directions** — any future shape study must report both. **Sign under the pinned alternate `HPB_m067_1978`: likely toward one, NOT determined, and Chapter 6's stated antecedent is not established.** Two opposing channels: monotone `l` makes m064's second episode's contribution non-negative (and it is live — arithmetic on recorded numbers, rating-coefficient-free via `s(Q) = (sqrt Q - sqrt Q_base)/(sqrt Q_peak - sqrt Q_base)`, puts the secondary peak at stage-shape **0.777** and the inter-peak trough at **0.4962**, the latter reproducing ADR-0023's independently measured 0.498; at both informative anchors the trough sits **above** the toe, so the canonical member presents one continuous ~55 h above-toe window, not two episodes), against a broader single crest — m067's only recorded comparable number is a **32 h rise against m064's 23 h rising limb, i.e. LONGER**, and its definition is unstated. So Ch 6's "a canonical event of shorter duration above the toe would move it toward one" is a sound *conditional* whose antecedent the repo does not record for this alternate. **The reorder route is blocked three ways** (`run.py:318` hard-codes `canonical_event_ids[0]`; the field is in `to_metadata()`/`config_hash()` — `config.py:753, 1090-1097, 1110` drop only the three `None` optional blocks, never the hydrograph block — so reordering a committed YAML breaks the `replay.py:222-228` hash gate; and `tests/test_configs.py:257` pins the exact ordered list, echoed at `test_config.py:447`/`test_run.py:971`); **the route that works is the in-memory-YAML pattern of `scripts/foreshore_width_study.py:80-89` with `persist=False` and the `:92-105` baseline bit-identity assert** — cost **~39 min** (the 8 sweeps' recorded `runtime_seconds` sum to 1,156 s, and an arm pair doubles it), no new module, no config field, no ADR. **NOT ATTEMPTED and reported as such:** Stage 6.6 and Phase 3 both lack a variant axis AND an out-dir (`stage6_6_gap_decomposition.py:57-58` module-constant `OUT_DIR` + dual-write over tracked `docs/figures/`; `phase3_campaign.py:152` defines **no** arguments at all), and — a hazard found on the way — `verify_against_production` **skips rather than fails** on a config mismatch beyond `length_effect` (`:187-190`), so a shape arm would silence the ADR-0040 gate (i) and overwrite the guarded record with unguarded evidence; and the Uemura surface curves hard-code the **same** member (`generate_uemura_surface_curves.py:69`), so a BEP-only swap would confound shape with mechanism in the RQ3 dominance share (Phase 3's *hazard* side is unaffected — `hazard.py:311-322` streams all members). **Thesis targets named, none edited** (this repo holds no thesis prose): Ch 6 §5.1 = `...Analysis.tex:1130-1142` (the two ways the brief named it are one paragraph), Ch 7 `tab: limitations` row at `7. Discussion.tex:709-714` (Affected column is too broad — narrow it to *transient* conditionals and enumerate the invariant set), `tab: gap components` caption at `...Analysis.tex:530-575` (shares conditional, components not), plus Ch 6 §2, Ch 5's GSA scenario-invariance paragraph (`5. Verification…tex:487-492` — "would evaluate an identical loading record" is weaker than the truth: it **is** the same record, pinned by `gsa_study.py:343-360`) and Ch 4 `subsec: Conditioning Sweep and the Canonical Hydrograph Shape`. **685 green**, ruff/black clean; no code, config, CSV, ADR, figure, persisted sweep or `.tex`/`.bib` touched.

- **The silent-skip class closed in a DRIVER gate; Stage 6.6 now refuses to overwrite a guarded record it never verified (2026-08-10; `scripts/stage6_6_gap_decomposition.py`, tests `tests/test_stage6_6_driver_gate.py`, record `docs/repo_audit_2026-07-31.md` §12.8, rule in tracked `docs/conventions.md` §9.4).** **This is a scope boundary the 2026-07-31 audit DREW, not one it missed** — Amendment 1 and §12.4 closed the class for `tests/` (13 guards skip→assert) and its two AST guards are **per-test-file by construction** (each parses the module it lives in), so neither could ever see a driver; §12.2's 43-script sweep looked for two *other* shapes (no-argparse, unguarded tracked writes), not for a gate that records a failure and continues. **The defect:** `verify_against_production` (ADR-0040 gate (i), C0/C4b bit-identical to the persisted sweep) set a `status` string and **returned** on `skipped_missing_production_file` / `skipped_config_mismatch_beyond_length_effect` / `skipped_n_mismatch`, after which the driver persisted `results/stage6_6/`, **dual-wrote the tracked `docs/figures/` copies** and exited 0 regardless — the campaign's G3 does check the status, but only after everything was already overwritten. **A FOURTH outcome was not in the brief's enumeration because it produced no status at all:** `main` called the guard only `if args.n is None`, so a pilot `--n 10000` — which writes to *exactly the same paths* and can never be bit-identical — skipped verification entirely; that is the sharpest of the four and is now `absent` ⇒ blocking. **The fix:** refuse with **exit 1, before persisting anything and before writing any figure**, unless `--allow-unverified` is passed (named for what it permits, the `system_integration --allow-stub` precedent); when passed, the summary records `allowed_unverified: true` beside the status so a permitted run stays legible and G3 still fails on it. **Ordering is deliberately the OPPOSITE of the 2026-07-30 hardening and the docstring says so** — that hardening made two sibling functions persist-then-gate after a gate discarded 2.5 h of freshly *created* evidence; the rule underneath is "never let a gate destroy the evidence it protects" and it is **direction-dependent**: there the write created evidence, here the write **overwrites a guarded record**, so the gate goes first. **Cheap outcomes cost zero compute:** three of the four are decidable from the config alone, so `production_comparability` was factored out of `verify_against_production` and is called once **before** the ladder — one implementation, two call sites, no parallel rules — turning a 20-minute punishment into a seconds-long refusal. `--figures-only` **cannot** reach the gate (read-only redraw of persisted evidence; a test replaces all three gate entry points with raisers and asserts the redraw still completes). **CAMPAIGN PATH ASSERTED, NOT ASSUMED:** it invokes `--n-jobs N --skip-figures` with no opt-out and its recorded status is `bit_identical` at both sections (38 + 23 levels); replaying the refactored guard over the persisted ladders reproduces both records **key-for-key, same order, same values**, so `stage6_6_summary.json` is byte-unchanged and the refusal never fires there. **Smoke-tested for real:** `--sections kp62_0 --n 1000` refuses in seconds with exit 1 and all **26** files in `results/stage6_6/` unchanged by mtime and size, tracked figures untouched — previously that invocation would have silently clobbered the production ladder and the publication figures with N=1000 evidence. **Tests (9):** the named pair `test_gate_refuses_a_mismatched_config_before_anything_is_written` / `test_gate_passes_on_the_production_path` run end-to-end on the stub hydrograph path at N=400 against a purpose-built stub "production" sweep; `test_every_status_the_driver_can_write_is_classified` **AST-parses the driver** (all four literal statuses found) and fails if a fifth non-verifying status is ever added unclassified — the per-test-file guards generalised to this driver. Nothing skips on a tracked path; the single `skipif` is on gitignored `results/stage6_6/` and says "untracked". **NOT swept across the other 42 scripts** — this closes the one instance the 2026-08-09 canonical-shape review surfaced, and conventions §9.4 now states the driver rule so the next one is written correctly. **694 green** (685 + 9), ruff/black clean, `--help` inert with `git status` unchanged; no physics, no `Config` field, no ADR, no persisted result, no figure content, no `.tex`/`.bib`.

- **Aquifer-conductivity bracket propagated through the Phase 3 annualisation; the mechanism-dominance ordering does NOT survive it (2026-08-10; pre-registered note `docs/decisions/conductivity-bracket-annualisation.md`, evidence `...json`, driver `scripts/conductivity_annualisation_study.py`, figure `docs/figures/conductivity_bracket_annual.png`, tests `tests/test_conductivity_annualisation.py`).** Closes defence-brief item A2 and Chapter 7's own concession that the bracket "was not tested at this level at all": ADR-0048 and `epistemic-bracket-synthesis.md` measured the largest declared epistemic knob in the project on the **conditional** curves only, while every RQ3/RQ4 headline is **annualised**. **SCOPE, in every sentence that quotes a number: matrix d70 and prior side ONLY — no bulk-d70 conductivity arm has ever been run and no Phase 2 posterior exists for any arm** (prior-against-prior is exact at KP 62.0, where the two rows are identical to full precision because the 2016 update rejects 0.00 % there; verified from `rq4_annual.csv` before anything was computed). **Nothing re-swept**: the 16 persisted ADR-0048 arm sweeps (N = 1e5) consumed read-only, warm hazard cache reused, under two minutes total. **Part 1 was written and committed before any number existed** and its primary prediction **FAILED**: P1 ("KP 62.0 is the only section contestable historically") is refuted **exactly at the cell falsifier F5 named in advance**, KP 58.8 historical. **The measured answer: the ordering is contested at 3 of 4 sections historically and 4 of 4 under +4K.** The `field_geomean` arm changes the answer at **all eight** section-and-climate cells (7 reversals, 1 collapse); the far milder `field_toe` arm still reverses 3; the upward `regional_upper` arm reverses none (P4 held, so the ADR-0048 monotone mechanism checks out). **It consumes a 43-fold dominance margin at KP 58.8 and a 666-fold one at KP 60.0 under +4K**; only KP 60.0 historical is ROBUST, and only because its overflow is *exactly* zero, which is a statement about overflow's absence, not piping's resilience. **The comparison an examiner will make**: Chapter 7 already reports that the SMALLER bulk-d70 bracket reverses 2 of 4 historically and 3 of 4 warmed — reproduced here exactly from `rq4_annual.csv` — so conductivity is strictly worse and subsumes every cell d70 flips, and unlike d70 it is a **two-sided** bracket that also moves the **climate ratio** itself (KP 60.0 7.58 → 671, KP 57.4 12.6 → 234 under the downward arms; all four sections fall to 3.4–7.3 under the upward one, P6 held in both directions at every defined cell). **Annualisation does not average the bracket away, and the mechanism is measured not assumed**: the new contribution-weighted driving-stage band shows every section's annual number is carried by stages **at or above design high water** (KP 62.0's whole band sits 1.1 to 2.3 m above it), i.e. the *wide* part of the bracket, and the dominance ratio has a **conductivity-free denominator** (the Uemura surface curves), so unlike the Stage 6.6 static-vs-transient ratio there is not even a partial common-mode channel. Falsifier **F3 did not fire**: the annualised span (8.3 to unbounded) exceeds the published length-effect yardstick (1.9 to 3.4) at **every** section and scenario. **Negative control `gamma_bl_sub_lower` moves every annual number by 0.009–1.4 % and changes no ordering** (P7). **GATE 1, the load-bearing one: the baseline reproduces `rq4_annual.csv` string-identically over all 228 matrix/prior/250 m/primary rows × 20 fields**, with the composition step **imported** from `phase3_campaign.py` rather than re-implemented (test-forbidden second copy) so the gate exercises the production path; plus arm provenance (grid, N, hash round-trip, scenario label, 16/16), **880 non-BEP segment-scenario cells bit-identical**, and the hazard cache asserted unchanged. **Coverage: zero flagged cells** — no number here is a clamped bound. **One genuine caveat surfaced, a property of the PRODUCTION deliverable not of the arms**: at KP 62.0 under +4K, 7 of 5400 ensemble years (0.13 %) peak above the ADR-0024 attainable maximum of 50.5 m MSL and, because the curve is near saturation there, carry **11.8 % of the annual piping probability** — no coverage flag fires (correctly, nothing leaves the grid), so "the clamp flags are clean" is not by itself a statement that an annualised number rests only on attainable stages; historical is exactly 0.0, KP 57.4 exactly 0.0 in both climates. **Architecture: standalone companion, `phase3_campaign.py` diff exactly ZERO**; the only campaign edit is the mandatory 21-line `FIGURE_DRIVERS` declaration (exact filename, never a glob; real `--figures-only` redraw that writes no evidence file), **G7 now 63 of 63 declared, 0 stale**. **Companions entry deliberately NOT added and no exclusion either**: the G6 enumeration was run and **does not flag this driver** (its regex wants a literal `results/tokachi_kp<digit>` and this one composes paths by format string), so there is no UNCLASSIFIED to resolve and an entry would be a key never read; substantively it must not be a campaign companion because it consumes ADR-0048 outputs the campaign deliberately does not produce (knobs OFF, decision 3). **Cost recorded**: gate 1 is not re-checked by the campaign. **Reported not fixed**: the same enumeration currently carries **three pre-existing UNCLASSIFIED hits** (`bayesian_reliability_updating/pipeline.py`, `epistemic_bracket_synthesis.py`, `hwl_bias_resolution.py`). **One deviation from the brief, stated**: `--n-jobs` omitted — the study re-runs no sweep and has no parallelisable work, so it would control nothing. Note section 2.8 lists the exact thesis passages to change (Ch 7 ll.868–873, the `tab:limitations` conductivity row, Ch 9 answers-register row 3, Ch 7 §2's KP 62.0 dominance narrative) without editing them. **711 green** (694 + 17), ruff/black clean; no `Config` default, physics module, config, CSV, persisted production sweep, Phase 2 posterior, `rq4_annual.csv`, existing figure or ADR touched, and no `.tex`/`.bib` anywhere.

- **Two Phase 3 records of record updated for the conductivity result; ADR-0024's "hazard carries zero weight there" REFUTED (2026-08-10; `docs/phase3_report.md` sections 5, 6, 8 and new 12; `docs/decisions/0024-fragility-deliverable-tail-sections.md` dated note).** Documentation only: **no measurement, no run, no code, no new artifact** — the two numbers below that were not already in the 2026-08-10 companion record came from reading the committed Phase 3 hazard cache. **(1) New `phase3_report.md` section 12**, in the section-11 / `phase2_report.md` section-14 pattern (dated, *authoritative where it differs from sections 1 to 10*): states the A2 result with the per-cell verdict table (**REVERSED / COLLAPSED / ROBUST**, three of four sections historically and four of four under +4K), the bracket-width table against the published length-effect yardstick, the climate-ratio movement, the d70 comparison (2 of 4 and 3 of 4, so conductivity is strictly worse and subsumes it), and the mechanism (the integral samples the *wide* part of the bracket because every section's contribution-weighted driving-stage band sits at or above design high water; the dominance ratio has a **conductivity-free denominator**, so no common-mode channel exists at all). **The matrix-d70 / prior-side scope is in the section's first sentence, not a footnote.** **Pointers added at the heads of sections 5 and 6** in the ADR-0047 style, each sending the reader to section 12 and each stating explicitly that **nothing in those sections is withdrawn** — they remain the production reading at the adopted prior means; the addendum adds the range around them. Neither section was rewritten. **(2) New caveat 8 in section 8**, the standing list every consumer carries: *a clean `AnnualizedResult.coverage` flag set is NOT by itself a statement that an annualised number rests only on attainable stages*, because the flags detect peaks leaving the grid and cannot see peaks landing on the part of the grid that is inside it but physically unreachable. At KP 62.0 under +4K, **7 of 5400 ensemble years (0.13 %) peak above the attainable maximum of 50.5 m MSL and carry 11.8 % of the section's annual piping probability**; historical is exactly 0.0 and KP 57.4 exactly 0.0 in both climates; a property of the **production** deliverable, identical under every sensitivity arm. Operative rule stated for consumers: **read `coverage` together with the section's attainable maximum, never `coverage` alone.** **(3) The ADR-0024 back-pointer question, decided YES — and it is a correction, not a cross-reference.** ADR-0024 Implementation item 5 asserts the added levels are *"harmless in the fragility x hazard composition (the hazard carries zero weight there)"*. Reading the committed hazard cache shows **4 of 5400 +4K member-years peak above 51.0 m, the FIRST added level** (7 above 50.5 m; highest peak 51.47 m), so the extension is **not** weightless — the claim is false on the +4K hazard side. A **dated note is appended to ADR-0024** giving the per-climate table and the reason it was right when written (2026-07-03, **ten days before Phase 3 existed** and 26 days before the ADR-0047 adoption raised that section's fragility ×8.7 at design HWL). **Its Decision section was asserted byte-identical** by the edit script, and **Implementation item 5 is byte-identical apart from an inserted pointer** (asserted by stripping the marker string and re-comparing against HEAD): the parenthetical itself stands **verbatim**, with a bracketed `[Pointer added 2026-08-10: ...]` marker beside it sending the reader to the dated note, in the ADR-0038 decision-4 style, so a reader who stops at item 5 cannot miss the correction. The appended note follows the file's own "Author caution (Pol, 2026-07-07)" precedent for a dated qualification. **One published number corrected while verifying:** the `gamma_bl_sub_lower` control range **0.02 to 1.4 % becomes 0.009 to 1.4 %** (the KP 62.0 +4K cell is 0.0089 %, which the earlier reading rounded away), fixed in the companion note, section 12 and the preceding bullet; the control is quieter than stated, so no conclusion moves. **711 green** (unchanged — documentation only), ruff/black clean; no `Config` default, physics module, config, CSV, persisted sweep, Phase 2 posterior, `rq4_annual.csv`, figure or ADR **Decision** touched, and no `.tex`/`.bib` anywhere.

- **The campaign's G6 companion enumeration now REFUSES an unclassified consumer; the silent-skip class closed for the third time (2026-08-10; `scripts/production_campaign.py`, tests `tests/test_companion_enumeration_gate.py`, record `docs/repo_audit_2026-07-31.md` §12.9, rule in tracked `docs/conventions.md` §9.4).** `enumerate_companions()` greps the tree for files that both reference a persisted production sweep and assert bit-identity or a config hash — the census of who depends on the campaign's own artifacts — and its result went to `gates.note`, **never `gates.check`**. G6's only assertion was that every companion which *runs* completes, so a hit that was neither run nor excluded would be written into the manifest as `UNCLASSIFIED -- investigate` and could not fail anything; **three accumulated across four earlier passes**. **One brief premise did not reproduce and that is a finding**: the manifest does **not** list the three as UNCLASSIFIED — it never recorded them at all (14 hits, none of the three, and an older plain-**list** `found_but_not_run_here` shape), because `git log` puts two of the three at 2026-07-30 and 2026-08-04, i.e. **after** the 2026-07-29 campaign. They did not slip past a gate that saw them; the note-only wiring guaranteed the **next** run would record and pass them. Leaving the manifest alone therefore stands for a **stronger** reason than the one given — rewriting it would fabricate history, not refresh a view. **This is the third instance of one class and neither prior closure could have caught it**: §12.4's 13 test guards (skip → assert) are policed by two AST guards that **parse only the test module they live in**, and §12.8's Stage 6.6 fix is a predicate inside a different driver — nothing in either can see the campaign's gate records. It is also **the first instance with no write anywhere in it**, which is why conventions §9.4 needed a new paragraph rather than a restatement: both existing paragraphs are scoped to a write, and their remedy ("refuse before the write") is meaningless where nothing is written. **Enumeration reproduced by direct call before anything changed** (17 hits / 5 covered / 9 excluded / 3 unclassified / 4 run-but-unmatched — every brief figure confirmed). **All three classified after answering "should this RUN?" first, and none excluded on cost:** `bayesian_reliability_updating/pipeline.py` is not a driver but the shipped Phase 2 module the campaign **already executes as three of its own stages** under G2 (and is structurally unreachable — `COMPANION_COMMANDS` keys resolve to `scripts/<name>.py`); `epistemic_bracket_synthesis.py` synthesises the 16 ADR-0045/0046/0048 arm sweeps the campaign **deliberately does not produce** (decision 3) and, on a missing arm, records `available: false` and continues — so running it would exit 0 and let G6's completion check **pass on a synthesis with every epistemic arm missing** (~20 min); `hwl_bias_resolution.py` **does** have a cheap verification-only mode (`verify`, ~25 min recorded), so it is excluded **not on cost** but because that mode **imports `stage6_6_gap_decomposition.verify_against_production`** and calls it at G3's own two sections — re-asserting G3's claim through G3's function — while its remaining stages are the N=1e6 evidence (`brute` 4.2 h) that conventions §10.2 calls evidence rather than a cache, and its `figures` subcommand is **already** run under G7. The ADR-0045/0046/0048 "sidecar-reconstructed hash gate" sentence was **deliberately not reused** for the synthesis, which reads the current `configs/*.yaml` — it reads well and would have been false. **The fix:** `unclassified` and `exclusions_with_no_file_on_disk` are now returned and gated by `gate_companion_classification`; **the note is kept — the note is the evidence, the check is the enforcement**; both directions gate (a hit no rule mentions, and a reason naming a file that no longer exists, which would also mask the renamed file's reappearance); it runs **before** the subprocesses, since the verdict is source-decidable and nothing has been written, so this is **not** §9.4's persist-then-gate case. **The gate's first live catch was its own test file** — it quotes the f-string stem and "bit-identity" in its docstrings, the self-matching trap the AST guards were written around — and it was **classified, not exempted by narrowing the pattern**. **Blind spot decided, not inherited:** `conductivity_annualisation_study.py` asserts against the persisted `rq4_annual.csv` yet evaded detection by composing `f"tokachi_kp{kp:.1f}_..."`, where the old `tokachi_kp[0-9]` wanted a digit; the path half **drops the digit requirement**, measured first to pull in **exactly two** files (that study + `generate_configs.py`, the campaign's own first stage), both classified in the same pass so the widening left no fresh crop. **The regex is still a floor, not a census** — either textual half can be evaded — recorded in the docstring *and* as `detection_is_a_floor_not_a_census` in the payload, so the manifest carries the caveat with the evidence. `run_but_not_matched_by_the_regex` **examined and found benign** (4 members run by the stage while failing one half; `segment_fragility.py` globs `tokachi_*_historical_*.h5` and asserts nothing the pattern sees) — running something undetected costs coverage, never correctness — and pinned by a test. **The gate is proved by being fired, not asserted to exist**: a classification is removed, both tables are emptied, and a stale key is injected with the pass/FAIL order checked. **723 green** (711 + 12), ruff/black clean, `--dry-run` completes with all 11 stages resumed, enumeration **20 hits / 5 covered / 15 classified / 0 unclassified / 0 stale** (the 20th is the new guard file itself). **`results/production_campaign_manifest.json` deliberately left untouched** (mtime 2026-08-04, verified after the change; see the premise correction above). No `Config` field, default, physics, config YAML, CSV, persisted sweep, Phase 2 posterior, figure content or ADR touched, and no `.tex`/`.bib` anywhere.

- **The conductivity bracket propagated under the BULK grain size too; the two brackets OFFSET rather than compound, and no cell's mechanism ordering survives their union (2026-08-10; note Part 3 of `docs/decisions/conductivity-bracket-annualisation.md`, evidence `…-bulk.json`, figure `docs/figures/conductivity_bracket_both_d70.png`, `docs/phase3_report.md` §12.7, tests `tests/test_conductivity_annualisation.py`).** Closes the **d70 half** of the 2026-08-10 A2 scope; the **prior-side half stands** (no Phase 2 posterior for any conductivity arm under **either** reading). **16 new Phase 1 arm sweeps** (4 sections × 4 arms, N=1e5, bulk, ~37 min) against the persisted bulk production sweeps read-only; the propagation re-swept nothing (19 s, warm cache). **The structural fact, read from `rq4_annual.csv` BEFORE anything ran and it inverts the study: under bulk the production lead is ALREADY overflow at 5 of 8 cells**, so the downward arms can only push piping further behind and **the decisive arm is the UPWARD one** — the mirror image of the matrix run, whose P4 recorded the upward arm reversing nothing anywhere. That is why the "cheap decisive single arm" (`field_geomean`) would have answered nothing here and **all four were run**. **Part 1 pre-registered B1–B9 + C1–C4 + BF1–BF5 before any bulk number existed; four failed.** **B3 FAILED by a hair at the least likely cell**: the upward arm reverses KP 62.0 **historically**, needing ×1.21e4 and delivering **×1.434e4** (share 0.542, a knife edge), while at +4K it needed ×544, delivered ×273 and did not — which is why KP 62.0 +4K is bulk's only ROBUST cell. **BF5 did NOT fire** (the named failure cell KP 58.8 +4K needed ×9.43 and got **×27.8**). **B7, B8 and B9 all failed for ONE mechanism, which is the substantive result: once the grain-size reading demotes piping below overflow, the system number is carried by the conductivity-free Uemura surface curves and every conductivity statistic about the SYSTEM collapses toward the overflow-only value** — the bulk system span is **narrower** than matrix at all 6 cells where both are finite (4.40 vs 185 at KP 58.8 hist), the KP 58.8 climate ratio *falls* 13.62→12.97 = overflow's own ratio to 3 figures, and the control is quieter under bulk (0.0000–0.025 %) than matrix (0.009–1.4 %). The pre-registration reasoned about the **conditional piping curve** while the recorded quantity is the **system** probability; the piping-only span is unbounded at 6 of 8 cells. **C4 held only VACUOUSLY and the truth is stronger**: the invariant set is **empty**, the two zero-overflow cells COLLAPSE rather than survive. **BF3 did not fire on the letter only** (2 unbounded spans break the "every cell" conjunction; all 6 finite cells are narrower) — recorded, not rescored, per Part 2's own F2 precedent. **THE ANSWER: no cell is ROBUST under both readings** (6 of 8 contested under both; KP 60.0 hist robust under matrix collapses under bulk, KP 62.0 +4K robust under bulk reverses under matrix), and **the two brackets OFFSET** — bulk hands the lead to overflow at 5 cells and the upward arm **restores piping at 4 of them**; they are **sub-additive** on the annual system probability; and **subsumption is not symmetric** (matrix: conductivity flips every d70-flipped cell; bulk: 4 of 5). **For RQ3 the claim rests on the UNION and in the union no ordering is invariant — which is NOT "the dominance finding collapses"**: each bracket's direction is known and monotone, production sits at neither end of either (55–77 % of the log conductivity range, matrix the conservative grain size), and the upper arm **strengthens** piping's lead under both readings. **Caveat 8 is 4× larger under bulk**: at KP 62.0 +4K the same 7 of 5400 years above the 50.5 m attainable maximum carry **81.2 %** of the annual piping probability (11.8 % under matrix) with **no coverage flag firing** — so that ROBUST verdict rests on a number four fifths built on unreachable stages. **A qualification on Part 2 found by running this**: `bep_clamped_above_grid` (a *different* flag from the two `coverage` ones §2.5 correctly cleared) fires on the bulk production **baseline** at KP 57.4/58.8 and on **six matrix arm cells**, because a low-conductivity arm drops its own max raw P_f below the ADR-0024 threshold; clamping **understates** piping so a reversal on a clamped arm is too easy to declare. Quantified: the 4 KP 62.0 arm-verdicts are **unclamped** (governing-section claim untouched) and only **KP 60.0 +4K** is genuinely exposed (its sole reversing arm sits ×1.93 behind overflow); Part 2's headline counts are unchanged, and the effect size is **not** quoted because quantifying it means extrapolating above a grid, which ADR-0024 forbids. **Gates:** G1 **228 bulk rows × 20 fields string-identical** to `rq4_annual.csv`; G2 adds a **`d70_interpretation` assertion from the arm's own config** (both readings share one arm directory and differ only in the stem); G3 880 non-BEP cells bit-identical; G4 cache unchanged with the node datum pinned to the matrix curve as the campaign pinned it; G5 nothing written outside the study; **G6 (new) the matrix path is unchanged — every matrix VALUE reproduces exactly by a key-by-key walk, matrix figure byte-identical**, with the gate **relaxed once and both departures named**: one additive key (the clamped-cell list) and one changed string (the scope sentence, which said "no bulk-d70 arm has ever been run" and became false the moment these arms landed; a test now forbids either record claiming it again). **`prior_mean_scenario_companion.py` gained the `--out` the 2026-07-31 audit listed as missing** (default = the tracked ADR-0048 record, so the no-argument call is unchanged); the merge was **verified non-destructive on a copy first** and the 4 matrix sections are byte-identical to HEAD afterwards, 4 → 8 sections. **G7 64 of 64 declared** (the new figure is its own entry by exact filename with a real `--figures-only` path, because it is the cross-reading comparison and staleness in **either** record must redraw it; a test forbids any figure being claimed twice). G6 enumeration **20 hits / 0 unclassified / 0 stale**, its `conductivity_annualisation_study.py` exclusion reason re-checked and widened (32 arm sweeps now, 16 per reading). **736 green** (723 + 13), ruff/black clean, `--dry-run` completes with all 11 stages resumed. No `Config` field, default, physics module, config YAML, CSV, persisted **production** sweep, Phase 2 posterior, `rq4_annual.csv`, ADR **Decision** or existing figure content touched.

- **Canonical hydrograph shape measured end to end; the pinned event is the LONGER of the two approved ones, so every transient number falls and the pre-registered direction was WRONG (2026-08-10; pre-registered note `docs/decisions/canonical-shape-sensitivity.md`, evidence `…json`, driver `scripts/canonical_shape_sensitivity_study.py`, figure `docs/figures/canonical_shape_sensitivity.png`, tests `tests/test_canonical_shape_sensitivity.py`).** Closes defence-brief item A1 ("the largest absence in the work") and discharges the sensitivity Chapter 4 promises; the companion `canonical-shape-invariance.md` supplied the free half (what cannot move) and **every invariance it derived is asserted here as a gate, not re-reported as a finding**. Member swapped **in memory only** (`persist=False`, the `foreshore_width_study.py` pattern): `run.py` hard-codes `canonical_event_ids[0]`, the field is inside `config_hash()`, and `tests/test_configs.py` pins the committed order, so reordering a config is forbidden three ways; committed configs asserted byte-identical after every arm. **THE PREMISE THAT FAILED, and it is the pivot: the recorded "32 h rise" for `HPB_m067_1978` does not reproduce.** Measured with the same `flood_timescales` that produced the production member's published 23 h, the alternate rises in **16 h**, and no onset threshold from 2 % to 50 % of amplitude yields 32 h in either the discharge or the stage domain (13 to 16 h). It is **shorter on every definition-matched measure** — rising limb 16 vs 23 h, 10-to-90 % 14 vs 18 h, plateau 5 vs 10 h, **half-amplitude width 21 vs 55 h**, 1 significant peak vs 2, and fewer hours above *every* shape fraction incl. 0.90/0.95 where the barrier binds. Comment in `generate_configs.py` corrected in place (configs regenerate **byte-identical**; comment-only). The production member's upper-quartile duration is bought entirely by its second episode, whose trough sits at 0.4962 ≈ the half-amplitude threshold, so the double-peak complex counts as one 55 h window — Part 1's "broader crest" channel therefore **does not exist**, and both channels point the same way. **Part 1 (7 predictions, 5 falsifiers, 4 gates, resolution rules) was written and saved before any number existed; its digest covers Part 1 ALONE** (`PART_TWO_MARKER`, newline-normalised) so appending Part 2 cannot invalidate the pin, verified preserved by the append. **P1 REFUTED, P3/P4/P5/P6 refuted in direction, P7 confirmed, P2 split** (duration half confirmed and strengthened; the conjunction fails with P1). **Phase 1, 8/8 strata, N=1e5, both arms: transient P_f FALLS, unanimously — 8 resolved negative, 0 positive** (midpoint ratios 0.579 to 0.755; max |ΔP_f| 0.041 to 0.236; 12 to 29 of 23 to 38 levels resolved by paired bootstrap over shared rows). **GATE 1** every baseline arm bit-identical to its persisted sweep (8/8); **GATE 2** raw static P_f **exactly equal** between arms at every level of every stratum (8/8) — the free one the invariance note earns. **F3 does not fire, decisively**: the midpoint effect is **13× to 72× the Clopper-Pearson half-width**. **F2 does not fire**: sign identical at all eight, nothing section-conditional. **P7 confirmed** — ratios compress to 0.93-0.96 at the matrix grid tops against 0.58-0.66 at the midpoint, and both arms are identically zero at the damped design anchors; the shape acts only where *time*, not head, decides. **THE HEADLINE AT RISK FELL: the peak-only over-rejection factor moves TOWARD one, 2.75 to 3.90 → 1.45 to 1.57** (KP 58.8 2.749→1.448, KP 60.0 3.899→1.568). **Phase 2 deliberately NOT re-run** — the replay drives the observed 2016 record, so the denominator is invariant by construction; it is carried from the committed slice verbatim and the baseline **reproduces the published numerator exactly** at every stratum first. KP 60.0's 2.49× fired falsifier **F4**, was escalated to the author before any prose, and the adopted disposition is **quote it as a measured two-event bracket 1.45 to 3.90 with the conditioning event named in the same sentence**. The two small-number strata (65 and 23 rejected rows) stay in the record and out of the headline; the four strata rejecting nothing under either reading stay **not defined**, never 1.0. Mechanistically clean: the factor measures the exposure the canonical event carries beyond its own peak, so **the SIZE of the over-rejection is a property of the event as much as of the method; the SIGN is a property of the method alone.** **Stage 6.6 driven through `run_comparator_ladder` directly, never the driver** — it has no shape axis and no out-dir and since `1a82d79` correctly REFUSES a mismatched config; that refusal was **respected, not bypassed with `--allow-unverified`**, and nothing was written to `results/stage6_6/` or to any tracked figure. The production side is **read from the persisted analysis** (the published table itself, first checked to be reproduced by its own matrices) rather than recomputed. **GATE 3 passed at both sections**: `C0/C0b/C1/C2/C3a/C3b` and the whole static Shapley lattice bit-identical; Euler flips **0 in both arms at both sections, at N = 1e5**. Auxiliary partition reproduces the invariance note's table **without exception** — resistance-scale toggle at static and at sustained exactly invariant (max change 0), at transient not (0.123/0.156), both `heq_conservatism` and both totals not. **Component vs share reported as different quantities**: head-convention component **unchanged to the last bit** (0.00873 at KP 62.0/46.75, 0.07418 at KP 57.4/39.75) while its share moves 0.549→0.520 and 0.810→0.774; time component 0.00717→0.00805 and 0.01598→0.02023. **The static-to-transient bias RISES, roughly tripling: 13.2→41.0 (KP 62.0 at 46.75, 130/42 rows) and 13.5→32.3 (KP 57.4 at 39.75, 731/306 rows)** — opposite one-sidedness to the peak factor, exactly as constructed. **Neither design-level anchor is quotable at this N and that is a property of the deliverable**: KP 62.0 carries 4/3 failing rows there and KP 57.4 carries 0/0, so a second anchor is reported at the lowest level where **both** arms clear the same k ≥ 30 criterion ADR-0040 pre-registered; resolving the shape effect at the anchor itself needs N=1e6, not attempted. **Phase 3 with the Uemura surface curves REGENERATED on the alternate** (25.5 min wall, against 5-10 declared, but concurrent with 16 production-N sweeps, so contention not cost; committed contract asserted unchanged) — a piping-only swap would have confounded shape with mechanism in the dominance share, since overflow is a duration-above-crest mechanism on the same pinned member. Composition **imported** from `phase3_campaign.py`, baseline reproduces all **228** published matrix/prior/250 m/primary rows, **GATE 4** hazard cache unchanged. Annual system P_f falls at all 8 cells (KP 58.8 hist 8.47e-3→5.58e-3); **ONE ORDERING CHANGES — KP 62.0 +4K, the cell the ADR-0047 adoption left at exactly 0.500, goes to 0.380 and overflow leads**; climate ratios **RISE** (12.64→15.28, 5.27→5.69, 7.58→8.50, 12.70→17.75, prior-side, matching the conductivity companion's baseline) because the historical number falls proportionally more. **So the Stage 6.6 bias headlines are canonical-event-conditional as well as L- and k_aq-conditional**, and of the three k_aq remains the largest. **G6 first live exercise on a brand-new consumer: the gate caught this driver as UNCLASSIFIED, then caught its own test file as a stale key** — both classified (the exclusion is substantive, **not** cost: all three of its baseline gates restate G1, G3 and G4 on the campaign's own artifacts, so running it would re-assert the campaign's claims through its own outputs; ~70 min noted so the author can overrule). **G7 65 of 65 declared**, exact filename, real `--figures-only` path that returns before any evidence write (AST-pinned). Two driver-shape defects found and fixed while building: a Phase 3 gate that filtered `bep_source == "exact"` when the column holds `prior`/`posterior` and so would have **passed vacuously on zero rows** (now asserts exactly 228), and both stages persisting only after their whole loop (now per stratum and per section, the collision this repo has fixed twice). **753 green** (736 + 17), ruff/black clean, `--dry-run` completes with all 11 stages resumed, `--help` inert. No `Config` field, default, physics module, config YAML, CSV, persisted production sweep, Phase 2 posterior, `rq4_annual.csv`, committed surface-curve contract, ADR or existing figure content touched.

- **Panel C of the epistemic-bracket ranking figure replaced with the conductivity stage curve (2026-08-15; commit `c356185`; driver `scripts/thesis_figure_gaps.py`, figure `docs/figures/epistemic_bracket_ranking.png`).** A coherence audit of the thesis found **panel C of `epistemic_bracket_ranking.png` and the right panel of `epistemic_vs_statistical.png` plotting the same quantity** - the largest resolved ratio-of-ratios departure per bracket, over the same four sections, from the same record. The bar panel is the better of the two because it carries the Clopper-Pearson yardstick on the same axis, so panel C is the one that changed. Panel C now takes the bracket that wins the ranking in panels A and B and plots its span **across the whole conditioning grid** - the one claim the thesis makes about that bracket and showed nowhere: it spans orders of magnitude at the low-stage end and collapses toward the unit line as the conditional probability saturates, **so no single factor for it exists**. Every value comes from the committed record (`grid_m_msl`, `P_f_trans_baseline_curve` per section, `P_f_trans_arm` per arm per level, `hwl_m_msl` for the axis ticks); the new `_bracket_span_curve` **reproduces the record's own `ratio_min`/`ratio_max` at all five anchors of all four sections**, which is the provenance gate on the curve, and it keeps `unbounded` (an arm at zero failures) distinct from `not defined` (the baseline at zero failures) exactly as `_span_cell` does. **No decade count is rendered into the figure**: the fall runs from 1.6 orders of magnitude at KP 57.4, whose grid stops before saturation, to 3.9 at KP 60.0, so any single figure would be wrong for two of the four sections. The cancellation numbers are not lost with the panel - they stay in this figure's own table source, in the `cancellation_arm` and `max_resolved_departure_factor` columns, **which is why the CSV is byte-identical** and `_cancellation_by_bracket` and its constants are untouched. **Presentation only: no physics, config, default, prior, evidence file or persisted result changed, so it consumed no ADR number** (conventions section 9.2, and the four figure passes before it). The `FIGURE_DRIVERS` entry needed no change - same driver, same command, same `produces`, and the panel's source JSON was already declared. **747 passed, 7 deselected**; ruff and black clean; the driver's figures command rewrote all six of its figures and **only this one changed content**, every other tracked figure byte-identical; **G7 resolves 65 of 65 declared** with this figure fresh.

- **Architecture and decision records audited and rebuilt as a context file; this log created (2026-08-20; using a pre-restructure backup).** The architecture and decision records had grown to **22,508 words across 170 lines**, of which the "Current state" section was **20,794 words (92.4 %)** in 46 bullets - 39 of them dated history, the longest 1,463 words. It was loaded in full into every session in this repository. **Audit: 16 claim classes verified correct** (commands and CI, all 10 module paths, 32 of 32 API names, the 8 production configs, prior CoVs, the 8 run sidecars, tau_aq and the aquifer verdict, ADR-0040's resolved anchors, Phase 2 rejection fractions, RQ4 ratios under both BEP sources, G7 65 of 65, 7 of 7 reference PDFs) and **12 defects found**. Four were structural falsehoods a session would act on: the ADR range read **0001-0033** against 48 accepted ADRs; **`gamma_p_sub` was claimed threaded from config** when it is not a `Config` field and `run.py` contains zero occurrences, so the file contradicted its own configs section; **`_thesis_*.tex` were said to "sit at the repo root"** though they were retired 2026-07-29 and zero `.tex` files are tracked, contradicting the file's own conventions section; and `theta_repose_rad`/`relative_density` were given as `Config` field names when the fields are `theta_repose_deg`/`relative_density_insitu`. Four were unmarked superseded or under-qualified values, now marked in place in this file: the **peak-shortcut factor 2.75-3.9x** (canonical-event-conditional; two-event bracket 1.45 to 3.90), the **RQ3/RQ4 ordering** at two sites (contested by the conductivity and canonical-event brackets), the **H_eq conservatism normalisation** (1 to 39 % against C4b as computed; **1.0 to 63.2 % against the end-factor-1.0 baseline C4c**), and the **Stage 6.6 section 10.2 attainability caveat** beside the bare "+0.61". Four were precision defects: `margin_vs_threshold` described as a per-run stamp when it is a campaign-manifest field, the test tally **"753 green"** against a suite that collects **754** and was unchanged since that bullet's own commit, the "Phase 1 of a thesis project" framing against three built packages, and "bulk <= 0.02 %" against a measured 0.023 %. **Two of the brief's own premises did not reproduce, and the corrections were accepted**: the head-convention share had **already** been corrected to 75-97 % on 2026-08-06 (commit `263bf85`), and the H_eq range was numerically right and needed only its denominator named. **Rebuild:** the 39 dated bullets moved here verbatim; architecture and decision records were re-triaged against the repository's context-management rules: keep the context file lightweight, retain non-obvious codebase gotchas, and keep it under 200 lines so that binding constraints remain prominent. Every retained line was tested by whether a new contributor, acting sensibly and without it, could make an expensive mistake. **17 of the 18 binding constraints survive unaltered**; the eighteenth, the coding-conventions block, was split - naming, type hints and docstring style cut, since a new contributor can follow surrounding code, and **SI base units at kernel boundaries, deterministic RNG seeds and the irreducibly serial time axis kept**, the first being `architecture.md`'s own Failure Mode 1 and a silent factor-of-86400. The five-number prior-CoV enumeration was cut to the rule, since `tests/test_configs.py` catches drift in seconds; the C_e exception stayed because 0.782 reads like a typo. Two binding constraints that were **buried in the dated history** were promoted into the invariants section: the **225 s integration timestep** and the **frozen scalar `evaluate_realization`** Phase 2 API. The three conditionalities on the Stage 6.6 bias headline - seepage length, aquifer conductivity, canonical event - were **consolidated into one statement**, having been scattered across four bullets with no single place naming all three. **The nine supporting local files that instruct appending a bullet to architecture and decision records were repointed here**, so the file cannot silently regrow. Nothing mechanical depended on architecture and decision records: no test, script, config or manifest reads it, and the four tracked mentions are prose in comments. **No `Config` default, physics module, config YAML, CSV, persisted sweep, Phase 2 posterior, ADR, report of record or figure was touched**, and no `.tex`/`.bib` anywhere.

- **Two tracked Stage 6.6 evidence records were pre-adoption copies carrying the retired "about 21"; replaced from the live artifacts and pinned against them (2026-08-20; no ADR - no default, physics, config or persisted result changed; new guard `tests/test_stage6_6_committed_evidence.py`).** `docs/decisions/adr0040-stage6-6-kp62_0-analysis.json` and `-duration-ladder.json` were still the **pre-adoption L = 47.0 m** run. Measured leaf by leaf against the live `results/stage6_6/` artifacts with the campaign's own `VOLATILE_JSON_KEYS` excluded and NaN treated as equal to NaN: **3,093 differing leaves in the analysis record and 60 in the ladder**, against **0 in both KP 57.4 twins** - the control that makes the cause ADR-0047's section-specific adoption and not a general drift. At the 46.39 m design HWL the tracked copy read C0 2.1e-4 / C1 4e-5 / C4b 1e-5 for a ratio of **21.0**, the live one 1.79e-3 / 4.8e-4 / 4e-5 for **44.75** (x8.52 / x12 / x4). **The thesis matches the live artifact, not the tracked one**: Chapter 6 `tab: gap components` reproduces live on all five KP 62.0 rows for C0, C4b and the head-convention share (46.39 m: 1.8e-3 / 4e-5 / 0.75 against the tracked 2.1e-4 / 1e-5 / 0.85), and its "4 transient failures" is the live count where the tracked record implies **1** - the single failing realization the retired figure rested on. **Provenance:** added by `05821aa` on 2026-07-17, one commit, never touched since; ADR-0047 adopted 40.0 m on 2026-07-29 (`e1e7789`); `263bf85` (2026-08-06) carried the post-adoption figures into four prose documents **but not into these copies**, so this is that pass's unfinished remainder. **Remedy (a), replace, chosen over (b), mark superseded in place**, on four grounds: the tracked file's headline is a number `docs/stage6_6_report.md` section 9 explicitly forbids quoting, and a note at the top of a 5,458-line array file does not stop an examiner who searches; the report's own line 11 ("every number below traces to those files") was **already false** for section 10 and (b) would leave it so; **nothing is destroyed**, because the pre-adoption numbers survive substantively identical at `results/superseded_adr0047_L47/stage6_6/`, retained until the defence by `docs/conventions.md` section 10.4 precisely because a question about the adoption is foreseeable, and in git at `05821aa`; and ADR-0047's own principle is **adopt where WRONG, hold where merely OLD** - 47 m credited a berm 28 of 28 clean DEM stations do not show. **G7 cost was identical either way** (any in-file supersession marker bumps the same mtime), so it did not discriminate. Each replaced record now carries a `provenance` block naming its source path, gitignored flag and SHA-256, stating that no sweep, ladder or study was re-run, and **pointing at the resolved figure** - B = 26.9, 95 % CI [21.6, 35.3], at 46.39 m on 63 rows - because the post-adoption 44.75 is *also* an N = 1e5 count section 9 tells the reader not to quote, so swapping one for the other is not on its own sufficient. **The summary was deliberately NOT replaced**: every leaf it shares with the live record is identical (both sections' `bit_identical` status, `levels_checked` 38/23, `theta_identical`, all five `flip_totals` 0), so it carries no superseded number; its two extra keys are true records of the 2026-07-17 run that the live driver no longer emits (`figures` only on a rendering run, `hash_note` only on a config-hash mismatch that no longer fires now both configs are regenerated), and deleting them would destroy true information - so it gained only a `provenance` block scoping the note to its run, since **benign is not the same as correct**. **The structural half matters more than the swap**: no driver writes `docs/decisions/`, so these five files were hand-placed and could only be kept current by hand, which is `docs/conventions.md` section 9.3's figure failure one level over. The new guard content-compares all four number-bearing copies against their live artifacts (skipping on the gitignored path, asserting on every tracked one), pins the provenance shape and the resolved-bias pointer, and pins the ground the summary was left on; **negative control**: re-injecting the pre-adoption 2.1e-4 fails it naming `p_f.C0[9]`. `docs/stage6_6_report.md`'s evidence paragraph gained a dated amendment stating the consequence plainly - **the committed copies now trace sections 8 to 10, not sections 1 to 7**, whose pre-adoption run is retained under `superseded_adr0047_L47/`. **No number the thesis or any report quotes moved**: `attainable_max_m` stays 50.5 and is the only field `scripts/thesis_figure_gaps.py` reads, so **all six G7-coupled figures re-rendered byte-identical** (SHA-256 unchanged on every one) and the six CSVs the same driver rewrote were content-identical line-ending churn, restored from git. **756 passed, 7 deselected** (747 baseline + 9 new); ruff and black clean; `--dry-run` completes with all 11 stages resumed; **G7 0 stale of 64 checked**, 1 waived, 65 declared. Neither file's SHA-256 is in `results/production_campaign_manifest.json`, which was not touched.

- **Hazard-sampling intervals on every Phase 3 annualised number (2026-08-20; companion `docs/decisions/annualisation-hazard-sampling-uncertainty.md` + `.json`, driver `scripts/annualisation_uncertainty_study.py`, guard `tests/test_annualisation_uncertainty.py`; no ADR, no `Config` field, no default and no persisted production result changed).** Chapter 7 conceded in prose that no sampling uncertainty was attached to any annual probability or climate ratio, then quoted ratios to three significant figures, the KP 62.0 mechanism split to three decimals and a 43-fold margin at KP 58.8 without one. **Verified first, and all four premises reproduced**: `AnnualizedResult` carries no uncertainty field of any kind and no script computes one (the only CIs in Phase 3 are the Clopper-Pearson bands on the *conditional* Phase 1 curve, loaded into `bep_input.py` and then never read again -- `compose()` and `annualize()` consume `p_f` and `p_sys` alone, so the conditional uncertainty is dropped at the composition boundary); the KP 57.4 long-duration stratum does rest on 3 historical years; the KP 62.0 warming split is 0.500336/0.499664 at a production margin of **1.00135**. **The ensemble is nested and that decided the estimator**: the 3,000 historical events are 50 members x 60 calendar years and the 5,400 warming events are 6 SST patterns x 15 members x 60 years, perfectly balanced, so an i.i.d. bootstrap over years is not a resample of the ensemble's independent units -- the estimator is a **block bootstrap over ensemble members**, R = 10,000, seed 20260820, two-sided 95 % percentile, fragility curves held fixed. All 114 nodes carry the identical event sequence, so **one block draw per scenario serves every node** and each between-section difference is paired on all 10,000 replicates; the two scenarios are drawn independently and the climate ratio is formed inside each replicate, never as a quotient of marginal intervals. **Gate 1 reproduces `rq4_annual.csv` string-identically over 912 rows x 20 fields** (all four 250 m / primary arms, 228 each), gate 0 requires each per-event matrix to average to its `AnnualizedResult` field with **no tolerance**, and the hazard cache and the whole Phase 3 output directory are asserted byte-unchanged. Headline, matrix/posterior: annual system P_f half-widths **29 to 58 % historically and 11 to 21 % under warming**, so **every absolute annual value in Chapter 7 is quoted to about one significant figure of sampling precision**; climate ratios 12.7 [7.3, 28.1] / 5.51 [4.13, 7.73] / 7.87 [5.34, 12.9] / 12.7 [7.7, 24.8]; KP 58.8 dominance margin **43.0 [33.9, 57.9] prior side, 37.6 [30.2, 49.4] posterior side** -- the thesis's "43-fold" is the prior-side number, which is right in its context because it hands off to the prior-side conductivity companion. **Pre-registered outcomes: Q1 PARTIAL, 5 of 6 pairs resolve -- the one that does not is KP 57.4 against KP 62.0, exactly the two the thesis prints as identical 12.7s, so their equality is an absence of resolution and not a property of the sections. Q2 TIE: the KP 62.0 warming split differs by +1.1e-5/yr inside [-9.2e-4, +9.1e-4] and the share is 0.500 [0.476, 0.532], so the three-decimal quotation is unsupported -- an independent and stronger second reason for the knife-edge reading the chapter already gives on canonical-event grounds. Q3 the lead resolves at all four sections, but two of them (KP 57.4, KP 60.0) only because the overflow branch is never loaded at all, which is coverage and not measurement, and neither measured share supports its printed precision: KP 58.8 0.974 [0.968, 0.980] and KP 62.0 0.812 [0.690, 0.980], so the "81" at the bottom of the "81 to 100 per cent" range is the one Chapter 7 headline needing rewording rather than annotating.** Sensitivity on the resampling unit, reported and never the headline: the member block is within about 10 % of the naive i.i.d.-over-events width in both directions (measured intra-cluster correlation 0.00 to 0.005), so **the nesting is worth almost nothing on this estimand and the member block is used because it is valid, not because it is wider**; the +4K **SST-pattern** grouping, by contrast, widens the interval **1.6 to 2.4x** (KP 58.8 25.0 % against 10.6 %) and is declared in advance as structural climate-model spread rather than sampling noise, since six patterns are a design and not a sample. Scope, in the record, the note, the figure and the handoff: **hazard only, fragility fixed, not the total uncertainty, and dwarfed by the k_aq bracket** (which moves the same KP 58.8 piping contribution by ~1.8e4). Deliverables: the RQ4 headline figure `phase3_rq4_four_sections.png` **amended rather than duplicated** (page budget is closed) with error bars on both panels and the scope rendered into the PNG, mirrored byte-identical to `msc-thesis/figures/`; `phase3_figures.py` now **refuses** to draw if the record is missing or its point estimates have drifted from the live table (conventions section 9.4, and a `csv`-module re-read because `pandas.read_csv` does not round-trip a float); the record added to that driver's `FIGURE_DRIVERS` sources; the study wired as a **campaign companion classified on substance** -- it consumes only artifacts the campaign itself produces, so none of the missing-gitignored-arm grounds excluding the other Phase 3 companions applies, ~45 s, and its fresh record compares identical to the committed one (a new `writes` volatile key carries the `--out` paths that would otherwise differ). Thesis handoff at `msc-thesis/scratch/BOOTSTRAP_HANDOFF.md`. **790 passed** (762 baseline + 28 new), ruff and black clean, G6 zero unclassified and zero stale exclusions, G7 0 stale of 64 checked and 0 undeclared.

- **The stratified RQ4 attribution entries given intervals behind a pre-registered member-block occupancy floor; the "151 to 378" headline re-based on the well-populated pair (2026-08-20; same companion `docs/decisions/annualisation-hazard-sampling-uncertainty.md` sections 3 and 4 + the `stratified_attribution`, `Q4`, `Q5`, `Q4_compound` and `Q6_floor_sensitivity` blocks of its `.json`; pre-registration committed in `aeeb918` before the driver carried a line of stratified code; no ADR, nothing in part one moved).** The part-one session had refused the stratified table an interval on the grounds that the KP 57.4 long-duration stratum rests on three simulated years. **That reasoning is right for KP 57.4 and wrong applied to the other seven cells**, and the counts that settle it are in the study's own resampling unit rather than in years: the historical long-duration stratum carries **152 years in 46 of the 50 ensemble members at KP 58.8 and 105 in 43 at KP 60.0, against 19 in 14 at KP 62.0 and 3 in 3 at KP 57.4** (under warming 727/90, 531/88, 186/72, 42/33 of 90; the compound stratum historically 35/23, 27/20, 10/9, 5/5). KP 58.8 and KP 60.0 are exactly the pair Chapter 7 already calls *"the two sections where the historical loading is frequent enough for the stratification to be well populated"* and which carries its 89 and 93 per cent claim, so the chapter's qualitative judgement and the measured block occupancy agree. **The floor, fixed before anything was computed: at least 20 carrying member blocks (F1) and no single block above 20 % of the stratum (F2).** Twenty is where the probability of a replicate containing none of the carrying blocks, about `e^-m`, reaches 2.1e-9 so that **no replicate is ever discarded** (an interval taken after discarding is silently conditioned on the stratum being non-empty), and simultaneously where one block's leverage `1/m` reaches the project's standing 5 % Monte Carlo tolerance. Below the floor: the year count, the carrying-member count, the failing criterion and the point estimate labelled count-limited, with **no interval keys in the record at all** and no resolution verdict. **The floor is explicitly not blind** -- the counts were measured first, as section 0 preceded section 1 in part one -- so the defence is that its justification names no section and that Q6 pre-registered the verdict's sensitivity to the floor value itself. **Outcomes. Q4 historical RANGE SUPPORTED**: two cells clear, 153 [98, 252] at KP 58.8 and 378 [141, 1358] at KP 60.0, paired difference **-225 [-1131, -9.3]**, so the two endpoints are resolvably different and the spread between them is a measured range; KP 57.4's 151 (3 years in 3 members, failing F1 and F2) and KP 62.0's 221 (19 in 14, failing F1) are named beside it with their counts and never folded into it. **Q4 +4K**: all four clear, range 35 to 72 with the endpoint pair resolving, but the three factors 54/64/72 at KP 57.4, KP 58.8 and KP 60.0 are not distinguishable from one another, so only "KP 62.0 is the lowest" survives as an ordering claim. **Rule 3 fails at all eight cells: not one concentration factor supports the precision the table prints**, relative half-widths running 24 to 161 %. **Q5 historical COLLAPSED, and this is the most directly usable correction: the "89 and 93 per cent" pair is one number.** The two shares are 0.891 [0.835, 0.931] and 0.932 [0.832, 0.980] and their paired difference is **-0.041 [-0.101, +0.043]**; the same pair fails to resolve under warming too (+0.022 [-0.005, +0.052]), so it is a property of the pair and not a historical accident. **Q6: the headline is floor-independent.** The historical duration range is 153 to 378 at floors of 10, 20 and 30 alike, because KP 62.0's 221 lies *inside* it rather than outside -- which is the strongest available answer to "you tuned the floor after seeing the counts", and also the reason the arithmetic correction is nearly free (151 becomes about 150). **The compound statement is by contrast genuinely floor-sensitive**: at 30 blocks no historical compound cell clears at all. **Applied to the compound rows as well, since the floor was fixed for the whole table**: the "3.7 to 91 historically" range has its *upper* endpoint at the 5-year, 5-member KP 57.4 cell, the two clearing cells 3.7 [0.3, 9.3] and 6.5 [0.1, 18.4] do not resolve from one another, and **neither interval excludes 1** -- historically the compound stratification cannot be distinguished from no stratification at all at either well-populated section, which *strengthens* the chapter's "duration is the sharper discriminator" argument rather than weakening it. New gates: **gate 4** reproduces `rq4_attribution.json` over all 8 cells and every field by float equality with no tolerance, through the production `stratified_annual_p_f`, with a 4a half asserting the unresampled block estimator against those conditional means at a **1e-12 bound, worst measured deviation 2.57e-16** (bit-identity is not asserted there and is not achievable: the block-grouped sum reorders the addends `np.mean` adds pairwise); **gate 5** asserts the random stream's state is unchanged across the stratified pass, and every part-one key came out **byte-for-byte identical** to the committed record. Gates 0 to 3 re-asserted unchanged at 912 rows, cache and Phase 3 directory byte-unchanged, and **zero of the 10,000 replicates left any reported stratum empty**. F2 fired once, at KP 57.4 historical duration (33.3 % of a three-event stratum), where F1 had already failed, so it **excluded no cell F1 admitted**. Neither the amended RQ4 headline figure nor `phase3_rq4_attribution.png` carries any of this, and the handoff says why: the first depicts annual probabilities and climate ratios rather than the stratification, and on the second a missing error bar would read as a small one, so the count-limited marking belongs in the table note. Recommended thesis wording for all eleven occurrences of the range, plus the share and compound rewordings, appended to `msc-thesis/scratch/BOOTSTRAP_HANDOFF.md` under "Part two". 803 passed, ruff and black clean.

- **The two un-numbered Phase 3 companions and the annualisation estimator given thesis-side records (2026-08-21; the change is entirely in `msc-thesis`, nothing in this repository's artifacts, defaults or persisted results moved).** An external review of the report raised four defects in the traceability of the 2026-08-20 hazard-sampling study, and **all four reproduced against the files**. (1) The block-bootstrap estimator behind every "95 % flood-ensemble sampling interval" in Chapter 7 existed nowhere in the Methodology: section 4.8.2 defined the annualisation mean and stopped, and the method lived only in Chapter 7's running prose and two table captions, with the Summary and Chapter 9 quoting intervals with no upstream method at all. (2) It had no supporting record in Appendix G, although that appendix carries one for the timestep stress ladder, the convergence study, the GSA design and the non-cancellation mechanism. (3) Appendix C's study register held exactly five rows (L, Bias, Bracket, Shape, k_aq) and was missing `annualisation-hazard-sampling-uncertainty.md` and `r10-foreshore-exhaustion-screening.md`; the second is cited in Chapter 8, carries a figure there and has its own Appendix G subsection, so its absence was a traceability gap rather than an omission. (4) The Chapter 8 limitations register still read *"the shares are point values carrying no interval"* for the surface-curve parameter row, which the part-one outcome had made false: Chapter 7 now prints share intervals of 0.69 to 0.98 and 0.48 to 0.53 at KP 62.0. **Closed thesis-side**: a methodology passage in 4.8.2 (mean over simulated years; the member block as resampling unit because the years are nested 60 to a member; the ratio formed inside the replicate; the curves held fixed, so hazard-only and dwarfed by the k_aq bracket; and the occupancy floor withholding an interval below 20 carrying members), a new Appendix G.2.4 carrying the design and R = 10,000, both floor criteria with the reason for each threshold, the measured intra-cluster correlation 0.00 to 0.005 and the **valid-not-wider** reading it forces, the SST set as structural spread with its 1.6 to 2.4 factor, and the six gates; two new Appendix C rows; and the Chapter 8 row reworded with *Quantified: sampling interval only*. Cross-referenced from 4.8.2 and from both first uses in Chapter 7. Verified after: 0 dangling refs and 0 duplicate labels over 358 labels and 774 ref sites, em-dash and Japanese-script checks CLEAN, and no new row needed in Chapter 7's standing-conditions register (a sampling interval is reported *on* a number, not a bracket *around* one, and the only genuinely conditional part, the six-pattern SST design, already has its own row). **Flagged, not edited:** section 4.1 of `r10-foreshore-exhaustion-screening.md` reads "(0.72 -> 27.3 m/h at the design HWL)" where 27.3 is the KP 60.0 **2016-event** value; that note's own table gives the design-HWL span as 0.72 to 9.77 m/h (13.6x), and 0.72 to 27.3 (38x) is the span across both forcings. The thesis quotes "roughly an order and a half of magnitude", which matches the across-forcings figure, so no thesis number moved; the parenthesis in the note is the slip.

- **The mis-scoped critical-rate parenthesis in the foreshore note corrected in place, and the thesis's two names for the resampling unit anchored to one sentence (2026-08-21; `docs/decisions/r10-foreshore-exhaustion-screening.md` section 4.1, plus a thesis-side edit in `msc-thesis`).** Both were flagged rather than fixed on 2026-08-21 and are now closed at the author's instruction. **(1)** Section 4.1 read *"~38x across the four sections (0.72 -> 27.3 m/h at the design HWL)"*. The ~38x is right and is what the thesis rests on, but the parenthesis conflated two forcings: **27.3 m/h is the KP 60.0 2016-event entry of that section's own table**, and the design-HWL rows of the same table run **0.72 to 9.77 m/h, a factor of 13.6**, which is the width contrast itself. Corrected in place with a bracketed dated marker quoting the old wording, per this file's own no-silent-deletion rule. **No computed value moved and no artifact changed**: the evidence JSON never carried the conflation, its `sanity_anchor` holding the separations apart at 10.54 (`event_2016`) and 13.62 (`design_hwl`), and the thesis quotes the across-forcings figure as "roughly an order and a half of magnitude". **(2)** Chapter 4 introduced the bootstrap unit as the "ensemble member" while Chapter 7 and the new Appendix G.2.4 called it the "member block", with nothing tying the two names together. Fixed at the point of introduction rather than by re-wording Chapter 7: section 4.8.2 now reads *"Each member is therefore resampled entire, as one block"* and then uses "member blocks" for the occupancy floor, matching Chapter 7's "occupancy floor of 20 member blocks" verbatim while leaving its "3 of the 50 ensemble members" counts, which name the physical object, untouched. **Still flagged, not fixed, and it needs a decision from the author:** Chapter 8 and the caption of `fig: foreshore exhaustion` both assert *"Every critical rate lies inside the assumed bracket"* without qualification, and Appendix G's own results paragraph prints 27~m/h at KP 60.0 under the 2016 event against a bracket of 0.1 to 10 m/h. The claim is exactly true at the design water levels, where all four critical rates are 0.72 to 9.77 m/h, and false for that one 2016-event cell; the note's own hedge, *"sits inside or at the edge"*, does not cover a value 2.7x above the top of the range either. The repair is one clause, "at the design water levels", in two places, and the thesis's surgical-edit rule reserves that call for the author.

- **The aquifer-conductivity bracket measured on the POSTERIOR side of the 2016 survival update (2026-08-21; pre-registered `4bec61a` before the first replay; companion note `docs/decisions/conductivity-bracket-posterior-side.md`, evidence `.json` + `-bulk.json`, drivers `scripts/conductivity_posterior_replay.py` and `scripts/conductivity_annualisation_study.py --side posterior`; no default, no config axis, no physics, no persisted production artifact changed).** The prior-side record of 2026-08-10 closed the d70 half of its scope and left the other half open in writing: *"No Phase 2 posterior exists for any conductivity arm, and building one is out of scope."* The thesis carried that as an open limitation in four places. It is now closed. **Phase 1 was not re-run**: the 32 persisted ADR-0048 arm sweeps were consumed read-only and pushed through 32 Phase 2 Accept-Reject replays (46.5 min per reading) and two Phase 3 passes. **The answer: the survival constraint NARROWS the bracket, never widens it, and leaves every mechanism ordering exactly where the prior side left it.** Matrix spans go 184.8 → 94.14 at KP 58.8 historical (**÷1.96**) and 4.437e5 → 1.581e5 at KP 60.0 historical (**÷2.81**), 48.59 → 32.58 and 2762 → 1399 under warming; KP 57.4 and KP 62.0 move 3.6 % and 0.02 %. Under bulk the movement is 1 % or less at five of six resolved cells and 35 % at the sixth, and the reason is **structural, not statistical**: that reading has already handed the lead to overflow at five of eight cells and overflow carries no conductivity dependence, so the span it dominates cannot follow the piping arm down. **The mechanism is the rejection ladder, and it is monotone in `k_aq` at all eight section-and-reading combinations**: matrix regional-upper rejects 3.951 / 65.530 / 86.881 / 0.010 % against a production 0.065 / 5.673 / 3.363 / 0.000 %, i.e. **11.6× and 25.8× more at the two middle sections**, so the narrowing is asymmetric exactly as predicted — the upper arm loses 49 % (KP 58.8) and 64 % (KP 60.0) of its annual number while **no downward arm moves by more than 2.8 % anywhere**. **All sixteen ordering verdicts are reproduced** (4 sections × 2 climates × 2 readings), because the two effects are decoupled: under matrix every reversal is driven by a downward arm, which the update cannot touch, and under bulk every reversal is driven by the upward arm, which it can, but there overflow carries the number. **Five of eight predictions held per reading; no falsifier fired.** P5 failed **on its magnitude clause only** — direction held at all 13 resolved cells, but KP 60.0 historical narrows by 2.81 against a predicted bound of two, because at 86.9 % rejection the arm has almost no prior left to carry the integral. P7 failed at the two upward-arm cells (+42 % KP 60.0, +32 % KP 58.8) while **production's own climate ratios move ≤ 4.6 %**, so no headline ratio is disturbed. P2/P3 failed under bulk only, both at KP 62.0 where nothing rejects; **P3 as worded is a matrix prediction and HELD there** (0.010 % against an exactly-zero baseline), so KP 62.0 stops being a bit-copy of its prior side for the first time. **F4, the pre-registered deflating outcome, did not fire**: the largest span movement is 64 %, so this is a real narrowing and not a null. **What it does not license:** after the update the span is still 94 and 1.6e5, so the constraint takes a factor of about two off a bracket spanning two to five orders of magnitude — it changes the number, not the character of the limitation. **Two riders.** (1) **GATE 2 was amended in the open before the campaign ran** (note Part 1a): `trace_breach_times` is exempted, because it re-runs the scalar M8 with trajectory storage once per *rejected* row, which is exactly what a high-conductivity arm inflates — measured at KP 58.8 matrix regional-upper, **5902.2 s on against 65.5 s off, a factor of 90**, and 32 of those is two days. It cannot reach the result (`state.alive` is fixed before the tracing block), and that was **proved by measurement on the worst-case arm**: every array the annualisation consumes, plus the rejection fraction and accepted-row count, bit-identical. (2) At KP 58.8 and KP 60.0 the upward arm keeps 34 470 and 13 119 rows, **below the 50 % headroom floor**, so those two posterior curves have degraded tail resolution; the direction is unaffected (the arm can only fall further) but the two most-narrowed cells are the two carrying the caveat. **Both prior-side records had overtaken their own scope sentence for the second time** ("no Phase 2 posterior exists...") and were regenerated: three lines changed each, timestamp, elapsed and scope, with **not one measured number moving** — which is also GATE 7's proof that the pipeline has not drifted since 2026-08-10. 812 tests green, ruff and black clean.
* **2026-08-22 — ADR-0050 accepted: the as-if-undrained caveat at KP 58.8 and KP 60.0 becomes a measured bracket, and the ranking's second place does not survive it.** The thesis's most exposed interpretive claim was that its top two ranked sections are the two whose installed toe drainage the engine does not represent. Chapter 8 named the route and this closes it. **The mapping, from PWRI 2014 Table 7.1.1 (printed p. 33), re-read from the source PDF and not only from provenance §6.3:** the berm is 断面拡大工法, "lengthen the seepage path", engine handle `geometry.L`; the drain is ドレーン工法, "reduce the hydraulic gradient at the landside toe", engine handle `i_exit` in the M5 heave limit state. **The berm magnitude is MEASURED and was sitting unspent**: ADR-0047's 2025 lidar read 42.0 and 43.0 m against 35.0 and 34.8 m and *held* both, on the explicit ground that crediting the longer path while the engine models no drain imports only the anti-conservative half of the works — this study is the one configuration in which that objection lapses, because both halves move together. **The drain magnitude is NOT grounded and was NOT invented**: PWRI's 0.3 design gradient sizes the drain *body*, the guidance states no equivalence to the foundation blanket exit gradient, and provenance §7.3 warns that *a `drained` label identifies a physical feature, not a design intent*. The relief is therefore a **swept** axis whose response curve is the deliverable; the 0.3 figure is recorded as a sourced observation and pinned by test never to become an arm. **Knob (ADR-0050):** keyword-only `toe_gradient_relief_factor` in (0, 1] on all three M8 entry points, a `Config` field **dropped from `to_metadata()` when None**, threaded by `run.py` and by the Phase 2 replay. `EvaluationResult`/`BatchDiagnostics` field sets untouched (ADR-0011); the reported `r_e` stays the *unrelieved physical* response factor, because r_e is a blanket-aquifer property and the relief is a credit for a structure. **All eight production config hashes byte-identical.** **Gate-only by construction and measured to be so: static invariance exactly 0.0** at every level, arm and case — the ADR-0028 separation confirmed a third time after `gamma_bl_sub` and `l_c`. **Results (24 full-N Phase 1 sweeps, 24 Phase 2 replays, 2 Phase 3 passes; all four Phase 1 gates bit-identical, Phase 3 gate 1 reproduced all 228 published rows field for field, gate 2 held all 1120 untouched cells).** Conditional at the design level: KP58.8 **0.2627 → 0.1084** on the measured berm alone **→ 0** at 80 % relief; KP60.0 **0.3143 → 0.1111 → 0**. The relief **shifts the curve right, it does not scale it down** (P4): lowest initiating stage 39.75 → **42.00** m and 41.25 → **43.50** m, both *above* the design level. Annual system P_f historical: KP58.8 **7.42e-3 → 4.25e-3 → 1.97e-4**, KP60.0 **1.80e-3 → 6.40e-4 → 0** (the strongest arm's transition is unbracketed, so by ADR-0024 those are **lower bounds, not estimates**). 2016 rejection **5.673 % → 1.551 %** and **3.363 % → 0.555 %** on the measured berm alone, putting a number on the direction Chapters 6/8/9 state qualitatively; **marginal transient rejection stays exactly 0.000 in all 24 replays**, so the nesting survives. **The climate ratio RISES with credited drainage** (5.51 → 14.22, 7.87 → 26.01): the as-if-undrained treatment *understates* warming sensitivity there, which is opposite to how the caveat is usually read. **THE RANKING: KP58.8 keeps the top under every arm but the strongest, in both climates; KP60.0 leaves second place for LAST under every arm, including the measured-berm-only arm that assumes nothing whatever about the drain.** At the strongest arm the unremediated KP62.0 leads and the BEP share at KP58.8 falls 0.974 → 0.009 with overflow leading (P7 confirmed: "BEP dominates 3 of 4" is itself as-if-undrained-conditional at two of those three). **Two predictions did not survive, both reported rather than smoothed.** **P2 (exact one-sidedness) FALSIFIED**: 1 row in 1e5 at KP58.8 bulk fails under stronger relief; the Δt ladder gives `Z = 0.00000` at 225 s and no inversion at 112.5 or 56.25 s, so it is an **ADR-0030 Euler barrier jump** at C_e = 0.314 / k_aq = 6.6e-3. The mechanism is specific to this axis — **relief DELAYS the gate, so the first active step meets a higher head and is larger** — making a gradient-relief axis a *more sensitive probe of the discretisation limit than production is* (artifact count grows 1/5/6/17 with relief strength, all at the one degenerate case). The gate was made **stronger, not looser**: every violation is re-integrated at 112.5 and 56.25 s and the driver refuses if one *survives* (29 violations, 29 artifacts, 0 survivors); at Phase 3 the annualised image (+1.2e-08 on 2.57e-03) is bounded by a **derived** `n_artifact/N` ceiling, not a chosen tolerance. **P5 REFUTED**: there is no single berm-versus-gate verdict, because the response is strongly non-linear in the relief fraction (×2.42 from the berm, ×1.03 from the first 20 % of relief, ×41 at 60 %) — the strongest argument for having swept the axis instead of choosing a value, since any invented drain performance would have landed on one arbitrary point of that curve. **A latent defect found in passing, not mine and affecting three ADRs:** `fragility_update.verify_posterior_fragility_by_reevaluation` forwarded **none** of the optional M8 keywords (ADR-0045 m_p, ADR-0049 l_c, ADR-0050 relief), so it re-evaluated a different model from the one that wrote the matrices and reported the parent unverifiable — transient flag mismatches scaling 2 481 → 1 008 624 with relief strength while static stayed exactly 0. Invisible in production because all three default to absent, which is why it survived. Fixed for all three; `tests/test_phase2_verification_threading.py` verified to fail on 3 of its 4 arms without the fix. Companion `adr0050-drained-configuration-bracket.md` + three evidence JSONs; 31 tests added; 899 green; ruff and black clean. **No production default, CSV cell, committed config, persisted sweep or headline of record changed.**

* **2026-08-21 — ADR-0049 accepted: the critical pipe length gets an override and a measurement, closing the last unquantified half of the equilibrium curve.** Pol SIE 2024 Eq. (11) has two anchors and only one of them had a hook: the `0.9·H_c` end value was named a gap component (ADR-0009), wired as an opt-in override (ADR-0041) and measured at field scale (Stage 6.6), while the critical pipe length `l_c` of Eq. (13) had neither. The thesis register of limitations said so in as many words. **Knob (ADR-0049):** keyword-only `critical_length_factor` on `compute_critical_pipe_length` and both M6 entry points, on `evaluate_realization`/`evaluate_batch`/`evaluate_batch_diagnostics` (the ADR-0045 precedent — the frozen `EvaluationResult` **field set** is untouched), plus a `Config.critical_length_factor` **dropped from `to_metadata()` when None**, threaded by `run.py` and by the Phase 2 replay in both its calls. Default None is bit-identical at every layer; **all committed configs and all 112 persisted sidecars rehash unchanged**. Scaling happens in M6, upstream of the timestepper, with two deliberate consequences: the reported `l_c` diagnostic can never disagree with the curve M7 builds, and unlike ADR-0041's end factor the knob is **not numba-refused** (the JIT kernel takes `l_c` as an input array). **Bracket ×0.643 to ×1.556**, the upper arm derived rather than chosen: DgFlow's 3D hole-exit critical length 1.36 m for the in-domain S2-2 case over Eq. (13)'s 0.874 m at the same geometry (Pol 2022 Fig. 5.9 caption, already recorded in `m7-pol-ode-reference-values.md` §2 as a caution against cross-validation — the caution stands, and a band is what it supports). The lower arm is a mirrored counterfactual and is labelled as one: **both empirical anchors sit above the formula**, the second being B25-245's measured 0.197 m against 0.0905 m, a factor 2.18, out of domain and used as a direction check only. **Result (`adr0049-critical-length-bracket.md`, 12 full-N sweeps, four matrix sections, five anchors):** all four bit-identity gates passed. Transient `P_f` span **1.00 to 2.08**, widest at KP62.0's design HWL on 15 failing rows, collapsing to 1.00 at every grid top. **Direction: a LONGER `l_c` RAISES the transient probability**, which is not the intuitive sign — total traverse time is `L·B + l_c·(A − B)` in the two branches' reduced coordinates, and every overload entering `B` sits at the bottom of the range entering `A`, so `B > A` and the descending branch is the slow part of the journey; a longer critical length buys a shorter one. The knob is **small** for the same reason it is calculable: `l_c/L` is 0.206/0.219/0.241/0.236 in the mean, so four fifths of the traverse is on a branch the bracket cannot move. **It does NOT cancel: ZERO common-mode channels.** `l_c` is absent from Eq. (12), from the leakage lengths, from `r_e`, and from the static comparator, and the driver **refuses to write a record if a single static cell moves** — none did, anywhere. So the ratio-of-ratios departure is not merely non-unity, it is *identically* the reciprocal of the transient displacement, **verified to 2.2e-16 over all 89 evaluated levels**. Max resolved departures ×1.11–1.23 (lower) and ×1.19–1.67 (upper), resolved at 16/16, 21–22/22, 22/22 and 28–29/29 levels. **Two riders.** (1) It is **not a fifth gap component**: the ADR-0040 sustained-peak closed form `gate ∧ H_erosion > H_c,trans` is `l_c`-free, so the critical pipe length cannot change whether a realization *can* breach, only how long it takes — it lives inside the temporal step exactly where the end anchor does, pinned by test at a converged level and at a deep-tail one. (2) A shorter `l_c` puts the ADR-0030 barrier fewer metres from the start, and on the development stub a factor-four shortening does produce Euler barrier jumps (2 rows in 400); **the production check is clean — zero rows failing transiently but not statically in either arm at any of the 89 levels, against a baseline that also carries zero.** **Two claims I had to withdraw before writing the note, both flattering and both false:** `l_c` is *not* the first zero-common-mode input (`gamma_bl_sub` reaches the gate and nothing else, and its static invariance was already measured as an identity), and it is *not* the smallest departure in the register (`m_p` at ×1.07–1.22 is smaller than `l_c`'s upper arm at ×1.19–1.67). What survives is sharper: of the two zero-common-mode inputs, `gamma_bl_sub` is **inert at the governing section** (×1.00 at KP62.0) and `l_c` is not (×1.67), and the `l_c`-vs-`m_p` pairing is the register's cleanest illustration that a ρ departure carries no information about mechanism on its own — the two depart by the *same* factor (×1.07–1.22 against ×1.11–1.23), `m_p` because a large effect on each branch nearly cancels and `l_c` because a small effect does not cancel at all. 26 tests added; ruff and black clean.

* **2026-08-21 — the Phase 3 composition seam measured instead of bounded, and it turns the KP62.0 warming tie over (un-numbered study; no default, no config axis, no production artifact changed).** Chapter 7 stated a seam and bounded it by analogy: the re-executed surface curves carry the paper Eq. (10) stage-rating error, so their argument is the stage a rating relation would report, while the piping curve's argument is the realized stage at the levee. **The term is drawn from per-segment `wl_err_mu_m`/`wl_err_sigma_m` fields, so suppressing it is a small well-defined change**: `draw_overflow(..., include_rating_error=False)` **takes the draw and then zeroes it, never skips it**, so crest and turf-velocity stay on the identical stream and the two arms are common-random-number paired on everything except the term under test. New committed companion `uemura_surface_curves_overflow_no_rating_error.csv`, generated in the same pass as the primary set on the same node seed; **the four previously committed curve CSVs came out byte-identical**, only the generation metadata gained a key and a timestamp. `scripts/composition_seam_rating_error_study.py` recomposes and re-annualises with the campaign's own `_compose_segment` (imported, not copied) and **gates on reproducing the production `rq4_annual.csv` rows — passed on all 8**, plus an independent reproduction of the published reach dominance table (4/4, 31/109, 0/0, 79/1). **Result.** The piping branch is unchanged to every digit at all four sections in both climates, so the thesis's "acts on the overflow branch alone" is an identity, not an approximation. The annual overflow contribution moves ×1.16 to ×1.26 at five of the six cells where it is non-zero, the system probability ×1.000 to ×1.070, and a mechanism share by at most 0.039. **The sign is NOT uniform and that is the finding**: suppressing the term both lifts the effective level by the mean (−0.160 m on the Tokachi) and removes the 0.294 m spread, so where the hazard already reaches the crest band the mean governs and overflow rises about a fifth, while where the crest is reached only by the favourable tail of the error the spread is what loads the mechanism at all and overflow goes to **exactly zero** (KP60.0 under warming, whose entire 2.30e-5/yr is carried by that tail). Reach-wide the second regime dominates: overflow-leading segments fall **31 → 8** historically and **109 → 69** under warming, and segments with no loaded mechanism rise **79 → 102** and **1 → 42**. **The KP62.0 warming crossing does not survive**: margin 1.0013 → **0.858**, piping share 0.500 → **0.462**, overflow takes the lead, and it is the **only** ordering in the reach that changes. The displacement, 1.41e-3/yr, is larger than that entry's flood-ensemble sampling interval, so the crossing genuinely fails rather than blurring — but the arm's own margin is 1.17-fold and its share sits just outside the production 0.48–0.53 band, so this is the **third** independent demonstration that the tie is an artefact of where two curves cross (the others being the canonical event and the conductivity bracket), not a lead for overflow. **Scope stated in the note, not implied:** the arm is one of two consistent readings — it removes from overflow the term piping does not carry; the opposite treatment, adding it to piping, would need Phase 1 re-run on a perturbed loading and is unavailable. It measures the magnitude of the seam and one of its two directions. 11 tests added; ruff and black clean.

- **The thesis model-selection argument sourced from `docs/references/`, and the proposed Chapter 8 figure refused as a duplicate (2026-08-22; thesis-side edit only, no engine code, config, artifact or figure driver changed).** The thesis carried no passage situating the Sellmeijer critical-head rule and the Pol progression law among the alternative BEP formulations: `Bligh`, `Lane`, `Schmertmann`, `Rice`, `Robbins` and `creep ratio` returned **zero typeset hits across all 124,680 words**, while `bligh_1910`, `lane_1935`, `robbins_2017`, `sellmeijer_koenders_1991`, `van_beek_et_al_2011`, `van_beek_hoffmans_2017`, `vandenboer_2017`, `pol_2021` and `vanderLinde2025` sat uncited in `references.bib`. The argument now in thesis Section 2.5.2 is sourced **only** from PDFs already in `docs/references/`, and the traceable claims are recorded here so a later session does not re-derive them from scratch. **(1)** The creep-ratio critique, Bligh's percolation coefficient with a maximum of 18 for very fine sands, Lane's weighting of the horizontal path by 1/3 on 278 cases, and the objection that both judge only the *average* gradient across the structure, is `van_beek_2015.pdf` **section 2.3.1** (printed pages 31 to 32). **(2)** That the transient equilibrium curve **anchors on the revised Sellmeijer rule**, interpolated to the points `(0, 0)` and `(L, 0.9*H_c)`, is `pol_sie_2024.pdf` in the equation block introducing `H_c = L*F_r*F_s*F_g` (printed page 5 of the article). This is the load-bearing sentence of the whole argument: it is why the single-source critical head is not a convenience but the only choice that keeps the paired comparison a comparison of *loading treatments*. **(3)** Vorogushyn et al. (2009), which **is** in `references.bib` as `vorogushyn_2009`, is the one duration-conditional predecessor: it produced piping fragility curves conditional on peak *and* duration but advanced the pipe at a **single deterministic 0.158 m/hour** taken from one large-scale experiment, a rate its authors flagged as materially affecting the computed failure probability. Source: `pol_sie_2024.pdf` literature review and its Table 1. That constant is exactly what the Tokachi question cannot use, since it responds to neither overloading nor grain size nor conductivity. **(4)** The ODE's own calibration set is six small-scale tests plus the large-scale FPH experiment of Pol et al. (2021), `pol_compgeo_2024.pdf` section 3.1 and Table 1; only the large-scale one has a `references.bib` key (`pol_2021`), so only it is named in the thesis. **Schmertmann and Rice have no `references.bib` entry and were therefore not cited**, even though `van_beek_2015.pdf` section 2.3.6 documents Schmertmann (2000) at length; citing either needs a bib entry the author supplies. Separately, a proposed **Chapter 8 figure** carrying the decay of the overestimation factor against stage together with the rotation of its composition was **refused as a duplicate of two figures this repository already produced**: `docs/figures/adr0040_hwl_bias_resolved.png` carries the decay in its left panel (B from 26.9 at the design level to 1.4 at the top of the attainable range) and `docs/figures/stage6_6_fractions_kp62_0.png` carries the rotation in its right panel (head-convention and temporal shares exchanging dominance near 46.9 m), both at KP 62.0, both on the same conditioning-stage axis, and both already placed in thesis Chapter 6. A third rendering would have been the third drawing of the same two curves. **No new figure driver was written; `docs/figures/` is unchanged.**

- **Whole-document submission gate on `msc-thesis`, and the defence brief (2026-08-22).** Ten checks run over `frontmatter/`, all nine `mainmatter/` chapters and all seven `appendix/` files. **Clean on the mechanical gates:** 359 labels, zero duplicates, **zero unresolved `\ref` targets** (the multi-line `\ref{fig: lhs vs crude\nkp60}` in Appendix G resolves; a line-based checker reports it as an orphan and is wrong); 501 citation sites over 152 bib entries with **zero keys missing** and zero malformed sites; 63 of 65 files in `figures/` used, the two unused being the TU Delft class's `logo-{black,white}.pdf`; the em-dash and Japanese-script commands of the msc-thesis project rules both return CLEAN verbatim. **Four defects found and fixed.** (1) Chapter 1's scope-register caption said "Three rows condition how the results may be read" while the paragraph pointing at it said "Two of its rows" and named only two; the caption was right and the paragraph now names the piping-population row first. (2) **The seven-versus-eight conductivity cell count had propagated to three sites** (Chapter 7 twice, Chapter 9 once), each claiming the `field_geomean` arm "changes the answer at every one of the eight section and climate cells". `docs/decisions/conductivity-bracket-annualisation.md`'s own 2026-08-21 correction settles it: that arm **reverses six, collapses one (KP 57.4 historical) and leaves one ROBUST (KP 60.0 historical)**, so it is not robust at seven of eight. Chapter 7's next two sentences already said six-plus-one and "the one cell that holds throughout is KP 60.0 historically", so the passage contradicted itself. All three now read "seven of the eight". The engine note's claim that the conflation "was corrected there on the same date" was therefore **only partly true**, and this closes it. (3) Chapter 1 quoted the KP 60.0 drained lower arm as 0.6 per thousand per year against 6.4e-4 in Chapters 7 and 8; now 0.64. (4) Three Appendix G figure captions began "The same comparison/bound at KP ..." with no short form, so their List-of-Figures entries could not be read alone; each gained a self-contained `\caption[...]`. **One addition on the thesis's own quoting discipline:** the Summary quoted the static-to-transient factor against the conductivity bracket and the canonical event but not the seepage lengths, which is the third non-cancelling bracket (ADR-0047); one clause added. **Two items flagged to the author rather than changed.** (a) 52 of the 104 captions carry no short form (55 before the three fixed above), all of them in Chapters 1 to 5 and the appendices, against a uniform short-caption convention in Chapters 6 to 9; the longest is 1,362 characters (`tab: section inputs`). **[Closed 2026-08-23: all 52 written; `msc-thesis` `b7bc182`. See the 2026-08-23 entry below.]** Harmless until commit 89f7b29 printed the two float indexes, and **not visible in the committed PDF, which predates it**. (b) Chapter 9's future-research register is captioned "ordered by the share of the present uncertainty each would remove" yet ranks the aquifer conductivity, which the same register calls the largest epistemic bracket in the study, fourth; the criterion is uncertainty *removed by the action*, which is defensible but is not stated. **Defence brief written to `docs/defence_brief_2026-08-22.md`**: twelve questions, each with the answer the document can carry and the section, float or artifact that carries it, plus three entries marked **KNOWN EXPOSURE** (the corrected fluvial-scour branch has no independent validation of its own, its equivalence gate testing it against the received implementation; the compound-event zero-recovery memory model is untested and cannot be tested against a survival record; and the future-research ordering criterion above). It deliberately carries **no page numbers**, because the committed `report.pdf` predates these edits.

- **The short-caption convention extended to all 104 captioned floats in `msc-thesis`, closing flag (a) of the 2026-08-22 submission gate (2026-08-23; thesis-side edit only, no engine code, config, artifact or figure driver changed; `msc-thesis` `b7bc182`).** Commit `89f7b29` made `\listoffigures` and `\listoftables` live, which turned a missing `\caption[short]{full}` from harmless into a whole caption printed in the index; the committed `report.pdf` predates it, so none of this has ever appeared in a build. Enumerated independently rather than taken on trust, over `frontmatter/`, `mainmatter/` and `appendix/`, with `%` comments stripped and every `\caption` argument brace-matched: **104 captioned floats, 52 already short-captioned and 52 not.** The 52 sat in Chapters 1 to 5 and appendices A, B, E, F and G; Chapters 6 to 9 were already uniform and supplied the register. Per file: appendix-g 19, appendix-a 9, Chapter 3 7, Chapter 5 6, Chapter 4 4, appendix-b 2, appendix-e 2, Chapter 1 1, Chapter 2 1, appendix-f 1. **The 2026-08-22 count of "52 of the 104" is confirmed exactly, but its file list was incomplete:** it named A, B, F and G and missed appendix-e, whose two floats caption with `\captionof{table}{...}` rather than `\caption` and are therefore invisible to a `\caption`-only scan. `\captionof` still writes a List of Tables entry, so both needed the same fix, and the optional argument goes after the float type: `\captionof{table}[short]{full}`. **The same blind spot puts the float census one out:** `89f7b29`'s message records "62 figures and 41 numbered tables", and the figure count is exactly right, but the List of Tables will print **42** entries, being 39 `table`, 1 `longtable` and the 2 `\captionof{table}`. **Every short form is 23 to 76 characters**, a noun phrase with no closing full stop, self-contained for an index reader, naming the section or scenario wherever the document plots one quantity more than once, and carrying no citation, cross-reference or emphasis. Terminology was taken from the caption below each float rather than from its Chapter 5 twin, so Appendix G reads "interaction involvement" where Chapter 5's paired figure reads "interaction gap". **No typeset caption text moved**, proved three ways: the whitespace-flattened body of all 104 captions is identical before and after at **53,504 characters** either way; stripping the 52 inserted brackets and then all whitespace reproduces each file's committed text exactly; and `git diff --word-diff` yields **52 deleted words, each one the `\caption{Firstword` token the insertion split**, and nothing else. Re-wrapping to 79 columns never breaks inside a braced group or inline math, so `\ref{...}` and `$\Pi = \tau_\mathrm{aq}/T_\mathrm{rise}$` stay whole; the 11 captions the author wrote as one deliberate long line were left as one line rather than re-flowed, and lines over 80 columns across the ten touched files **fell from 1,027 to 1,009**. Label and reference integrity re-checked with **whitespace flattened before matching**, the same correction the 2026-08-22 gate recorded: **359 labels, zero duplicates, zero unresolved targets over 789 reference sites**, and the em-dash and Japanese-script commands of the msc-thesis project rules both return CLEAN verbatim. The two uncaptioned `longtable`s in `frontmatter/nomenclature.tex` and the one in `appendix/appendix-c.tex` were left alone, since they raise no index entry. **No caption's subject was undeterminable from the caption alone**, so nothing is flagged on that ground; the thinnest is Appendix G `fig: mp ztoe factors`, whose "surveyed exit datum" is never expanded in its own caption, but the document does define it as $h_e = z_\mathrm{toe}$ in Chapter 4 and Appendix G and gives its 0.30 m band in Chapter 6, so the short form reproduces the caption's own words rather than resolving them. Flag (b) of the 2026-08-22 gate, Chapter 9's future-research ordering criterion, **remains open and was not touched.**

- **Flag (b) of the 2026-08-22 submission gate closed: Chapter 9's future-research register now states its ordering criterion, and the order of the six rows is unchanged (2026-08-23; thesis-side edit plus a dated close-out in `docs/defence_brief_2026-08-22.md`; no engine code, config, artifact, figure driver or number changed).** The register was captioned "ordered by the share of the present uncertainty each would remove" and introduced as "ordered by how much of the present uncertainty in the reported answers each would remove", while ranking the aquifer conductivity, which the same register calls "the largest epistemic bracket in the study, spanning a factor of $6.6	imes10^{3}$ ... dominating every absolute number reported", **fourth**. **The defect is real and it is in the caption, not in the order.** Two criteria are in play and they give different rankings, which `defence_brief_2026-08-22.md` Q12 had already worked out and recorded as one of its three KNOWN EXPOSURE entries: ordering by *uncertainty present* puts conductivity first, ordering by *uncertainty the action would remove from the four answers* puts the bank-retreat mechanism first. The evidence supports the second as the operative one. `epistemic-bracket-synthesis.md`, `conductivity-bracket-annualisation.md` and `conductivity-bracket-posterior-side.md` have measured the conductivity bracket at four sections, five anchors, both gradation readings, through the annualisation and on both sides of the 2016 update, so a field determination narrows a **quantified** bracket that the thesis already carries as a stated condition on every answer; `r10-foreshore-exhaustion-screening.md` is Tier 1 only, its consequence stands in the Chapter 8 limitations register as **"Exposure only"**, and it is the single row there that bounds a headline answer (mechanism dominance) without a quantified consequence. **Rows 2 and 4 of the register already carried the justification in their own text** ("The range is already established"; "what a determination would supply is a narrowing of the bracket and not a first quantification of it"), so nothing in the table needed rewriting; what was missing was the criterion those clauses answer to. **Fix chosen: state the criterion, leave the order (option (a)), with the divergent ordering conceded in one clause rather than printed as a second ranking (the compressed half of option (c)).** The introducing sentence now reads "ordered by how much of the uncertainty in the four answers each would remove, which is not the ordering by the width of the bracket each addresses. Ranked by the absolute probabilities alone, the fourth item would lead", and the caption "ordered by how much of the uncertainty in the four answers each would remove, not by the width of the bracket each addresses". **Reordering was considered and refused**: it would have promoted an item whose consequences are the best-measured thing in the document over the one omission that can overturn a headline answer, contradicted `subsec: A Migration-Capable Bank-Retreat Model` ("the single most consequential extension available to the integrated framework"), broken the "first is developed below" structure of both the caption and the section, and produced the mirror-image follow-up question instead of removing it. **Cost against the closed page budget: about two typeset lines in the body and under half a line at `ootnotesize` in the caption**, no float, no row and no `\label` touched. **Verification:** whitespace-flattened and comment-stripped over `frontmatter/`, `mainmatter/` and `appendix/`, **359 labels, zero duplicates, zero unresolved targets over 789 reference sites**, reproducing the 2026-08-23 baseline exactly; the em-dash and Japanese-script commands of the msc-thesis project rules both return CLEAN verbatim; `pytest tests/test_repo_hygiene.py` passes. **Engine-side close-out:** the Q12 KNOWN EXPOSURE paragraph in `docs/defence_brief_2026-08-22.md` is **left standing verbatim** with a dated "Closed 2026-08-23" note under it recording the two new sentences, the exposures table drops the Q12 row from three entries to two with a dated removal note, and the brief's own "three of the twelve entries end in a stated limit" carries a dated amendment to two. **Flag (b) of the 2026-08-22 entry above is left standing as written**, per this file's rule that entries are never rewritten; this entry is its outcome.

- **The `msc-thesis` page budget re-opened by the author and re-baselined at a hard ceiling of 115 main-body pages (2026-08-23; thesis-side campaign, seven commits on `msc-thesis@main`; no engine code, config, artifact, figure driver or number changed).** The 2026-08-20 ruling closed the budget at 112 pages and forbade re-opening it. That closure was overtaken by growth: `git diff --numstat df6fed5..HEAD` shows **444 net source lines of main-body material** landed after it (Ch1 +1, Ch2 +72, Ch3 0, Ch4 +33, Ch5 0, Ch6 +39, Ch7 +161, Ch8 +89, Ch9 +49), and the fresh `output.toc` of the 2026-08-23 build put the main body at **119 pages, References on 120**, per chapter 7/11/15/14/15/19/16/13/9. **Measured, not compiled:** page extents were read off the `\contentsline` entries (main body = References page minus one), because the committed `report.toc`, `report.aux` and `report.pdf` were all still from the 2026-08-15 build while the author had dropped fresh gitignored `output.toc`/`output.lof`/`output.lot` in the repo root. **The single worst offender was a table geometry, not prose.** Table 8.2, the limitations register, was a five-column `longtable` whose "Quantified" column sat at `0.093\textwidth` while carrying entries of up to 45 words; a 45-word paragraph in a 9 per cent column sets as a tall thin ribbon and forces the whole row to that height, and the conductivity row alone ran to 30 lines against a table mean of 7.5. **Method that settled it:** each candidate layout was rendered as monospace ASCII at its real per-column character budget (text block 482.6 pt, footnotesize 8 pt, average Palatino advance 0.482 em, so 125 characters per full-width line and 12 in a 0.093 column) and iterated, rather than tweaking widths once. Merging "Direction", "Quantified" and "Detail in" into one prose column with labelled run-ins took the register from **165 footnotesize lines to 104** with every word, number, section pointer and all 22 rows carried through verbatim; a four-column variant keeping a short Yes/Partly/No token cost 12 lines more and was refused, and `\scriptsize` would have saved a further 0.3 page and was refused as a legibility cost the author had ruled out in spirit. The same measurement rebalanced four more registers (Ch2 model selection 30 to 26 lines, Ch6 piping conditions 50 to 43, Ch7 system conditions 49 to 38, Ch8 transferability 24 to 21) with **no cell text touched at all**. Table 9.1, the answers register, was a `table[!ht]` of about 97 source lines, longer than a page, so LaTeX could not place it inline and deferred it whole, stranding the sentence that introduces it on a short page; converting it to a `longtable` makes it begin where it is introduced, and folding "Carried by" into "The number" as a run-in took it from 74 lines to 60. **A page model was calibrated before any prediction was quoted:** prose at 100 characters per line at 10 pt, `\parskip`, headings and displays modelled explicitly, gave per-chapter residuals of **0.42, 0.43 and 0.43 pages per figure float** at the three float-heavy chapters, which is the consistency that makes the residual a float budget rather than a fudge. **Preamble changes, itemised and all in `report.tex`:** `\textfloatsep` 20 to 14 pt, `\floatsep` and `\intextsep` 12 to 10 pt, caption skip 10 to 6 pt, `\parskip` from `0.5\baselineskip` to `0.4`. parskip 2.0h patches `\addvspace` to subtract `\parskip` from it, so **heading separation is unchanged by the `\parskip` change**, which is why no `\titlespacing` was touched. Type size, margins and line spacing were out of scope and were not touched. **Three supporting-record figures migrated into Appendix G**, each into a subsection that already described exactly what it shows: the GSA convergence ladder to the estimator design that states its own 0.02 acceptance criterion, the GSA companion runs to the dependent-input companion, and the KP 62.0 comparator ladder beside the KP 57.4 ladder it is the counterpart of. Each left its pointer in the main body naming the appendix. **Recovered about 4.45 pages of ink; predicted main body 114.6 pages, integer outcome pending the author's rebuild**, the residual risk being that savings spread thinly across nine chapters may not each cross a page boundary. **Refused as indefensible content cuts** and listed for the author instead: the Yabe out-of-sample validation sentences and the crack-reduced secondary factors in the Summary, the KP 62.0 grid-extension warnings repeated across six Chapter 6 captions, the third view of the eight fragility curves, and the Chapter 3 `tab: section inputs` caption, which is a data dictionary for an 11-column table and not restated argument. **Verification, all reproducing or matching the 2026-08-23 baseline:** 359 labels with zero duplicates, **795 reference sites with zero unresolved targets** (up from 789 by the six appendix pointers added), 503 citation sites over 100 distinct keys all present in `references.bib` with zero malformed sites, 59 graphics with none missing, 105 float environments unchanged, all six register row counts identical to HEAD (scope 12, piping conditions 9, system conditions 8, limitations 22, answers 4, future research 6, so "nine conditions", "Eight conditions" and "Six extensions" all still hold), both the msc-thesis project rules PowerShell style gates CLEAN verbatim, and a **whole-document numeric-token diff against HEAD** in which every one of the nine changed tokens is individually accounted for. **Two number-bearing sentences were removed and both are traced**: the confidence interval `18.8 to 25.2` on the secondary KP 62.0 anchor is gone from the Summary and survives in Chapters 6 and 9, and the clause spelling out the `1.000` and `0.000` historical shares is gone from the Table 7.3 caption while both values remain in the table body and the sentence explaining why those cells carry no interval remains. `docs/thesis_number_reconciliation_2026-08-21.md` carries a dated addendum for both.

- **The 115-page ceiling met on rebuild, and a second pass taken on top of it to 112 (2026-08-23, same date as the entry above; thesis-side, six commits on `msc-thesis@main`; no engine code, config, artifact, figure driver or number changed).** The author's Overleaf rebuild of the campaign above returned **exactly 115 main-body pages**, References on 116, per chapter 7/11/15/14/14/18/16/12/8, so the prediction of 114.6 was right to within the integer and the hard ceiling was satisfied. Fewer is better, and the rebuild exposed what a prediction cannot: **four chapters and the Summary ending a few typeset lines onto an otherwise empty page**, plus a list of tables one line over. The second pass cut each overflow at source, targeting **112**. **The lever was not the one the first pass used.** The first pass found its four and a half pages in table geometry, float and paragraph spacing, and figure migration; by the second pass those were spent, and essentially all of the second pass came from **passages restating something already established elsewhere in the same chapter**, which is where a document assembled in parts accumulates ink. Chapter 1, ten lines: the piping-population limitation was stated four times over, in the geographical scope, in a row of the scope register, in that register's caption and again in the paragraph introducing the register, and the technical scope quoted the five-value drained-configuration bracket that Chapter 8 establishes in full with three qualifications the Chapter 1 passage did not carry. Chapter 4, six lines: the overview carried a prose recital of the chapter's own eight sections, which the contents now prints to subsection depth, and Section 4.5 pre-announced two Chapter 6 findings before stating the method that measures them, against the rule that the thesis presents the final methodology only. Chapter 8, nine lines: six restatements plus the foreshore-exhaustion figure, whose reading condition appeared in its caption and in the text two lines above it, and which was set to 0.85 of the text width. Summary, two lines: five more repetitions. **The list of tables was a pure layout defect.** `\@chapter` writes a fixed `\addvspace{10\p@}` into the `.lof` and `.lot` before each chapter's entries; with 42 entries and 16 such gaps, 160 pt of that list, about a fifth of it, is group spacing, and it ran a single line onto a second page. Cutting the gap to 6 pt recovers 64 pt and closes it with margin. `\addvspace` appears in no other contents file and `\@starttoc` reads each list inside its own group, so the redefinition reaches nothing outside the two lists it is wrapped around, and both list headings sit at a page top where vertical glue is discarded. **Refused:** shrinking the two Chapter 4 framework diagrams, whose type is already `\scriptsize`, when rephrasing was available; setting the limitations register in `\scriptsize`, which would have paid a further sixteen lines at seven-point type for a 22-row register a reader must actually read; and migrating the foreshore-exhaustion figure to Appendix G, which would have paid twenty-five lines but left the Discussion with no figure at all. **Useful conversion, recorded in the msc-thesis project rules rule 4:** at this measure and leading, body prose runs about **one typeset line per 100 characters**, so a nine-line overflow is roughly 900 characters of restatement to find, which is what made the second pass estimable before any rebuild. **Verification, all identical to the pre-pass state:** 359 labels with zero duplicates, 795 reference sites with zero unresolved targets, 503 citation sites over 100 keys all in `references.bib`, 59 graphics with none missing, 105 float environments, all six register row counts unchanged, zero brace imbalance in any source, both PowerShell style gates CLEAN verbatim, and a whole-document numeric-token diff in which every change is individually accounted for. `docs/thesis_number_reconciliation_2026-08-21.md` carries the one further claim site this pass removed.

- **Main body closed at 112 pages, verified against a build rather than estimated (2026-08-23, third pass; thesis-side, three commits on `msc-thesis@main`; no engine code, config, artifact, figure driver or number changed).** After the second pass the author's Overleaf build still showed Chapters 4 and 8 ending four lines onto an otherwise empty page. Both are now closed. Final map: Ch1 6, Ch2 11, Ch3 15, Ch4 13, Ch5 14, Ch6 18, Ch7 16, Ch8 11, Ch9 8, References on 113, so **112 main-body pages against a 115 ceiling**, with every chapter's last page full, the Summary in exactly two pages and the list of tables in exactly one. **The verification method changed and that is the transferable part.** The author asked for confirmation by rendering specific pages, so a local XeLaTeX build was made and checked page by page instead of estimated from character counts. **The trap that makes the obvious version of that wrong:** building with `latexmk -outdir=<scratch>` against the working tree leaves XeLaTeX resolving `\input` from the current directory, so biblatex reads the **stale `report.bbl`** sitting in the repo root; every citation added since that file was written silently goes undefined, and the build lands **one to two pages short** of the real document while reporting only a mild "failed to resolve" warning. Copying `report.tex`, the class, `references.bib`, `frontmatter/`, `mainmatter/`, `appendix/` and `figures/` into an isolated directory and building there reproduced the author's chapter map **exactly**, which is what licensed trusting it. Arial and Roboto Slab resolve locally; the only font substitutions are Latin Modern Sans italic inside TikZ and IPAexMincho italic in the bibliography, neither of which moves body text. **Two typesetting mechanics decided the pass, and neither is visible from the source.** First, **a cut only becomes a page where the chapter has no float to repack.** The second pass cut Chapter 4 in Sections 4.1 and 4.5, which sit among its four floats, and almost the whole gain went into float repacking: 5.9 lines removed, one line of overflow recovered. The third pass cut the same chapter in Sections 4.6 and 4.8, which carry no float, and it landed one for one. Second, **`\widowpenalty` and `\clubpenalty` are 10000, so a paragraph breaking across a page cannot leave a single line**: the last paragraph either fits on the preceding page or leaves at least two. Chapter 8 sat at exactly two lines through a whole round of cutting for that reason, and only moved when the entire final paragraph came back. Aim to pull the paragraph back, not to shave lines off it. **What was cut, all restatement and no result:** in Chapter 4, the persistence paragraph of Section 4.6.2, which listed the parameter matrix, both Boolean failure matrices, the conditioning grid, the seed and six further recorded quantities, and which is data-format detail that Appendix B carries and the msc-thesis project rules rule 3 keeps out of the main body, plus three smaller duplications; in Chapter 8, six passages each established elsewhere, including a sentence Chapter 7 states in the same words. **Verification:** 359 labels with zero duplicates, 795 reference sites with zero unresolved targets, 503 citation sites over 100 keys all in `references.bib`, 59 graphics, 105 floats, all six register row counts unchanged, zero brace imbalance, both PowerShell style gates CLEAN, and the local build itself reporting zero unresolved references or citations. The whole-document numeric-token diff shows a single change, the `7` of "the complete $N \times 7$ parameter matrix", and the vector's dimension remains stated at three other sites.

- **The thesis's "a levee counts as safe below the Planned High Water Level" claim was FALSE, and the sources it cited say the opposite (2026-08-28; record `docs/japanese_levee_failure_criterion_review_2026-08-28.md`, applied to msc-thesis the same day).** Raised by the author's Japanese professor and confirmed against the governing documents, three ways. **Polarity:** the binary HWL rule that exists in Japanese practice is a *failure* condition, not a safety condition. MLIT 洪水浸水想定区域図作成マニュアル（第4版） §2.4(1) sets 氾濫開始水位 = 計画高水位 in principle and assumes breach-induced inundation at the corresponding discharge; HKV/Docon WP2 2024 §1.1 names exactly this ("a breach will occur if the water level exceeds the planned level") as the convention the collaboration replaces, and Uemura's PhD §4.1.1 calls it deliberately conservative. The thesis stated the mirror image and framed a conservative convention as a permissive one. **Category:** below the HWL safety is *demonstrated*, not presumed. 河川堤防設計指針 (2002, rev. 2007) §5(2) fixes 照査外水位 = 計画高水位, 照査降雨 = 計画規模の洪水時の降雨, method = 非定常浸透流計算 + 円弧すべり, allowables Fs >= 1.2*a1*a2 landside, Fs >= 1.0 riverside, i < 0.5 without a blanket, G/W > 1 with one. Over the ~10,000 km directly managed network only 59.2 % passes everything (PWRI Fig. 6.3.1, 治水課調べ H23.1), so **40.8 % is deficient and 25.1 % on piping**. **Direct contradiction:** 河川砂防技術基準 設計編 技術資料 第1章第2節 2.2.1 states that a levee built to the standard 「計画高水位以下の洪水に対して絶対的な安全性を有するものではない」 and gives **継続時間が著しく長い** floods as the reason; the 2002 guideline §2(1) says the same from the damage record. **That is the strongest available warrant for this thesis: the national standard names loading duration as the exposure and supplies no limit state that can represent it**, so the correction replaced a refutable motivation with an insider one. Provenance of the error: Uemura PhD §4.1.1's "in terms of river planning, HWL is the range in which the safety of levees is guaranteed", a statement about the design *requirement*, read as a statement about the assessment *verdict*. Field record for scale: 2019 Typhoon No. 19 breached 142 levees (越水 86 %, 侵食 9 %, 浸透 1 %), and **58 of the 72 nationally managed sites where overtopping was confirmed, 81 %, did not breach** (MLIT 2020 doc 2), which also disposes of a deterministic crest-overtopping rule. **A second claim fell with it.** Chapter 2 read JICE 2019's no-confirmed-completion statement as covering foundation piping; enumerating the vector drawings of その Fig. 1.1 shows the page carries **exactly two dashed strokes, both at x = 125 pt, the centre of the 堤体パイピング box** (the arrows in from 漏水・噴砂 and out to 天端陥没). The foundation route is solid throughout, and the thesis's own Chapter 5 validates against the **2012 Yabe River foundation-piping breach**, so the unqualified generalisation contradicted the thesis internally. Now scoped: initiation attested on both routes, completion rare and, on the embankment route, absent. Appendix G and Chapter 2 §287-293 were **already accurate** and needed only two additions; the defect was confined to the Summary's first sentence, Chapter 1 ¶2 and Chapter 2 ¶1, which had been contradicting the thesis's own appendix. Two `references.bib` entries added (`mlit_teibou_sekkei_2007` dated to the 2007 revision in force, `mlit_shinsui_manual_2015`); net +7.9 typeset lines of main body, **not verified against a build**.

- **The uplift/heave question from the 2026-08-27 supervisor meeting: answered, and one thesis claim found to outrun its citation (2026-08-28; thesis-side edits plus one new figure driver; no engine default, no ADR, no persisted result changed).** The committee asked whether the Bayesian updating conditioned on the Sellmeijer criterion alone or on uplift and heave as well. **It already conditions on both, and that is now measured rather than asserted:** across all four sections and both `d70` readings, **zero of 100 000 realizations fail the transient limit state with the gate shut**, so uplift-plus-heave is strictly necessary for a transient rejection. The **static comparator** is Sellmeijer alone by ADR-0028, and gating it is nearly inert: adding the gate to the static branch moves no cell at KP 62.0 at any level (so the headline design-level bias **B = 26.9 is exactly invariant**), and bites only in the low tail elsewhere (KP 57.4 at 39.50 m, ratio 0.64; KP 58.8/60.0 at and above their design levels, ratio 0.9996 or better). Under the bulk reading the gate is inert on the static branch at all four sections. This reproduces the Chapter 6 gap-ladder's `gate` column (0.00 to 0.03) from the persisted matrices independently. **The strict `no_breach_no_initiation` criterion reduces exactly to `not initiation`** (`n_strict == n_no-initiation` at all four sections, verified row-wise), so it discards the survival evidence it is supposed to strengthen, and its rejection is **identical under matrix and bulk** because it is not a function of `d_70` at all. **The literature verdict is decisive and comes from the thesis's own benchmark**: Schweckendiek (2014) §5.4, Table 5.5, licenses "no uplift" only from an observed absence of **seepage**, not of sand boils, and states explicitly that seepage without erosion licenses **no conclusion about the heave limit state**, heave being necessary but not sufficient for transport to the surface. The baseline therefore stays `no_breach`. **Provenance defect found and corrected:** `tokachi_levee_committee_2017` was cited in four places for a no-sand-boil observation *at the study segments*. The report was fetched from its MLIT URL and read (77 pages; it is **not** in `docs/references/`): its three no-boil statements (§4-3-1, §5-3-1, §5-3-4, Tables 4-3 and 5-5) are all scoped to the **immediate vicinity of the three breach sites on the Satsunai and Otofuke**, and the Tokachi mainstem right bank at KP 57.4 to 62.0 **does not appear in the report at all** (zero occurrences of the leakage, heave and patrol terms). Chapter 3, Chapter 4 and Appendix A were rewritten onto what the source supports (no seepage-attributed breach anywhere in the directly managed system; no boil *recorded* at the study segments, an absence of record rather than a survey), and Chapter 3's claim that this was "not an accident of inspection coverage" was scoped to the breach statement, which is the half it is true of. Chapter 1, Chapter 8 and Appendix G already used absence-of-record phrasing and needed no change. **New deliverable:** `scripts/plot_initiation_fragility.py` draws P(uplift and heave) beside both piping branches on the identical sample (`docs/figures/initiation_fragility.png`, thesis Figure 6.2), discharging Chapter 2's promise to evaluate all three sub-mechanism probabilities explicitly. The gate probability is closed-form under the ADR-0008 collapse (`r_e (h - z_toe) > (gamma'_bl/gamma_w) D_bl`) and the driver **refuses to plot unless it reproduces the Phase 2 replay's own initiation flags row for row**, which it does at 100.0000 % on all four sections. The gate reaches 0.99 between **0.75 and 3.11 m below the stage at which the transient branch reaches one half**, so initiation is not the controlling sub-mechanism anywhere. Main body **115 pages, verified against an isolated build**, the ceiling met exactly: the new figure cost Chapter 6 one page and Chapter 4 was brought back from 14 to 13 by cutting restatement (a forward-pointer paragraph folded into the survival-discrimination subsection where the mechanism is stated in full, and a Phase 2 handoff sentence). 880 tests green; the new figure is registered in `production_campaign.FIGURE_DRIVERS`, which `tests/test_figure_pass.py` caught as missing.

- **The RQ1 supervisor criticisms answered in one campaign: the comparison re-expressed in reliability-index terms, and the equal-head-convention run built and measured (2026-08-28; plan of record pre-registered in ADR-0051; ADR-0051; companions `docs/decisions/equal-head-convention-study.md` and `docs/rq1_beta_reexpression_2026-08-28.md` with `docs/decisions/rq1-beta-reexpression.{json,csv}`; drivers `scripts/rq1_beta_analysis.py` and `scripts/equal_head_convention_study.py`; gates ruff/black/pytest 903 green, whole-matrix static bit-identity knob on and off, ladder C0/C4b reproduction at all ten 1e6 levels).** Three results of record. (1) The head-convention difference between the two limit states is exactly the 0.3·D_bl crack term and nothing else, and its provenance is now verbatim-verified: Sellmeijer 2011 contains no such term (zero hits for crack/blanket; gross-head definition quoted), Pol SIE 2024 Eq. (6) adopts it citing Schweckendiek 2014 and TAW 1999, Schweckendiek 2014 applies it to the *static* limit state for Dutch assessment, and the TAW 1999 primary source is absent from `docs/references/` (unread — flagged, not cited). (2) In reliability-index terms the design-level anchors are Δβ = 0.90 [0.85, 0.97] (KP 62.0, 1e6), ≥ 1.27 (KP 57.4, bound), 1.22 and 1.87 (drained sections): the ratio-space spread "2.75 to ≥ 148" collapses to 0.9 to 1.9, the section ordering *reverses* (the drained sections carry the larger index shift) **[CORRECTED 2026-08-29: false. KP 58.8's 1.22 is below KP 57.4's bound of 1.27, so it is a *re-ordering*, not a reversal — KP 62.0 falls from second to last, KP 60.0 rises from third to first, KP 57.4 slips only from first to second. See the entry of 2026-08-29 and the correction recorded in this log and in `docs/rq1_beta_reexpression_2026-08-28.md`. The band 0.9 to 1.9 is unaffected, and its top is resolved, not a bound.]**, Δβ is shallowly U-shaped in stage (dips ≤ 0.14, rises up to +0.76 to its maximum at the top attainable stage) while B decays by factors 29 to 269 toward 1, and the ladder telescopes additively in β (KP 62.0 design: head 0.36, gate exactly 0.00, temporal 0.55). The "most conservative at design levels" and "not principally temporal" claims are ratio-space and probability-difference statements respectively; both are now carried in two registers. (3) The equal-head-convention run (crack term removed from the transient driver, ADR-0051 factor 0.0): B_eq = 7.34 [6.52, 8.30] and Δβ_eq = 0.572 [0.540, 0.605] at KP 62.0's design level, 23.1 / 0.842 at KP 57.4 (which *resolves* there, 49 rows, where the as-published comparison never did), 1.87 / 0.879 and 2.11 / 1.549 at the drained sections — 54 to 83 % of the as-published index difference survives equalisation **[CORRECTED 2026-08-29: quote **63 to 83 %**. The 54 % is KP 57.4's 0.842/1.558, and 1.558 is that section's below-floor point estimate (2 transient rows, R1 floor 30); against the bound Δβ ≥ 1.27 that the tables actually carry, the KP 57.4 fraction is at most 66 %. See the entry of 2026-08-29.]**, so the time dimension is worth Δβ ≈ 0.57 to 1.55 at design levels on its own. There is no unique equal convention: gross-vs-gross and reduced-vs-reduced agree within 10 to 24 % at KP 62.0 but split 23.1 vs 12.0 on 2 rows at KP 57.4. Pre-registered expectation E3c (Δβ_eq 0.2 to 0.7) failed at 3 of 4 sections because it was converted from the ratio band by a stage-dependent mapping — recorded as the finding it is. Riders: 10 Euler-flip rows at KP 57.4 1e6 (1 of 49 at the anchor, excluding it moves B_eq 23.10 → 23.58, inside the CI) and one barrier-jump row at KP 58.8 1e5 (head 3.04 mm below H_c; stalls at Δt ≤ 112.5 s). Thesis-side integration ran as waves on `msc-thesis@main` the same date.

- **The Hokudai reviewer's major-revision report triaged and answered, then the answer audited (2026-08-29; plan and verdicts summarised in this entry; thesis-side only, six commits on `msc-thesis@main`; no engine code, config, artifact or number changed).** A Hokkaido University full professor, a non-BEP specialist, reviewed the **first draft** Abstract and final chapter only, reproduced no numbers, and returned *Major Revision* on seven major and ten minor comments, all of them about framing rather than method. The triage's governing finding is that **the three August campaigns had already overtaken most of it**: MC3 (separate the total formulation difference from the isolated progression-time effect) was implemented in full by the Δβ re-expression, MC1 by the 2026-08-28 Japanese-practice correction, which went further than his suggested wording, and m2, m3, m7, m8 by the terminology and evidence audit. Verified residuals were sized into three work packages plus one optional, executed as four separate passes: **P1** the neutral comparison register (claim-verbs softened where the subject is practice or the criterion, the defined symbol B kept everywhere with a definitional caveat added at its Chapter 6 definition, the last "excluded from levee assessment altogether" sentence dropped, "risk" renamed to the probability quantity where no consequences are modelled), **P2** the conditional adaptation framing and the precise climate attribution, **P3** the early nesting statement in Chapter 4 and the as-if-undrained caption pass over nine deliverable floats, **P4** the ~180-word one-sentence Chapter 9 closing split into three. **Not adopted, with reasons recorded**: renaming B (a defined symbol in ~40 places whose direction is structural, and the supervisor-approved Δβ-first register already demotes it), his three suggested new analyses under MC4 (the two-sided bracket propagation is the stronger robustness statement; equalizing the uncertainty treatment would mean rebuilding Uemura's surface models), and his positioning paragraph wholesale (the current Overall Conclusion carries every conditionality he lists with measured numbers attached).

  **The audit found four things the packages left, three of them substantive, and all are now closed.** (1) MC5's "unsafe" sharpening reached the Summary and Chapter 2 but not the three chapter sites that state the same peak-only conditioning finding: Chapter 9 recommendation 1 was named in the triage and dropped from P1's numbered edit list, and Chapters 6 and 8 were never named at all, so the Summary had ended up **gentler than the chapters it summarizes**. All three now read "non-conservative direction"; Chapter 5's "a bounding case in the unsafe direction" is a different sense and is kept. (2) P2 replaced the unsupported height-only premise the reviewer objected to with a **different unsupported assertion**, that the revised design discharge is accommodated through a program of measures "of which crest raising is one". The 816-page Chisuishi review records no such measure: it records the 2022 revision **retaining** the planned high-water level at 38.14 m T.P. with a verbatim reason for avoiding a raise, and the levee works it records in this reach are side berms and revetments. The membership claim is dropped and the conditional carries the argument alone, which is what MC7 asked for. **Lesson: a conditional reframe can smuggle in a new factual premise, and the guardrail written for it ("do not assert what share of the program each measure carries") was too narrow, catching shares but not membership.** (3) P3's new early nesting statement exposed a pre-existing **error of direction** 461 lines later in the same chapter, where Section 4.7 said the two rejection sets "are not nested" and gave the static criterion's *lack* of an uplift gate as the reason. The containment is the correct claim and it is structural, exactly as ADR-0030 argues it: H_eq peaks at H_c at l_c, and the transient erosion head is below the static gross head by the crack term, so a pipe whose driving head never reaches H_c stalls short of breach; the missing gate can only make the static set the larger one. (4) P1 fixed, in passing, a **latent direction error in the closing sentence of Chapter 9**, which had said a steady-state criterion "overstates the per-event reliability index" when it returns an index 0.9 to 1.9 *lower*. Everything else verified clean: no number, interval, bound, label, reference or citation moved anywhere in the campaign, the only numeric-token changes being P3's caption additions, and the nine touched captions all already carried short arguments, so the lists of figures and tables are unchanged.
- **Four register claims about the RQ1 reliability-index re-expression found miscalibrated and corrected across the thesis; two of them originated in this repository's own campaign plan (2026-08-29; thesis-side edits on `msc-thesis@main`, four files; engine-side edits to architecture and decision records, the plan's §1.3 and this log; no code, config, artifact or measured value touched).** The 2026-08-28 Δβ re-expression had not been through an independent number check. Re-reading `docs/rq1_beta_reexpression_2026-08-28.md` §2 and §4 and `docs/decisions/equal-head-convention-study.md` §4.2 and §4.3 against the text turned up four claims about the *register* of the comparison that the tables do not support. **(1) "The ordering reverses, the drained sections carrying the larger index shift" is false**, at 9 thesis sites and in the campaign plan §1.3 where it started. The measured orderings are B: KP 57.4 (≥148) > KP 62.0 (26.9) > KP 60.0 (2.92) > KP 58.8 (2.75); Δβ: KP 60.0 (1.87) > KP 57.4 (≥1.27) > KP 58.8 (1.22) > KP 62.0 (0.90). KP 58.8's 1.22 sits **below** KP 57.4's bound, so the berm-only section outranks a drained one and only two of four sections move far: it is a **re-ordering** (KP 62.0 second to last, KP 60.0 third to first, KP 57.4 first to second), not a reversal. The surviving substantive point, kept in the text, is that a fifty-fold spread in probability is about one index unit and that the section with the largest ratio is not the one with the largest index shift. **(2) "0.9 to at least 1.9" puts the bound at the wrong end**, at 3 sites. In B the top of the range *is* a bound (KP 57.4, ≥148); in Δβ the top is KP 60.0's fully resolved 1.87 and the bounded section sits in the middle, so the "at least" attaches to the ratio only. The Summary and Chapter 6 already had "0.9 to 1.9". **(3) The 54 % retained fraction is not reproducible from the table printed beside it**, at 7 sites (2 of them outside the reported list). 54 % is KP 57.4's 0.842/1.558, and 1.558 is that section's **below-floor point estimate** on 2 failing transient rows in 1e6 against the pre-registered R1 floor of 30 — a denominator the thesis explicitly refuses to quote 400 lines earlier ("The point estimate that a resampling of two realizations would produce is not reported"). `tab: equal convention` carries the bound Δβ ≥ 1.27 instead, from which a reader computes 66 %. Resolved: quote **63 to 83 %** (63 KP 62.0, 72 KP 58.8, 83 KP 60.0, at most 66 KP 57.4 against its bound), every figure now divisible straight out of the table, with the 54 % kept once in Chapter 6 and named for what it rests on. **(4) "Two readings agree within about a quarter" held at one of two sections.** Gross-vs-gross against reduced-vs-reduced is 7.34 vs 8.03 at KP 62.0 but 23.1 vs 12.0 at KP 57.4, a factor of two; Chapter 6 already said so ("KP 62.0 as 7 to 8, KP 57.4 as about 5 to 23") and only the Summary kept the flattering half. Two generalisable lessons. First, **the error was in the plan before it was in the thesis**: §1.3 asserted the reversal from a 1.22-versus-≥1.28 comparison it never carried out, and three later documents inherited it verbatim, so a pre-registration's *readings* need the same adversarial check as its numbers. Second, **a bound behaves differently in the two registers** — the same KP 57.4 anchor is the range maximum in B and a mid-range value in Δβ, and it is a legitimate denominator in one and not the other; monotone re-expression preserves the numbers and not the rhetoric built on them.

- **The literature-carried claims calibrated: nine cited values checked against their sources, six corrected, two confirmed, one left open (2026-08-29; evidence record `docs/literature_claim_calibration_2026-08-29.md`; thesis-side only, five `.tex` files; no engine code, config, artifact or number changed).** The 2026-08-21 reconciliation traced 431 claim groups to computed artifacts and stated in writing that it did **not** verify the 58 values carried by a citation. Nine of those were verified here, chosen because each carries an argument. **Two open flags from that pass are now closed.** The **Abashiri 234-hour duration** is VERIFIED verbatim: it is on page 4 of 4 of `docs/references/tokachi_river_basin/ctll1r0000001cmh.pdf`, which page 1 identifies as the 帯広開発建設部 levee detailed-inspection maps of March 2008, i.e. exactly the cited `obihiro_levee_inspection_2008`, and every element of the Appendix G passage (two districts, September 2001, 234 h above the warning level, evacuation advisory, embankment leakage, ring-levee works at 7 sites with monitoring at 10, works from 2002, landside-foundation piping and landside slope sliding as the stated purpose, 1:3.0 and 1:5.0, 6.5 m drain) is in that one paragraph; it appears in an Obihiro-jurisdiction report only as an explicitly labelled 参考 reference example, which is why reach-side searching does not find it. The **conductivity "factor of about 2.9"** is corrected to **2.5** at both sites: the sentence claims a band, and `exp(1.96 * sigma_ln) = 2.524` at `sigma_ln = sqrt(ln 1.25) = 0.4724`; the appendix now states the construction and the full percentile span, 6.371, so it is reproducible either way. **The largest correction is D6, and it reverses an attribution.** PWRI Technical Note 4300 (2015) was located and read in full (295 pages, retrieved from the PWRI site, not committed): it contains **no 15 per cent gravel threshold anywhere**. Its own summary states 「礫分を10％以上含み60%粒径の大きいケースでは ... 進行性破壊が生じなかった」, i.e. **10 % or more gravel jointly with a coarse D60**, inside tests whose maximum gradient was about 0.5, and it records slope collapse at 33 % gravel and survival at 69.1 %. The 15 % is **Fukuoka's** (2019, p. 5, 「礫分含有率の堤体内平均値が15%以上」), and it is an **average** and stated as "or more". Chapter 2 was therefore already exactly right and was left alone; Appendix G, which had put the 15 % on `pwri_4300_2015` and written "exceeds", was corrected. **D1** is the Pol range: the abstract says "a factor of 5 to more than 10^6", not 10 to 10^6, and section 3.4 splits it by configuration, the factor-5 end being coarse sand under a thin blanket on a short path; river cases run 10 to 100 at worst and **below 5** for the Tokachi configuration class. **D2** turned that last clause into the corroboration the thesis was entitled to and did not use: one sentence in Chapter 8 section 1 only, noting that the measured pure-duration factor of one to about six matches what the progression model's own authors expect for river levees on coarse sand under a thin blanket. It is an independent published expectation matching a measurement and **not a validation**: their F_td is a cumulative-to-2050 ratio, this thesis's factor is a per-event conditional ratio at a stage. **D3** kept two exact numbers and dropped a non-sequitur: Pol's nine-month recovery test shows the **pipe closed** (erosion restarted from the downstream exit, and the absence of a fast upstream pressure response is his explicit evidence that no shortcut remained) while the **resistance stayed reduced**, which is the opposite pairing from `r_l = 0`, that carries length forward and holds resistance fixed; the interval between typhoon peaks being hours to days rather than nine months now carries the assumption, with the Gounokawa blanket-memory observation of Chapter 5 pointing the same way. **D5** removed a symbol collision: the thesis defines `i_c` as the Terzaghi heave gradient, about 0.70 here, so the 0.5 national screening value can no longer wear that symbol. **D9** fixed a juxtaposition: Chapter 5 named "the widest" IJkdijk deviation and then illustrated with test 1, which is not it; it now illustrates with the coarse-sand test 2, and Sellmeijer's own 25 per cent for that test is confirmed verbatim in both his section 8 and his conclusions. **D4 is the one item left open and no number was substituted:** Lane (1935) is not in `docs/references/`, the ASCE copy is paywalled and the Internet Archive holdings that carry the paper body are lending-restricted, and the reliable secondary literature disagrees, USACE ERDC/GSL TN-14-3 saying "278 dam case histories" and Stark et al., who entered Lane's own tables into a database, saying 247 dams with 21 failures; the thesis's "278" stands unedited with the options recorded. **Four things are flagged for a ruling from the author and deliberately not edited**, all outside the approved passage list: the Lane count; the identical `$i_c \geq 0.5$` twin in Chapter 3, whose own table caption two paragraphs earlier writes it correctly as `$i \geq 0.5$`; the fact that both conductivity passages compare a half-band against a guidance **span**, so "materially narrower" is drawn between two constructions and the comparable figure is 6.4; and Appendix G calling the 13 per cent IJkdijk band a "regression scatter" where Sellmeijer defines it as a **drift**, `Hcmva/Hcexp - 1`, the constant offset marking "the imperfection of the model for the small scale tests". Both hard style scans return clean over every `.tex` file after the edits.

- **The unpublished-consultation claims given a citable source (claim-calibration work package 5 of 6, findings F1 and F2, 2026-08-29; `msc-thesis` commit `6970ded`; no engine behaviour touched).** Five thesis claims rested on the 2026-07-06/07 Pol meeting and its 2026-07-08 follow-up email and carried **no in-text attribution of any kind**, so a reader could not see that a source existed at all. All five were verified against `docs/joost_pol_meeting_vragen.md` and `docs/validation/pol-meeting-2026-07-07-dispositions.md` first and **all five hold**: the flood-fighting omission (item 8, "organized flood fighting would likely be very difficult or impossible in such flashy rivers during typhoons"), the plane-strain exponent quoted twice (item 5, "the most pragmatic choice is to assume alpha = -1/3", justified by sub-meter blankets), zero recovery (item 7 plus the email's "realistic assumption, especially for peaks so close together"), and the B25-245 erratum. **The erratum is the one that needed the dispositions and not the meeting note**: the questionnaire's own answer says only that Pol "acknowledged the inconsistencies" and "is currently looking into their exact origins", which would not support the thesis's "confirmed erratum"; the dispositions' follow-up section does, verbatim ("Dit moet inderdaad 0.01 zijn"), so the claim stands and now says who confirmed it and that it was in writing. **Convention chosen: one BibLaTeX `@misc` entry, `pol_2026_pers_comm`, cited with `\parencite` at every site** (the register the thesis already uses for `fukuda_2025_internal`, and the cheapest of the three candidates against the hard page budget: about eleven typeset characters per site, with the bibliography line falling in backmatter). Two wordings were calibrated down to the record: "the realistic assumption" became "a realistic assumption", and the erratum sentence names its confirming party. **A sixth site of the same class was found and attributed identically**: the k_aq-d_70 two-soil endorsement in Chapter 3 (item 4), which the audit's own evidence column listed but its location column did not, and which would otherwise have left the convention non-uniform. Appendix C now names the key, so the citation leads to the register that dispositions each answer.

- **The reliability-index block reconciled against its artifacts, and seven defects corrected (claim-calibration work package 6 of 6, the closing audit, 2026-08-30; record `docs/thesis_number_reconciliation_2026-08-30.md`; `msc-thesis` commit `4f922f9`, four `.tex` files; no engine code, config, artifact or measured value touched).** The 2026-08-21 reconciliation ran one week before the Delta-beta re-expression, the equal-head-convention run and the two rebuilt conditions registers landed, so that block had never been traced to an artifact. 95 claim-groups were traced here, a row of the register being the unit: 71 carry `EXACT`, 22 `ARITH`, 1 `ROUND`, 4 `CITED`, 11 carry `FIXED` across **7 distinct corrections**, and **no new `FLAG` was opened**. Both flags the 2026-08-21 pass left open are closed, neither by this pass: the conductivity "factor of about 2.9" became 2.5 with its construction stated, and the Abashiri 234 h was verified verbatim, both in the 2026-08-29 literature calibration. **All 63 numeric entries of `tab: gap components beta` and all 16 of `tab: equal convention` match their JSON to every printed digit**, and every `B` printed beside its two counts equals their quotient at all 12 sites. **The seven corrections.** (1) The removed fraction of the index difference was quoted as **17 to 46 per cent** at four sites while its own complement, the retained fraction, was quoted as **63 to 83 per cent** at six: 46 divides by KP 57.4's below-floor point estimate 1.558 and 63 to 83 divides by the bound 1.27, so one paragraph in each of four chapters carried both conventions at once. The 2026-08-29 correction had moved seven sites onto the bound and had not carried the change into the complement. Now **17 to 37**. (2) Chapter 6 asserted that the Delta-beta confidence intervals "are the same exact Clopper-Pearson intervals mapped through that transform". **They are not, and neither are the intervals on B**: both are paired percentile bootstraps over the shared realization set (1000 replicates for Delta-beta, 10 000 for `B_eq` and for the ADR-0040 ratio). Only the *per-branch* beta intervals are the mapped ones, verified exactly here at all eight anchor branches to 1e-9. The naive Clopper-Pearson difference at the KP 62.0 anchor is [0.828, 0.983], which is the artifact's `delta_beta_lower_bound` construction, against the reported [0.852, 0.969]. This was the one methodological statement in the block a reader could test. (3) and (4) The factor **10.5 in B and 0.11 in Delta-beta span all four *resolved* epistemic arms**, whose upper end is the `m_p` arm; two sites and the Chapter 9 answers register attributed them to conductivity alone, which in fact moves `B` by 10.4 and Delta-beta by **0.04**. (5) Chapter 9's conductivity-scoped sentence paired "2.59 to 26.9" in probability with "0.86 to 0.97" in index, but 0.97 is the model-factor arm; the conductivity arms give **0.86 to 0.90**. (6) All three below-floor KP 62.0 arms are one-sided bounds (0.47, 0.49, 0.65), not one; the printed 0.49 sat beside a 0.76 endpoint belonging to a different arm. (7) The additive ladder's steps sum to its total **exactly in the artifact at all nine rows** (residual 0.00e+00) but at two decimals four rows differ by 0.01, so the caption now states that entries are rounded independently. **The five whole-document gates all pass**: 373 labels with 0 duplicates and 0 dangling references (a newline-tolerant matcher is required; a naive one reports 32 false positives from line-wrapped `\ref` arguments), 0 em dashes in either form, 0 Japanese-script characters on any non-comment `.tex` line, 0 hyphen or en-dash ranges (the three surviving hyphen pairs are a borehole identifier and `(1 - 1/K)^K`), and eight appendices in both Chapter 1 and `report.tex`. A sixth gate was added: **106 distinct citation keys, 0 unresolved**, including the new `pol_2026_pers_comm`. **The generalisable lesson repeats the 2026-08-29 one from the other side**: correcting a quantity at every site it is *stated* is not the same as correcting it at every site it is *implied*. Defect (1) survived a dedicated four-claim calibration pass because the complement is a different string, and defects (3) to (5) survived because a span across a mixed arm set reads as a statement about the arm that sets one of its ends. **One residual for a ruling from the author**: `figures/rq1_beta_curves.png` was committed by `943d6f9` and is referenced by no `\includegraphics`, so it is either a dropped placement or a surplus file.

- **The last untraced numbers in `msc-thesis` reconciled, and six defects corrected, two of them arithmetic in the Overall Conclusion's base-rate check and four of them missing scope (2026-09-03; record `docs/thesis_number_reconciliation_2026-09-03.md`; thesis-side only, three `.tex` files; no engine code, config, artifact, figure or measured value touched).** The 2026-08-21 pass audited the document as it then stood, at thesis commit `cb590c7`; the 2026-08-30 pass audited only the reliability-index block; the 2026-09-02 pre-push gate compared numeric-token **sets** across the shortening campaign and never checked a value against an artifact. Everything landed between those two passes and outside that block had therefore never been traced. **88 claim-groups traced: 70 carry `EXACT`, 18 `ARITH`, 4 `ROUND`, 11 `CITED`, and 7 carry `FIXED` across 6 distinct corrections; no `FLAG` is left standing.** Nothing in a results chapter or in the Summary is now untraceable. **The pass ran in two rounds on one date:** it first made the two arithmetic corrections below and raised four judgment calls with the text left alone, and the author then ruled that all four be closed, which is what the second round did. **The work package's one unlocated claim is an internal arithmetic construction, and two of its six figures did not survive being reproduced.** The Chapter 8 base-rate check (thesis commit `3b31ec1`, 2026-08-24) rests on no artifact of its own: it composes the four characterized segments' annual piping probabilities in series and reads the result over sixty years. Four figures reproduce: the series composition is 1.07216e-2 (the *sum*, 1.07502e-2, would print 1.08e-2, so the series is the construction), the expected count is 0.6468 by hazard rate or 0.6450 by summed probabilities, both printing 0.65, and `(1-p)^60 = 0.5237` prints 0.52. Two did not. **`0.69` for the probability that none is observed crediting the measured berm is 0.68** on every construction the paragraph could be using: the berm-credited series is 6.3699e-3 per year (KP 58.8 and KP 60.0 from `adr0050-drained-bracket-annualisation-matrix-posterior.json` `arms.berm_only`, the other two unchanged), giving `(1-p)^60 = 0.6815`, `exp(-60p) = 0.6824` and `exp(-60 sum p_i) = 0.6819`; to print 0.69 the expected count would have to be at most 0.3785 against the 0.3822 to 0.3834 these inputs give, and the sentence's own companion figure 0.38 **is** that count. **`0.091` for the failure of at least one of the four at 2016 stages is the sum of the four Phase 2 transient rejection fractions (0.065 + 5.673 + 3.363 + 0.000 = 9.101 per cent), not the union**; under the independent series composition the same paragraph states, `1 - prod(1-r_i) = 0.089045`. That distinction is one the paragraph draws correctly two clauses earlier ("0.65 expected failures **and** a probability of 0.52 that none is observed"), so the mislabel is local; nothing in the argument moves, survival staying modal at 0.911 either way. Both corrections are character-count-neutral, so no line reflowed and no page moved. **Everything else in the hole reconciles**, including all 48 entries of Table 7.2 re-checked in the position the shortening campaign moved them to, the ADR-0050 drained bracket at fifteen sites (the 0.263-to-0.108 and 0.314-to-0.111 conditionals, the 7.4e-3/4.2e-3/2.0e-4 and 1.8e-3/6.4e-4/zero annual triples, the 0.57 and 0.35 and 0.027 annual fractions, the factor 37, the 77 and 76 per cent post-drain relief from `oyo_1998_framing_review_2026-08-24.md` 表7-5-1, and the 0.7 to 1.0 m displacement of the lowest initiating stage, which is `joint_0.20`'s first non-zero grid row at +0.97 and +0.75 m), ADR-0049 at nine (the 89 evaluated levels, the 1.11-to-1.23 and 1.19-to-1.67 split, the 2.2e-16 reciprocal identity), the composition seam at seven, both conductivity studies at fourteen, the hazard-sampling intervals at eleven, foreshore exhaustion at five, the Japanese-practice block at six, and twelve residual groups no named block covered. **Two of those residual groups needed a read-only reproduction, because no companion file carries them:** the initiation-gate curve (the gate reaches 0.99 at 0.749 to 3.107 m below the transient median, printed 0.75 and 3.11, and stands at 0.974 where the transient probability is 8.8e-5 at KP 62.0's design level, printed 0.97 and 9e-5) was recomputed from the persisted Phase 1 matrices and Phase 2 response factors exactly as `scripts/plot_initiation_fragility.py` does, and the Chapter 2 figure caption's leakage geometry (`lambda_in` 38.73 m printed as approximately 39; `r_e` 0.3514 giving `Delta h_blanket` 0.5236 m printed 0.52 on a 1.49 m head difference; 102.5, 116.6 and 87.5 m printed as 87 to 117) was recomputed through `bep_reliability_engine.hydraulics` at the prior means. Neither wrote anything. **A second `\ref`-matcher trap, beside the recorded newline one.** This thesis's labels contain commas (`chap: Study Area, Geological Setting, and Data`), so a matcher that comma-splits `\ref` arguments the way it must split `\cite` and `\cref` arguments reports 14 false dangling references on top of the 24 a non-newline-tolerant scan reports. Split for `\cite` and `\cref`; never for `\ref`. With both handled: **387 labels, 0 duplicates, 0 dangling**, 0 em dashes in either form, 0 Japanese-script characters on any non-comment line, 0 hyphen or en-dash ranges (the 15 survivors are `\cmidrule` column specs, a borehole identifier and `(1 - 1/K)^K`), 106 citation keys with 0 unresolved, and **eleven** appendices agreeing across `report.tex`, the Chapter 1 roadmap figure's A-to-K node and Section 1.6. **An isolated faithful build confirms the ceiling**: main body **99 pages with References on 100**, per chapter 6/10/12/12/11/18/12/11/7, 193 pages in all, zero undefined references and zero undefined citations. **The four judgment calls, raised and then closed, and not one of them moves a digit.** (a) Chapter 8's "every critical rate lies inside the assumed bracket" is exactly true on the design-HWL row set and not on the 2016-event one, where KP 60.0's 27.3 m/h sits a factor of 2.7 above the assumed 0.1-to-10 bracket, the engine's own note hedging with "or at the edge of"; now "every **design-level** critical rate", one word. (b) Chapter 8 compressed the conductivity bracket to a bare "69 to 185" range at two sites, both endpoints `EXACT` but the range dropping the scope Chapter 7 carries; **the climate half of that scope is load-bearing**, because under warming the four spans are 8.27, 27.6, 48.6 and 2762 and none lies between 69 and 185, so both sites now read "69 to 185 ... **historically** at two of the four sections and more at the other two". (c) Appendix F's "25.1 per cent ... **of which** 13.8 per cent" reads as 13.8 per cent of the 25.1 when both are shares of the whole network, the fraction-of-25.1 figure being 55; now "**Of the total length,** 13.8 per cent". (d) Chapter 7's "and more at **the two sections whose lowest arm produces no failures at all**" is true of KP 57.4 historical, whose lowest conductivity arm is exactly zero, and false of KP 60.0 historical, whose lowest arm holds at 5.17e-8 and which the same chapter prints fifty lines earlier; now "and more at **the other two**", which is what the artifact supports (undefined and 4.4e5, both above the 185 just quoted) and which is the only one of the four corrections that makes the text shorter. **The rebuild after them is page for page identical**: main body 99, References on 100, every chapter starting on the same page, 387 labels with 0 dangling, 106 keys with 0 unresolved. **The 2026-08-30 residual is unchanged**: `figures/rq1_beta_curves.png` is still referenced by no `\includegraphics`.

- **Adversarial examiner red team over the finished thesis, and eight prose additions landed at zero page cost (2026-09-03/04; record `docs/examiner_redteam_2026-09-03.md`; thesis-side only, five `.tex` files; no engine code, config, artifact, figure or measured value touched).** The fifteen hardest defence questions were written and graded against the current source, which post-dates `defence_brief_2026-08-22.md` by the RQ1 index campaign, ADR-0051, the terminology audit and the shortening campaign. **Eight of the fifteen are FULLY ANSWERED IN PLACE and volunteered before being asked**, including four of the six lines the work package named: the dominance claim's five qualifications, the gross-head comparator, direction-is-a-theorem, the conductivity bracket, the plane-strain baseline and the Phase 2 zero. **Six graded ANSWERED BUT BURIED and one NOT ANSWERED**, all seven now closed on the page. **The NOT ANSWERED one is the important find: no selection criterion for the canonical hydrograph appeared anywhere in the document.** Chapter 4 said only "a real, pinned ensemble event" and "a compound, multi-peak member of the historical ensemble", and Appendix H's canonical-event provenance compares the two approved members to each other and never to the ensemble, so a committee asking "how did you pick one member out of three thousand, and did you pick the one that gives your answer?" had no printed reply. The answer exists in ADR-0023 and is good: `HPB_m064_1987` sits in the ensemble's **upper duration quartile** (t50 55 h against an HPB median of 40 h [32 to 54]) and among the **9.6 per cent** of historical members carrying a second significant peak, so the choice runs *against* the headline, a longer conditioning wave raising the transient probability and narrowing the reported gap. That is now one sentence in `subsec: Conditioning Sweep and the Canonical Hydrograph Shape`, immediately before the paragraph that says the pinned member reaches the results through one channel only. **The other six closures**: the calibration-domain breach at three of four sections stated where the absolute numbers are quoted rather than only in Ch2/Ch3; the 1998 exit-gradient ordering reconciled as an initiation judgment where the model's own ordering is reported; the commensurability asymmetry added to the paragraph stating the dominance shares, Ch7 having carried it in neither its eight-row conditions register nor its prose; the elongation negative scoped against the annual-maximum extraction window; a pointer from the +4K annual numbers to Ch8's sixty-year record check; the retained 1998 seepage lengths given their direction at KP 58.8/60.0 (the 2025 surface gives 42 and 43 m, so retaining 35 and 34.8 is the conservative choice); and the Ch5 ordering-not-magnitude hedge on Yabe carried into the Overall Conclusion, which had given the three probabilities without it. **The page-cost measurement is the transferable finding, and it refutes the page model.** Six isolated `latexmk -xelatex` builds; the baseline reproduces the 2026-09-02 map chapter for chapter. Three typeset lines in Ch4 bought a page, compacting to 1.5 lines bought the same page, and **so did a change measured at net minus six words** with two offsetting cuts already in it: Ch4's extra page was float repacking, not ink, and came back only after about 70 further words went. Ch6 behaved as ink and was paid off by about 45 words. **In a chapter carrying floats the offsetting cut must be several times the size of the addition, and `ink = f(characters, bands, headings)` does not predict it.** **Nine restatements paid for the eight additions**, each a second or third printing of something that survives verbatim elsewhere with a cross-reference now added: the tanh saturation credits (Ch3 has them), the unremediated-evaluation policy (Ch4's own commitments paragraph and Ch3), two pure signposts, the nine-month reload's 20/140 per cent figures (Ch2 has them), the Clopper-Pearson 1.01 span (Ch5 has it), the 46-and-about-two prose (kept, its second pointer dropped), the posterior separate-and-reconverge clause (its own figure's caption has it), and a duplicated forward pointer. **Final state: main body 99 pages, References on 100, per chapter 6/10/12/12/11/18/12/11/7, identical to baseline; 774 labels and 815 citation instances identical to baseline; zero overfull hbox and zero underfull vbox in both; the one pre-existing `undefined` hit unchanged.** **Three residues stay open and are not editable from existing evidence** [all three CLOSED the same day, 2026-09-04, on the author's instruction; see the following entry and `docs/decisions/examiner-residues-closed-2026-09-04.md`]**:** the peak-only over-rejection's *direction* is generalised from two conditioning events, both longer than the 2016 record at the same peak, so the sign would reverse for a shorter conditioning wave and a third short-tail member would settle it; Ch8's "0.65 expected failures in sixty years against zero observed" is a consistency test whose power is not stated, and a model an order of magnitude lower or a factor of three higher would also pass it; and Ch4 justifies retaining the full `I_er` gate structure partly because it "becomes load-bearing again the moment a sensitivity analysis decouples the critical heave gradient from the Terzaghi value", a sensitivity reported nowhere, so either run the decoupled-gradient arm or rest the justification on the compound-event clause alone, which is the half that is evidenced.

- **The three red-team residues closed, none of them needing a new sweep (2026-09-04, on the author's instruction; record `docs/decisions/examiner-residues-closed-2026-09-04.md` and its JSON; three thesis sentences in Ch4, Ch6 and Ch8; no engine module, config, persisted sweep, geotechnical CSV, figure or measured production value touched, and no ADR).** The preceding entry left three residues open as "not editable from existing evidence". All three close, and **two of the three closed against evidence the repository already held**, which is the finding worth recording: the red team's own judgment that they needed new work was wrong twice. **R1, the peak-only over-rejection's direction, closed with its premise refuted.** The residue held that the direction claim was generalised from two conditioning events both LONGER above the toe than the 2016 record, so the sign would reverse for a shorter conditioning wave, and asked for a third member from the short tail of the ensemble's t50 distribution. There is nothing to draw: recomputing ADR-0023's shape diagnostic over all 3,000 HPB members at the KP 58.8 rating reproduces it **exactly** under its own stated definitions (t50 median 40 h, IQR [32 to 54], peaks median 1 and mean 1.10, compound fraction **9.6 per cent** under `scipy.signal.find_peaks(shape, height=0.3, prominence=0.2)`), and places the approved alternate `HPB_m067_1978` at **t50 quantile 0.005**, the lowest half per cent, against an ensemble minimum of 17 h to its own 21 h. That same scan independently verifies both quantities the 2026-09-04 Chapter 4 edit added, the production member's upper-quartile position (quantile **0.756**) and the one-in-ten compound fraction. **The decisive measurement is the above-toe comparison at one common peak, counted on one rule**, the real record built through the committed ADR-0035 ingestion and the members through the verbatim `conditioning_record_for_level` path: the reconstruction reproduces the thesis's own 9 / 24 / 31 / 6 h, and at the two informative sections the alternate stands above the toe for **23 h against the real record's 24 (0.96x) at KP 58.8 and 26 h against 31 (0.84x) at KP 60.0**, where the production member stands 2.50x and 2.00x as long. The committed peak-shortcut slice gives factors of 1.448 and 1.568 on the alternate against 2.749 and 3.899 on the production member. **The two approved members therefore bracket the survived loading's own duration and the peak-only reading over-rejects on all four arms**, so the direction is a property of the method and not of a conditioning wave longer than the event it is read against. **The mechanism is worth keeping**: above-toe duration is not the currency the equilibrium barrier charges in, a pipe advancing only where the erosion head clears H_eq and therefore only near the crest, so the real record's 24 and 31 hours spread over sub-peaks far below its own crest lose to a rescaled single-crest member whose t90 is 5 h. **R2, the base-rate check's power, closed by two lines of exact Poisson arithmetic on numbers already printed.** Zero events in 60 years put the one-sided 95 per cent upper bound at `-ln(0.05)/60 = 4.99e-2` per year, so the record **excludes any annual probability above about 5.0e-2 per year and excludes nothing below the reported value**: headroom **4.7x** over the as-if-undrained series 1.07216e-2 (0.643 expected, P(none) 0.526) and **7.8x** over the berm-credited 6.3699e-3 (0.382, 0.682). The asymmetry is the point and it is favourable, because the objection the check answers is that the probabilities are implausibly high, which is the only side sixty years of nothing can discriminate on. **R3, the full I_er gate's decoupling justification, closed as a deduction and never needed a run.** Under the Terzaghi substitution the heave threshold is a deterministic function of the SAME sampled gamma'_bl that sets the uplift threshold, which is exactly why `Z_heave = Z_uplift/D_bl` and the latch is redundant; any decoupling draws the gradient separately, and two independent draws cannot change sign at the same instant except on a measure-zero set, so the latch binds wherever they separate. "Load-bearing" follows from the independence, not from an effect size, so a sensitivity run would add nothing to the argument. ADR-0008 already recorded the displaced alternative (Pol's `i_c,h ~ Lognormal(mu 0.7, sigma 0.1)`, CoV 0.143, after Schweckendiek et al. 2014 and SIE 2024 Table 2) and its decision 2 already made the same forward-looking claim in the same words; Chapter 4 already carried the direction of the consequence and Appendix B already carried the 0.143-against-0.056 spread comparison. **Only the reason was missing, so this was a wording defect and not a missing measurement**, and no `i_c,h` knob was added: it would be an eighth random variable against ADR-0001's seven, the trade ADR-0008 weighed and refused. **Page budget held at zero cost again**, and the offsetting mechanics of the preceding entry repeated: the R1 addition is about 3.4 typeset lines and was paid for by three further Chapter 6 restatements (the "exactly the information the peak approximation would have fabricated" clause, which is Chapter 4's own sentence; the excluded-strata paragraph's duplication of its own figure caption; and the 24-to-42-per-cent and 2.75-and-2.92 strings, which the chapter's standing-conditions register carries), the R2 addition sits inside Chapter 8's existing slack and the R3 edit is net five words. **An isolated faithful build confirms it: main body 99 pages with References on 100, per chapter 6/10/12/12/11/18/12/11/7, byte-for-byte the baseline chapter map, 774 labels and 815 citation instances identical to baseline, zero overfull hbox and zero underfull vbox in both, and the one pre-existing `undefined` hit unchanged.** **Two residues remain open by construction and neither is closable here**: the corrected fluvial-scour branch has no validation of its own, its equivalence gate testing it against the received implementation only, and the compound-event memory model cannot be tested with a survival record, since a survival cannot discriminate an assumption that only ever raises the computed probability.

- **The repository prepared for publication and released as v1.0.0 (2026-09-05; `README.md`, `CHANGELOG.md`, `CITATION.cff`, `pyproject.toml`, `data/raw/README.md`; no physics module, configuration, persisted artifact, figure driver or measured number touched; gates unchanged at ruff clean, black clean, 910 tests green).** The engine and its evidence base were complete; what was missing was everything that makes the repository legible from outside it. **Four defects were real, not cosmetic.** (1) `CITATION.cff` carried the DOI `10.4121/8ffa1f3e-942e-4852-b02a-a259b9d6d00d`, which **does not resolve**. The first reading of that was wrong and is corrected here: the DOI is **reserved and valid**, from a 4TU.ResearchData deposit created earlier in the project. A reserved 4TU DOI is *inactive by design* and begins resolving only when the deposit is published, so a 404 is the expected state before submission and is not evidence of a fabricated identifier. The lesson is that **a DOI that 404s is not the same as a DOI that does not exist**, and the registry's own reservation semantics have to be checked before concluding anything from the resolver. The file now carries the DOI, the `url` points at it in preference to the GitHub URL, and the ORCID beside it was checked against the public record and **is** correct. (2) `packages.find` carried no `include` filter, so the distribution installed **`tests` as a top-level package** alongside the three real ones — verified fixed against a fresh wheel build, whose top-level entries are now exactly the three. (3) `scripts/rq1_beta_analysis.py` hard-coded an absolute machine-local path for its optional figure mirror; it now reads `BEP_THESIS_FIGURES` and writes nothing outside this repository when unset, which is the only state a fresh clone has. (4) The operating-system classifier claimed Windows only while CI had been running the suite on Ubuntu throughout. **The README had drifted in exactly the way the 2026-07-31 audit predicts of an unguarded count:** it claimed 52 figures against 71 and ADRs 0001–0048 against 0001–0051, and named a machine-local path as the location of the thesis; `conventions.md` §9.2 carried the same stale ADR range. **The one genuinely new document is `data/raw/README.md`**, now tracked inside an otherwise-ignored directory: the 143-file, 98.0 MB third-party drop stays out (a climate-model discharge ensemble supplied under collaboration, agency survey and rating tables, Kunijiban borehole logs, GSI elevation tiles — none of it ours to relicense), but its required layout, per-source provenance keyed to the governing ADR, and a **SHA-256 manifest of every file as used for every result here** are now publishable, which is the standard treatment for restricted inputs and closes the one real reproducibility hole a reader would hit. **`docs/work_packages/` was retired** — seven task briefs, every one executed and its outcome carried by the ADR, companion note or report it commissioned, with ADR-0051's cited pre-registration reproduced inside the ADR itself; the nine inbound references were retargeted, including the `plan_of_record` field that `scripts/rq1_beta_analysis.py` writes into `rq1-beta-reexpression.json`, so nothing dangles. The 2026-07-31 audit ruled KEEP on that directory; it is a **dated record and was annotated, not rewritten**. **Working-process wording was neutralised**: the deciding role read as "the owner" at 93 sites and is now "the author", and a few dated audits described their own execution rather than their findings. **Two occurrences are deliberately left standing, and the second is the transferable point:** ADR-0010 uses "owner" in its software sense, of the module that owns a value; and `canonical-shape-sensitivity.md` is a pre-registration whose Part 1 SHA-256 is pinned in its evidence record, so the one-word edit **failed `test_the_record_pins_the_preregistration_it_was_measured_against` and was reverted rather than re-pinned** — re-pinning a frozen pre-registration to accommodate a cosmetic change is exactly the edit the digest exists to make visible. **Git topology.** `origin/main` held a five-commit stub with **no common ancestor** with `develop` (the working line was started fresh), so `main` was moved to the release tip; the stub stays reachable through the already-pushed `v0.0.1` tag, which is what makes the move lossless. Ten merged feature branches were deleted locally and the five of them that had ever been pushed were deleted on the remote, leaving `main` and `develop`. **Continuous integration, and what it had been hiding.** The badge had been red since **2026-07-02** — 64 of the last 84 runs — and the work had gone on locally without it. Two independent causes, found by cloning the published tag into a scratch directory and running the suite there. (1) **Twelve tests read the untracked `data/raw` drop with no skip guard**, so they errored with `FileNotFoundError` on any clone without it: three `load_hwl` tests, the eight parametrized rows of the config drift guard, and the kasumi-tei coincidence audit. The suite already has the idiom for this and applies it in five other modules; these were simply missed. They are guarded now, and **where a test mixed tracked and untracked sources the untracked half was split into its own guarded test rather than letting a skip cost the tracked half** — the config drift guard keeps its other forty assertions on a fresh clone, and the kasumi-tei register keeps its counts. Fresh clone: **878 passed, 41 skipped, 0 failed**, against 12 failed before. (2) **Four tests fail only on Linux**, in `test_evaluator_batch_diagnostics.py` (2), `test_gsa_qoi.py` and `test_model_factor.py`: the vectorized batch path and the scalar loop differ in the **last representable bit** (`Z_static` -2.6277282510615922 against ...905), which is numpy taking different SIMD and libm paths for arrays than for scalars. **The headline bit-identity gate, `test_run.py::test_orchestration_matches_reference_loop`, passes on Linux too**, and all four pass on Windows. The runner moved to `windows-latest` — the platform every persisted result, figure and thesis number was produced on — and **no assertion was relaxed**, so the difference stays visible; it is disclosed in the README under *Platform*. Local suite 910 → **919** (the split-out HWL check is eight parametrized rows, the kasumi-tei split one more).
