# Final-Campaign Close-Out (session of 2026-07-12/13)

> **SUPERSEDED (header added 2026-07-31; content unchanged).** This document
> records the 2026-07-12/13 campaign as it stood then, and is retained as the
> historical record of that session. It is **not** the current state:
>
> * the campaign itself is superseded by
>   [`docs/production_campaign_2026-07-29.md`](production_campaign_2026-07-29.md),
>   the definitive end-to-end re-execution (gates G1 to G7);
> * its section 3 blocker manifest (D1/D2/D5/D7 and the Uemura CSV contract,
>   2011 stage record and OYO longitudinal profile) is superseded by
>   [`docs/phase3_report.md`](phase3_report.md) section 9 -- D1/D2 closed by
>   ADR-0042/0043, D7 closed 2026-07-22, the 2011/2006 event question closed by
>   ADR-0044;
> * the "433 passed" gate count below is that session's, not the current one.
>
> Quantitative numbers here predate the ADR-0047 seepage-length adoption
> (2026-07-29) and must not be quoted against current runs.

Mission: close the gap between the repository state and "the owner can run
one final analysis campaign that answers all four thesis research
questions" (RQ1 transient-vs-static conditional P_f; RQ2 the 2016 Bayesian
constraint; RQ3 multi-mechanism dominance with Uemura's surface curves;
RQ4 climate-shifted basin failure probability from the d4PDF ensembles).
Branch: `feature/final-campaign-closeout` (off
`feature/phase2-bayesian-updating`). Full gates green at close: `ruff`,
`black`, `pytest` (**433 passed**).

Everything resolvable without owner input has been resolved. Section 3 is
the blocker manifest: the complete list of what only the owner can supply,
with the exact interface each item must arrive in.

---

## 1. Triage table (Phase A census -> final status)

Classes: A = resolved by analysis/code, B = resolved by compute, C =
decision made here (recommended, documented, reversible), D = hard-blocked
on owner data / external input.

| # | Item | Source | Class | Final status |
|---|---|---|---|---|
| 1 | 4 bulk-d70 co-primary sweeps missing | `configs/*_bulk.yaml` vs `results/` | B | **DONE.** All 4 run at N=1e5/225 s (`results/tokachi_*_historical_bulk.h5`). Bulk is the resistant reading: max raw P_f,trans 0.15-0.38; KP57.4 static still fits (max 0.59). |
| 2 | KP58.8 r_e-halved QA member (registered) | `scripts/run_sweep.py` header; architecture.md open list; `shikaga-case.md` §3.4 | B | **DONE, measured.** `scripts/qa_re_halved_member.py`; baseline drift guard bit-identical at all 29 levels; results §2.2 below. Registration sites updated. |
| 3 | Phase 2 production update at full N | `phase2_report.md` §9.1-9.2 | B | **DONE.** All 8 strata, `--verify` EXACT everywhere. `results/phase2/`; report §11. |
| 4 | Anchor sensitivity (`--anchor rating`) | report §9.3, §8.1 | B | **DONE** (4 matrix members; bulk members reject ~0 so uninformative — documented choice). First-order term: §2.3. |
| 5 | Criterion sensitivity (`no_breach_no_initiation`) | report §9.3 | B | **DONE** (matrix members). Confirms the strict criterion is qualitative-only (posterior collapse at KP58.8). |
| 6 | Marginal-informativeness re-verify at production N | report §9.7 | B | **DONE: exactly 0.000 in every stratum.** The nesting is not a small-N artifact. |
| 7 | λ_ac undetermined / length effect unwired | architecture.md "Genuine open decisions"; architecture and decision records | C | **DONE: ADR-0037.** λ_ac = 250 m primary (Kanning 2012 Table 4-7 p. 90 + §4.7.3; Schweckendiek 2014 Table 7.1 — both PDFs local), bracket 100/40 m. Config-gated OFF; segment tables produced (§2.4). Reversible: one config field; revision trigger = OYO longitudinal profile. |
| 8 | Phase 3 composition layer absent (RQ3+RQ4) | mission; thesis ch. 4 | B/C | **SKELETON DONE + FIRST REAL RUN: ADR-0038**, `system_integration` package (23 tests). Hazard side runs on the real d4PDF workbooks **now**; RQ4 headline numbers exist (§2.5). RQ3 blocked only on Uemura's curves (seam defined, D-item 1). |
| 9 | Uemura machine-readable surface curves | thesis §3 "Pre-Calculated Surface Failure Fragility Curves" | **D** | Interface + validating loader + stub built (`system_integration/surface_curves.py`). See manifest. |
| 10 | Uemura section-aggregation table (Tokachi 1-5 / Satsunai 1-4) | thesis scope; architecture.md Tradeoff 3 | **D** | Loader + contiguity validation built (`segments.load_section_table`). See manifest. |
| 11 | 2011 sequential survival event | `phase2_report.md` §10 | **D** | Architecture sequential-ready; needs items §10.1-10.3. See manifest. |
| 12 | OYO longitudinal soil profile (土層縦断図) | thesis §3.8; ADR-0037 | **D** | λ_ac empirical-revision trigger. See manifest. |
| 13 | tanh l_c field-scale validity (3D hole-exit, ~1.5-2.2x under-prediction) | `m7-pol-ode-reference-values.md` §6.1 | **D** | Awaiting Pol. Unchanged: M6 keeps the published Eq. (13). See manifest. |
| 14 | H_eq ≈ 1.0·H_c at field scale (the ≈1.95x progressive-phase conservatism) | m7 note §6/§7; ADR-0009; architecture open list | **D** | Awaiting Pol; carried in FM4 attribution. See manifest. |
| 15 | KP62.0 transient transition unbracketed | architecture open list | — | Unchanged **by design** (ADR-0024 raw-tail deliverable). Production consequence now measured: the 2016 update is vacuous there (§2.3, report §11.2). |
| 16 | Tokoro boil-site case | validation campaign follow-up | C | **Plan drafted** (`docs/validation/tokoro-case-plan.md`). Upgrade found: the committee report IS local (`icrceh00000032zs_compressed.pdf`); only the Tokoro gauge 2016 stage record is class D. Recommendation: defer (synthesis already uses Tokoro as corroboration). |
| 17 | 4-case synthesis consolidation | campaign memory | A | **Verified already done**: `_thesis_validation_japan.tex` is the four-case synthesis (Tokoro included as corroboration-only, deliberately, with its own table row). No action. |
| 18 | Stale `OPEN-*` filename | `docs/validation/` | A | **Renamed** `head-datum-re-convention-CLOSED.md`; pointer in `reference-anchor-status.md` updated. |
| 19 | architecture.md stale flags (Phase 2 replay Δt bullet + §13 1800 s row; λ_ac and QA-member open bullets) | architecture.md | A | **Fixed**: replay-Δt discharged per ADR-0036; λ_ac discharged per ADR-0037; QA member marked executed; §13 rows added for ADR-0037/0038. |
| 20 | ADR-0010 "M6 docstring stale hint" (`lambda_out_params`) | mission B6 | A | **Verified already discharged** — the hinted text no longer exists in `sellmeijer.py` (grep clean). |
| 21 | m7 note §7 stale C_e prior row (0.014/0.50) | m7 note vs ADR-0026 | A | **Dated errata added** under the §7 table (historical record deliberately not rewritten). |
| 22 | M9 bootstrap vectorization (~19 s Python loop) | ADR-0029 "next bottleneck" | B (opportunistic) | **DEFERRED, deliberately.** Post-ADR-0029 the bootstrap is ~20% of a 90-190 s per-section sweep and no longer gates any campaign; a statistical-identity-preserving rewrite is exactly the kind of change not to make in the same session that produced the production artifacts. Pin requirement documented here for whoever picks it up: seeded test asserting identical bands to the loop implementation. |
| 23 | Drains not modeled at KP58.8/KP60.0 (`remediation_state` is a label) | thesis §4 tiering text vs engine behaviour | **C/D** | **Flagged loudly** (report §11.2, architecture and decision records). The production posteriors at the drained sections are as-if-undrained constraints. Owner decision needed on thesis presentation (see manifest, decision item). |
| 24 | Interpolated priors for borehole-free reaches (KP53.8-56.0, Satsunai) | thesis §3 scope | D/C | Future work; needs its own ADR (uncertainty-inflation policy). The Phase 3 registry carries these segments with `bep_source=None` (honest gap; `nearest` policy exists but is stamped and non-default). |
| 25 | 2016 stage workbook September sheet corrupt | phase2_report §2 | — | Unchanged; proven harmless (window closure below toe at all 4 sections). Listed for completeness. |

---

## 2. Headline numbers produced this session

### 2.1 Bulk-d70 production sweeps (new; N = 1e5, 225 s, ADR-0026/27/28/30 physics)

| Member | Static max raw P_f (form) | Transient max raw P_f (form) |
|---|---|---|
| KP57.4 bulk | 0.590 (fitted, deliverable) | 0.378 (raw tail) |
| KP58.8 bulk | 0.262 (raw tail) | 0.152 (raw tail) |
| KP60.0 bulk | 0.993 (fitted) | 0.839 (fitted) |
| KP62.0 bulk | 0.427 (raw tail) | 0.234 (raw tail) |

The bulk interpretation is systematically the resistant reading (larger
d_70 -> higher H_c), consistent with its role as the co-primary bound.

### 2.2 KP58.8 r_e-halved QA member (ΔP_f per conditioning level; `results/qa_re_halved_kp58_8.json`)

Baseline = the production matrix member, reproduced bit-identically by the
harness at scale 1.0 (drift guard). Halving r_e produced **zero new
failures** (r_e drives only the uplift/heave gate since ADR-0027/0028 —
pure one-sided gate sensitivity; standard r_e confirmed conservative).

| Level [m MSL] | P_f base | P_f halved | ΔP_f | ratio |
|---|---|---|---|---|
| 39.75 | 0.00042 | 0.00000 | -0.00042 | 0.00 |
| 40.00 | 0.00477 | 0.00001 | -0.00476 | 0.002 |
| 40.50 | 0.07470 | 0.00633 | -0.06837 | 0.085 |
| 41.00 | 0.26273 | 0.09325 | -0.16948 | 0.355 |
| **41.25** | **0.37803** | **0.19667** | **-0.18136** | **0.520** |
| 42.00 | 0.68133 | 0.58562 | -0.09571 | 0.860 |
| 43.50 | 0.93743 | 0.92797 | -0.00946 | 0.990 |
| 45.00 | 0.98779 | 0.98668 | -0.00111 | 0.999 |

Shoulder-concentrated exactly as the Shikaga bound predicted; the
transition level itself moves < 0.5 m. The M4-translation question is now
a measured magnitude, not a bound.

### 2.3 Phase 2 production posteriors (all 8 strata; verification EXACT everywhere)

| Stratum | Transient rej. | Static rej. | Marginal transient | ΔC_e mean | Δk_aq mean |
|---|---|---|---|---|---|
| KP57.4 matrix | 0.07% | 6.26% | **0.000** | -0.1% | -0.1% |
| KP58.8 matrix | 5.67% | 57.63% | **0.000** | -4.1% | -4.2% |
| KP60.0 matrix | 3.36% | 73.31% | **0.000** | -3.7% | -3.0% |
| KP62.0 matrix | 0.00% | 0.00% | **0.000** | 0 | 0 |
| all 4 bulk | ≤ 0.02% | ≤ 1.84% | 0.000 | ~0 | ~0 |

* **Marginal transient rejection is exactly zero at production N in every
  stratum** — the §9.7 re-verification. The 2016 survival adds no
  rejection beyond the static criterion anywhere; its value for the
  transient mechanism is the *calibration* asymmetry (5.7% vs 57.6%
  "should have failed" at KP58.8).
* WBI+ peak-shortcut over-rejection at production N: **2.75x** (KP58.8),
  **3.90x** (KP60.0).
* Anchor sensitivity (rating vs trace): KP58.8 5.67% -> 10.81%, KP60.0
  3.36% -> 0.34%, KP57.4 -> 0.00%, KP62.0 -> 0.01%. First-order,
  section-specific in direction; report baseline + bracket together.
* Criterion sensitivity (no-initiation): 66.4% rejection at KP57.4,
  collapsed posteriors at KP58.8 (432 rows kept, 99.57%) and KP60.0
  (696 rows, 99.30%; both auto-flagged statistically meaningless), and
  **30.46% at KP62.0** — the one usable strict posterior, showing the
  initiation margin (gate latches in ~30% of the prior) separating
  cleanly from the progression margin (breach rejection exactly 0) at
  the governing section. The strict reading stays qualitative overall,
  as ADR-0036 documented.
* **Interpretation caveat (new, load-bearing):** the informative updates
  land at the *drained* sections because the engine evaluates the
  unremediated foundation everywhere (drains are labels). KP62.0's update
  is vacuous because its transition sits ~4 m above attainable stages.
  See report §11.2 and the manifest decision item.

### 2.4 Segment-level fragility under ADR-0037 (`results/segment_fragility_adr0037.json`)

Primary λ_ac = 250 m ⇒ n_eff = 1: **the 200 m segment curve equals the
cross-section curve** — a finding (Kanning's own blanket-thickness
correlation distance spans the segment), not an omission. Conservative
bracket at the worst measurable transient shoulder point (posterior,
matrix):

| Section | P_cs (shoulder) | segment @ λ=100 m (n_eff=2) | @ λ=40 m (n_eff=5) |
|---|---|---|---|
| KP57.4 | 0.463 | 0.711 | 0.955 |
| KP58.8 (post.) | 0.461 | 0.709 | 0.954 |
| KP60.0 (post.) | 0.405 | 0.646 | 0.925 |
| KP62.0 | 0.496 | 0.746 | 0.968 |

### 2.5 Phase 3 first real run (ADR-0038; BEP-only, posterior matrix curves, real d4PDF hazard)

Annualized BEP failure probability per section (ensemble mean of the
composed curve at each annual-max event peak; HPB = 3,000 yr, HFB = 5,400
yr; `results/system_integration/`):

| Section | Historical [1/yr] | +4K [1/yr] | Ratio |
|---|---|---|---|
| KP57.4 | 7.6e-4 | 9.5e-3 | **12.5** |
| KP58.8 | 7.4e-3 | 4.1e-2 | **5.5** |
| KP60.0 | 1.8e-3 | 1.4e-2 | **7.8** |
| KP62.0 | 3.5e-4 | 4.4e-3 | **12.5** |

Climate enters ONLY through the stage-frequency (ADR-0023 honored
structurally). Caveats stamped in every output: BEP-only (no surface
curves yet), as-if-undrained at KP58.8/60.0, conditional on the
canonical-shape conditioning of the fragility curves. These are the first
RQ4 numbers; they become RQ3+RQ4 numbers the day Uemura's CSV arrives.

**RQ4 attribution machinery, demonstrated live** (KP58.8, posterior,
above-toe exposure stratifiers now threaded through the CLI):

| Metric | Historical (3,000 yr) | +4K (5,400 yr) |
|---|---|---|
| Years loading the toe | 24.7% | 40.8% |
| Median hours above toe (when loaded) | 15 h | 19 h |
| Years with > 24 h above toe | 5.1% (152) | 13.5% (727) |
| Conditional P_f, long-loading years | 0.129 | 0.273 |
| Conditional P_f, short-loading years | 0.0009 | 0.0044 |
| Compound years (≥ 2 excursions), share of loaded | 4.7% | 5.9% |
| Conditional P_f, compound years | 0.026 | 0.076 |

Annual BEP risk concentrates ~150:1 in the long-duration-loaded years,
and the +4K shift nearly triples the frequency of exactly those years —
the duration/compound attribution RQ4 asks for is now one predicate call
(`system_integration.annualize.stratified_annual_p_f`).

---

## 3. BLOCKER MANIFEST (class D — only the owner can supply these)

> **STALE AS A MANIFEST (header added 2026-07-29). The current manifest is
> `docs/phase3_report.md` §9.** The list below is preserved as written on
> 2026-07-12/13; four of its items have since closed, and each is marked
> inline: **D1 and D2** by the ADR-0042 / ADR-0043 re-execution and section-table
> reconstruction (2026-07-17/18), **D3** by ADR-0044's event-set closure at 2016
> (2026-07-18), and **D7** — raised after this document and therefore absent
> below — by the measured H–Q rating error of 2026-07-22. D4, D5, D6 and the
> class-C drained-section decision stand as written. One caveat when reading the
> current manifest: `phase3_report.md` §9 lists D3 as "unchanged from the
> close-out" because it was written days before ADR-0044; ADR-0044 governs there.
> Nothing else in this document has been rewritten.

**D1. [CLOSED 2026-07-17/18 by ADR-0042 — surface curves obtained by faithful
re-execution of Uemura's own models rather than by a curve-table delivery; see
`docs/phase3_report.md` §9.] Uemura's overflow + fluvial-scour fragility curves
(unblocks RQ3).**
Machine-readable, in the exact interface defined by ADR-0038 decision 5
and enforced by `system_integration/surface_curves.py::load_surface_curves`:
long-form CSV, UTF-8, header `river,bank,kp,mechanism,scenario,stage_m_msl,p_f`;
`river` in {Tokachi, Satsunai}; `bank` in {right, left}; `kp` on the 0.2 km
grid; `mechanism` in {overflow, fluvial_scour}; `scenario` in {historical,
+4K} (`plus4K` accepted); `stage_m_msl` in T.P. metres (the ADR-0021 MSL
datum — convert crest-relative curves BEFORE the CSV and document the
offset); `p_f` in [0,1], non-decreasing in stage, ≥ 2 stages per curve.
Source: Uemura (2025) PhD workproduct / uemura_wp2_2024 — the thesis PDFs
are local but carry no machine-readable curves. What already consumes it:
`python -m system_integration --surface-csv <file>` composes and
annualizes immediately (stub-tested end to end).

**D2. [CLOSED 2026-07-18 by ADR-0043 — the 9-section table was reconstructed from
Uemura's own `SECTIONS.shp` and validated against his segment count; an
owner-supplied table would now replace one committed CSV verbatim.] Uemura's
section-aggregation table (segment -> Sections Tokachi 1-5 /
Satsunai 1-4; unblocks section-level RQ3/RQ4 reporting).** CSV
`river,bank,kp_from,kp_to,section_id`; validated by
`system_integration/segments.py::load_section_table` (contiguity +
coverage checks). Possibly derivable from Uemura's thesis figures — if so,
say which figure/table and I can digitize; until then results are
per-segment.

**D3. [CLOSED 2026-07-18 by ADR-0044 — the event set is closed at 2016. The
2011/2006 drops arrived with empty stage directories, and the sustained-peak
bound rejects zero rows at 7 of 8 strata, bounding the 2011 marginal beyond 2016
at 0.316%. Note `phase3_report.md` §9 still lists this item as open; it predates
ADR-0044.] The 2011 Obihiro stage record (strengthens RQ2; not blocking).**
Per `phase2_report.md` §10 items 1-3: (i) hourly observed stage at the
Obihiro gauge, window ~2011-08-25 to 2011-09-20, same workbook family as
the 2016 drop; (ii) confirmation of 2016-style survival for the study
reaches in 2011; (iii) optionally a 2011 flood-mark/maximum-stage record
near the study KPs (else the run uses `anchor='rating'`, now with a
measured first-order anchor sensitivity — see §2.3). What consumes it:
the sequential-event machinery is built; a 2011 event is one
`ObservedEventSource` + one `event_records` entry.

**D4. OYO longitudinal soil profile 土層縦断図 (upgrades ADR-0037 from
literature to site-specific).** The report-appendix continuous A_c
thickness profile (OYO 1999). What consumes it: method-of-moments
correlogram (Kanning eqn. 4.20-4.21) -> replace λ_ac = 250 m; one config
field, no code change (the ADR names this its revision trigger).

**D5. Pol confirmations (m7 note §6; sharpen FM4 attribution, not
blocking).** (i) Was the tanh l_c (Eq. 13) validated at field scale /
3D hole-exit geometry, or should a 3D-corrected l_c be substituted?
(every checkable 3D case exceeds the formula by 1.5-2.2x). (ii) Does
DgFlow's effective H_eq stay ≈ 1.0·H_c at field scale (the ≈1.95x
progressive-phase conservatism, ADR-0009)? What consumes them: the FM4
four-component gap attribution in the Discussion; ADR-0017's α hook and
the module constants are already in place for either answer.

**D6. Tokoro 2016 gauge stage record (only if the Tokoro case is ever
executed; recommendation: defer).** Hourly stage at the Tokoro main-stem
gauge (太茶苗 or nearest), window 2016-08-17 to 09-05. Everything else
for that case turned out to be local (`tokoro-case-plan.md`).

**Decision item for the owner (class C, made reversibly here, please
ratify or override): the drained-section presentation.** The engine
computes as-if-undrained posteriors at KP58.8/60.0 (drains are provenance
labels). Options: (a) keep as computed and present the drain credit as a
separate qualitative layer (what report §11.2 does now — recommended,
zero engine change); (b) model the drains (new ADR, new physics, needs
drain geometry/conductance data — a real scope extension). The thesis §4
text currently *implies* (b)-style behaviour ("exit head set to zero");
either the text or the model must move.

---

## 4. Proposed ordering of YOUR next actions to final-results readiness

1. **Ratify the two class-C decisions** made this session (10 min):
   ADR-0037's λ_ac = 250 m primary (or hand me the Kanning pages you
   prefer anchored differently) and the drained-section presentation
   (manifest decision item). Both are one-line reversals if you disagree.
2. **Request D1 + D2 from Uemura now** (the only RQ3 blockers; everything
   else composes the moment they land) and **D3 items 1-2 from the data
   provider** (RQ2 strengthening, longest lead time, zero code cost).
3. **Read `docs/phase2_report.md` §11** and decide which stratum table
   goes into thesis Chapter 5; the marginal-transient-zero finding and
   the 2.75-3.9x shape factor are the two production numbers the text
   must carry exactly.
4. **Run the +4K/RQ4 stratification you want for Chapter 6** with the
   built machinery (`system_integration.annualize.stratified_annual_p_f`
   takes any predicate over duration/compound-count/peak — say which
   strata and I'll script the figures), then send D5 to Pol with the two
   questions verbatim from the manifest.
5. On D1/D2 arrival: `python -m system_integration --surface-csv ...`
   (+ section table) — RQ3 mechanism-dominance results are then one
   command; on D3 arrival: one CLI call for the sequential 2011 update;
   on D4 arrival: re-estimate λ_ac and regenerate configs.
6. Optional hygiene when convenient: merge this branch; the M9 bootstrap
   vectorization stays parked with its pin requirement (triage #22).

---

## 5. Session artifacts

Commits (this branch): QA member (`08267ee`), ADR-0037 length effect
(`d3f94da`), ADR-0038 Phase 3 package (`0b69675`), hazard datum threading
+ RQ4 stratifier fix (`1de59f5`), docs/close-out (`5cb2308`) plus this
amendment. New ADRs: 0037, 0038. New packages/modules:
`system_integration/` (7 modules), `scripts/qa_re_halved_member.py`,
`scripts/segment_fragility.py`. New results: 4 bulk sweeps, 8 baseline
posteriors + 4+4 sensitivity posteriors, QA-member table, segment tables,
8 Phase 3 composition files + hazard caches. Docs: phase2_report §11,
architecture.md open-list/§13 refresh, architecture and decision records refresh, Tokoro plan,
OPEN-* rename, m7 errata. Suite: 433 passed; ruff/black clean.
